# Manual emergency caller for the legacy Grok Build PR review.
# Implementation lives in RetireGolden/.github — do not copy it here.
#
# This workflow intentionally has no pull_request trigger. It is independent
# of the required OpenRouter workflow and spends xAI tokens only when a human
# explicitly dispatches it for a PR number.
name: Grok code review

on:
  workflow_dispatch:
    inputs:
      pr_number:
        description: PR number to review
        required: true
        type: string

permissions:
  contents: read
  pull-requests: write
  statuses: write
  checks: write

jobs:
  review:
    uses: RetireGolden/.github/.github/workflows/grok-code-review.yml@571cb618da27029371b3468ee3ad2ad580f42d2d # independent OR cutover; manual Grok safeguards
    with:
      pr_number: ${{ inputs.pr_number || github.event.inputs.pr_number || github.event.pull_request.number }}
    secrets:
      XAI_API_KEY: ${{ secrets.XAI_API_KEY }}
