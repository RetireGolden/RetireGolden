/**
 * Coverage attestations for `rules/`.
 *
 * One slice of the coverage attestation registry. `../coverageAttestations.ts`
 * composes every slice into `COVERAGE_ATTESTATIONS`; read it for what an
 * attestation means and how sweeps work. Entries were moved here verbatim from
 * the single file this registry used to be.
 */
import type { CoverageAttestation } from '../coverageAttestations.js'

export const rulesAttestations: Readonly<Record<string, CoverageAttestation>> = Object.freeze({
  'rules/attestations/actions.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-09-03', note: 'registry/coverage tooling; one slice of the coverage attestation data, split out of coverageAttestations.ts; enforced by coverageAttestations.conformance.test.ts' }),
  'rules/attestations/allocation.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-09-03', note: 'registry/coverage tooling; one slice of the coverage attestation data, split out of coverageAttestations.ts; enforced by coverageAttestations.conformance.test.ts' }),
  'rules/attestations/decisions.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-09-03', note: 'registry/coverage tooling; one slice of the coverage attestation data, split out of coverageAttestations.ts; enforced by coverageAttestations.conformance.test.ts' }),
  'rules/attestations/insights.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-09-03', note: 'registry/coverage tooling; one slice of the coverage attestation data, split out of coverageAttestations.ts; enforced by coverageAttestations.conformance.test.ts' }),
  'rules/attestations/internal.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-09-03', note: 'registry/coverage tooling; one slice of the coverage attestation data, split out of coverageAttestations.ts; enforced by coverageAttestations.conformance.test.ts' }),
  'rules/attestations/ladder.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-09-03', note: 'registry/coverage tooling; one slice of the coverage attestation data, split out of coverageAttestations.ts; enforced by coverageAttestations.conformance.test.ts' }),
  'rules/attestations/longevity.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-09-03', note: 'registry/coverage tooling; one slice of the coverage attestation data, split out of coverageAttestations.ts; enforced by coverageAttestations.conformance.test.ts' }),
  'rules/attestations/model.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-09-03', note: 'registry/coverage tooling; one slice of the coverage attestation data, split out of coverageAttestations.ts; enforced by coverageAttestations.conformance.test.ts' }),
  'rules/attestations/montecarlo.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-09-03', note: 'registry/coverage tooling; one slice of the coverage attestation data, split out of coverageAttestations.ts; enforced by coverageAttestations.conformance.test.ts' }),
  'rules/attestations/params.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-09-03', note: 'registry/coverage tooling; one slice of the coverage attestation data, split out of coverageAttestations.ts; enforced by coverageAttestations.conformance.test.ts' }),
  'rules/attestations/projection.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-09-03', note: 'registry/coverage tooling; one slice of the coverage attestation data, split out of coverageAttestations.ts; enforced by coverageAttestations.conformance.test.ts' }),
  'rules/attestations/rmd.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-09-03', note: 'registry/coverage tooling; one slice of the coverage attestation data, split out of coverageAttestations.ts; enforced by coverageAttestations.conformance.test.ts' }),
  'rules/attestations/rules.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-09-03', note: 'registry/coverage tooling; one slice of the coverage attestation data, split out of coverageAttestations.ts; enforced by coverageAttestations.conformance.test.ts' }),
  'rules/attestations/scenarios.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-09-03', note: 'registry/coverage tooling; one slice of the coverage attestation data, split out of coverageAttestations.ts; enforced by coverageAttestations.conformance.test.ts' }),
  'rules/attestations/schema.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-09-03', note: 'registry/coverage tooling; one slice of the coverage attestation data, split out of coverageAttestations.ts; enforced by coverageAttestations.conformance.test.ts' }),
  'rules/attestations/socialSecurity.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-09-03', note: 'registry/coverage tooling; one slice of the coverage attestation data, split out of coverageAttestations.ts; enforced by coverageAttestations.conformance.test.ts' }),
  'rules/attestations/spending.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-09-03', note: 'registry/coverage tooling; one slice of the coverage attestation data, split out of coverageAttestations.ts; enforced by coverageAttestations.conformance.test.ts' }),
  'rules/attestations/strategies.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-09-03', note: 'registry/coverage tooling; one slice of the coverage attestation data, split out of coverageAttestations.ts; enforced by coverageAttestations.conformance.test.ts' }),
  'rules/attestations/tax.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-09-03', note: 'registry/coverage tooling; one slice of the coverage attestation data, split out of coverageAttestations.ts; enforced by coverageAttestations.conformance.test.ts' }),
  'rules/attestations/testing.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-09-03', note: 'registry/coverage tooling; one slice of the coverage attestation data, split out of coverageAttestations.ts; enforced by coverageAttestations.conformance.test.ts' }),
  'rules/attestations/topLevel.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-09-03', note: 'registry/coverage tooling; one slice of the coverage attestation data, split out of coverageAttestations.ts; enforced by coverageAttestations.conformance.test.ts' }),
  'rules/coverageAttestations.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-08-24', note: 'registry/coverage tooling' }),
  'rules/coverageReport.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-08-24', note: 'registry/coverage tooling' }),
  'rules/describeRefusal.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-09-03', note: 'registry/coverage tooling' }),
  'rules/describeRule.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-08-24', note: 'registry/coverage tooling' }),
  'rules/index.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-08-24', note: 'registry/coverage tooling' }),
  'rules/records/annuities.ts': Object.freeze({ status: 'registered', sweptOn: '2026-08-29', note: 'one slice of the record store, split out of taxRuleRegistry.ts; enforced by taxRuleRegistry.conformance.test.ts' }),
  'rules/records/charitableDeductions.ts': Object.freeze({ status: 'registered', sweptOn: '2026-08-29', note: 'one slice of the record store, split out of taxRuleRegistry.ts; enforced by taxRuleRegistry.conformance.test.ts' }),
  'rules/records/charitableDistributions.ts': Object.freeze({ status: 'registered', sweptOn: '2026-08-29', note: 'one slice of the record store, split out of taxRuleRegistry.ts; enforced by taxRuleRegistry.conformance.test.ts' }),
  'rules/records/contributionAndDeferralLimits.ts': Object.freeze({ status: 'registered', sweptOn: '2026-08-29', note: 'one slice of the record store, split out of taxRuleRegistry.ts; enforced by taxRuleRegistry.conformance.test.ts' }),
  'rules/records/earlyDistributionsAndSepp.ts': Object.freeze({ status: 'registered', sweptOn: '2026-08-29', note: 'one slice of the record store, split out of taxRuleRegistry.ts; enforced by taxRuleRegistry.conformance.test.ts' }),
  'rules/records/healthSavingsAccounts.ts': Object.freeze({ status: 'registered', sweptOn: '2026-08-29', note: 'one slice of the record store, split out of taxRuleRegistry.ts; enforced by taxRuleRegistry.conformance.test.ts' }),
  'rules/records/individualIncomeTax.ts': Object.freeze({ status: 'registered', sweptOn: '2026-08-29', note: 'one slice of the record store, split out of taxRuleRegistry.ts; enforced by taxRuleRegistry.conformance.test.ts' }),
  'rules/records/investmentIncomeAndBasis.ts': Object.freeze({ status: 'registered', sweptOn: '2026-08-29', note: 'one slice of the record store, split out of taxRuleRegistry.ts; enforced by taxRuleRegistry.conformance.test.ts' }),
  'rules/records/iraBasisAndRollovers.ts': Object.freeze({ status: 'registered', sweptOn: '2026-08-29', note: 'one slice of the record store, split out of taxRuleRegistry.ts; enforced by taxRuleRegistry.conformance.test.ts' }),
  'rules/records/medicareAndHealthCoverage.ts': Object.freeze({ status: 'registered', sweptOn: '2026-08-29', note: 'one slice of the record store, split out of taxRuleRegistry.ts; enforced by taxRuleRegistry.conformance.test.ts' }),
  'rules/records/requiredMinimumDistributions.ts': Object.freeze({ status: 'registered', sweptOn: '2026-08-29', note: 'one slice of the record store, split out of taxRuleRegistry.ts; enforced by taxRuleRegistry.conformance.test.ts' }),
  'rules/records/rothAccounts.ts': Object.freeze({ status: 'registered', sweptOn: '2026-08-29', note: 'one slice of the record store, split out of taxRuleRegistry.ts; enforced by taxRuleRegistry.conformance.test.ts' }),
  'rules/records/socialSecurity.ts': Object.freeze({ status: 'registered', sweptOn: '2026-08-29', note: 'one slice of the record store, split out of taxRuleRegistry.ts; enforced by taxRuleRegistry.conformance.test.ts' }),
  'rules/records/socialSecurityMaritalEligibility.ts': Object.freeze({ status: 'registered', sweptOn: '2026-09-04', note: 'one slice of the record store, split out of taxRuleRegistry.ts; enforced by taxRuleRegistry.conformance.test.ts' }),
  'rules/records/statesMidwest.ts': Object.freeze({ status: 'registered', sweptOn: '2026-08-29', note: 'one slice of the record store, split out of taxRuleRegistry.ts; enforced by taxRuleRegistry.conformance.test.ts' }),
  'rules/records/statesNortheast.ts': Object.freeze({ status: 'registered', sweptOn: '2026-08-29', note: 'one slice of the record store, split out of taxRuleRegistry.ts; enforced by taxRuleRegistry.conformance.test.ts' }),
  'rules/records/statesSouthAtlantic.ts': Object.freeze({ status: 'registered', sweptOn: '2026-08-29', note: 'one slice of the record store, split out of taxRuleRegistry.ts; enforced by taxRuleRegistry.conformance.test.ts' }),
  'rules/records/statesSouthCentral.ts': Object.freeze({ status: 'registered', sweptOn: '2026-08-29', note: 'one slice of the record store, split out of taxRuleRegistry.ts; enforced by taxRuleRegistry.conformance.test.ts' }),
  'rules/records/statesWest.ts': Object.freeze({ status: 'registered', sweptOn: '2026-08-29', note: 'one slice of the record store, split out of taxRuleRegistry.ts; enforced by taxRuleRegistry.conformance.test.ts' }),
  'rules/records/transfersAndUnmodeledRegimes.ts': Object.freeze({ status: 'registered', sweptOn: '2026-08-29', note: 'one slice of the record store, split out of taxRuleRegistry.ts; enforced by taxRuleRegistry.conformance.test.ts' }),
  'rules/symbolLines.ts': Object.freeze({ status: 'rule-free', sweptOn: '2026-08-28', note: 'registry/coverage tooling' }),
  'rules/taxRuleRegistry.ts': Object.freeze({ status: 'registered', sweptOn: '2026-08-24', note: 'the record store itself; enforced by taxRuleRegistry.conformance.test.ts' }),
})
