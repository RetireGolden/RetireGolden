/**
 * Monte-Carlo calculation records.
 *
 * One slice of the calculation registry: the historical market series and
 * blends, the per-path RNG, the pluggable market models, LTC shock sampling,
 * risk-based starting investable, and the SPIA/QLAC payout-rate table.
 * `../calculationRegistry.ts` composes every slice into `CALCULATION_REGISTRY`;
 * read it for what a record must carry.
 */
import type { CalculationRecord } from '../calculationRegistry.js'

const PATH_FAMILIES = [
  'monte-carlo-success-rate',
  'monte-carlo-investable-fan-percentiles',
  'monte-carlo-ending-investable-histogram',
  'monte-carlo-ending-after-tax-estate-percentiles',
  'monte-carlo-depletion-probability-by-year',
] as const

export const monteCarloRecords = {
  'allocation-cholesky-factor': {
    title: 'Cholesky factor of a correlation matrix',
    purpose: 'The lower-triangular mix that turns independent Gaussians into correlated class shocks on an allocated Monte Carlo path.',
    kind: 'formula',
    outputs: [],
    feeds: [
      'monte-carlo-success-rate',
      'monte-carlo-investable-fan-percentiles',
      'monte-carlo-ending-investable-histogram',
      'monte-carlo-ending-after-tax-estate-percentiles',
    ],
    statement:
      'For a symmetric matrix A, the lower-triangular L satisfies L L^T = A when A is positive definite: L_ij = (A_ij − sum_{k<j} L_ik L_jk) / L_jj for i > j, and L_ii = sqrt(A_ii − sum_{k<i} L_ik^2), with L_ij = 0 for j > i. When a diagonal pivot A_ii − sum_{k<i} L_ik^2 is not a positive number, A is not positive definite and no factor with a positive diagonal exists (the pivot is 0, as for a perfectly correlated pair, or negative, as for an indefinite matrix), so the matrix is refused with a message. For the 2×2 correlation [[1, r], [r, 1]], this is L = [[1, 0], [r, sqrt(1 − r^2)]]. Units: dimensionless. Rounding: none.',
    formula: {
      expression: 'L_ii = sqrt(A_ii − sum_{k<i} L_ik^2), refused unless that pivot is positive; L_ij = (A_ij − sum_{k<j} L_ik L_jk) / L_jj for i > j; L_ij = 0 for j > i',
      variables: [
        { symbol: 'A', meaning: 'Input matrix (a correlation matrix when used for class shocks)', unit: '1', domain: 'square and positive definite, refused otherwise; symmetry is not checked' },
        { symbol: 'L', meaning: 'Lower-triangular factor', unit: '1', domain: 'L_ij = 0 for j > i' },
        { symbol: 'r', meaning: 'Off-diagonal correlation in the 2×2 worksheet case', unit: '1', domain: '|r| < 1 for an exact factorization' },
      ],
      timing: 'once per configured correlation matrix; reused every year of every path',
      rounding: 'none',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/allocation-cholesky-factor.md',
    },
    limits: [
      'A matrix that is not positive definite (a diagonal pivot at or below 0, which includes a perfectly correlated pair of classes) is refused with a message; the shipped default matrix is positive definite',
      'A positive-definite matrix whose pivot is positive but below 1e-12 is now factored exactly; it used to have that pivot raised to 1e-12, so its class shocks differ from before (for [[1, r], [r, 1]] with r = 1 − 1e-13, L_22 is 4.4728311955343587e-7, not 1e-6). This is the one valid input whose factor changed',
      'The function neither checks symmetry nor rescales a correlation matrix to unit diagonal',
      'Class-shock models pass this factor a matrix over ASSET_CLASS_IDS order; a 2×2 is the worksheet case, not a live allocation',
    ],
    implementedBy: ['packages/engine/src/allocation/assetClasses.ts'],
    implementedByFunctions: ['packages/engine/src/allocation/assetClasses.ts#choleskyDecompose'],
    verifiedOn: '2026-09-17',
    provenance: { derivedBy: 'codex', implementedBy: 'grok', reviewedBy: 'cursor' },
  },
  'historical-market-series': {
    title: 'Embedded annual stock, bond, and inflation series, 1928–2023',
    purpose: 'The offline 96-year Damodaran/Shiller snapshot every historical bootstrap and blend reads.',
    kind: 'data',
    outputs: [],
    feeds: [...PATH_FAMILIES],
    statement:
      'Ninety-six calendar-year rows, 1928 through 2023 inclusive, each storing S&P 500 total return, 10-year US Treasury total return, and CPI-U calendar-year inflation, all in percent, transcribed to one decimal. The dataset fact this record pins is the embedded table: 96 rows, those endpoints, column sums 1118.9 / 466.6 / 298.8 percentage points, and the sample rows 1928 43.8/0.8/−1.2, 1929 −8.3/4.2/0.6, 2023 26.1/3.9/3.4. Values are approximate at about 0.1–0.5 percentage point. Rounding: one decimal as stored.',
    formula: null,
    justification: {
      kind: 'dataset',
      source: {
        citation:
          'Aswath Damodaran / NYU Stern, “Historical Returns on Stocks, Bonds and Bills,” derived from Shiller annual series: S&P 500 total return, 10-year Treasury total return, and calendar-year CPI inflation',
        url: 'https://pages.stern.nyu.edu/~adamodar/New_Home_Page/datafile/histretSP.html',
        asOf: '2023-12-31',
        retrievedOn: '2026-09-14',
        rights:
          'Facts and three attributed sample rows are restated; the source reuse license was not established, so the full table is not recopied in the worksheet',
      },
      transformation:
        'Transcribed to one-decimal percent storage as HISTORICAL_YEARS. The digest is sha256 over the canonical JSON of the 96 {year, stocksPct, bondsPct, inflationPct} objects in file order, as UTF-8.',
      digest: 'sha256:e6863cd6a8f79b88bcb5be7446c560c8ef77177ba8fdc9d933d27ffe28a53718',
    },
    limits: [
      'Each value is approximate at about 0.1–0.5 percentage point; bootstrap sampling uses the joint distribution and sequencing, not basis-point precision',
      'Refresh cadence is annual with the parameter packs',
      'The full table is not reproduced in the worksheet; the evidence pins row count, endpoints, column sums, and the three sample rows the worksheet names',
    ],
    implementedBy: ['packages/engine/src/montecarlo/historicalReturns.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/historicalReturns.ts#HISTORICAL_YEARS'],
    verifiedOn: '2026-09-17',
    provenance: { derivedBy: 'codex', implementedBy: 'grok', reviewedBy: 'cursor' },
  },
  'historical-portfolio-mean': {
    title: 'Arithmetic mean of blended historical portfolio returns',
    purpose: 'The sample mean every centered historical bootstrap subtracts so the average additive shock is zero.',
    kind: 'formula',
    outputs: [],
    feeds: [...PATH_FAMILIES],
    statement:
      'For equity weight w percent, mean = (1/96) sum_t [w/100 · stocks_t + (1 − w/100) · bonds_t] over the 96 embedded years. Equivalently w/100 · mean(stocks) + (1 − w/100) · mean(bonds). Arithmetic, not geometric, so the average additive shock is zero. For w = 60 the column sums 1118.9 and 466.6 give 8.93729166666667%. Units: percent per year, nominal. Rounding: none.',
    formula: {
      expression: 'mean(w) = (1/N) sum_t [ (w/100) stocks_t + (1 − w/100) bonds_t ], N = 96',
      variables: [
        { symbol: 'w', meaning: 'Equity weight', unit: 'percent', domain: 'typically 0–100' },
        { symbol: 'stocks_t, bonds_t', meaning: 'Embedded year-t stock and bond total returns', unit: 'percent/year', domain: 'finite' },
        { symbol: 'mean(w)', meaning: 'Arithmetic mean blended return', unit: 'percent/year', domain: 'finite' },
      ],
      timing: 'once per configured equity weight; subtracted from every sampled year of a centered bootstrap',
      rounding: 'none',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/historical-portfolio-mean.md',
    },
    limits: [
      'Arithmetic mean is required to center additive shocks; a geometric mean would not make the average shock zero',
      'The mean is taken over the embedded 96-year window only; it is not a forecast of the plan expected return',
    ],
    implementedBy: ['packages/engine/src/montecarlo/historicalReturns.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/historicalReturns.ts#meanPortfolioReturnPct'],
    verifiedOn: '2026-09-17',
    provenance: { derivedBy: 'codex', implementedBy: 'grok', reviewedBy: 'cursor' },
  },
  'historical-portfolio-return-blend': {
    title: 'One-year two-asset blended nominal return',
    purpose: 'The dollar-weighted stock/bond mix of one historical year, before centering.',
    kind: 'formula',
    outputs: [],
    feeds: [...PATH_FAMILIES],
    statement:
      'For a year with stock return s percent and bond return b percent and equity weight w percent, the blended nominal return is (w/100)·s + (1 − w/100)·b, with no rounding. For 1928 (43.8, 0.8) at w = 60 this is 26.6%. Units: percent per year, nominal. Domain: finite series values; w is used as a fraction and is not clamped.',
    formula: {
      expression: 'R = (w/100) s + (1 − w/100) b',
      variables: [
        { symbol: 's', meaning: 'S&P 500 total return for the year', unit: 'percent/year', domain: 'finite' },
        { symbol: 'b', meaning: '10-year Treasury total return for the year', unit: 'percent/year', domain: 'finite' },
        { symbol: 'w', meaning: 'Equity weight', unit: 'percent', domain: 'typically 0–100; not clamped' },
        { symbol: 'R', meaning: 'Blended nominal portfolio return', unit: 'percent/year', domain: 'finite' },
      ],
      timing: 'one calendar year; one blend per sampled historical row',
      rounding: 'none',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/historical-portfolio-return-blend.md',
    },
    limits: [
      'A two-asset stock/bond mix; cash, international, and class-level sleeves are not in this blend',
      'The weight is not clamped to [0, 100]; a value outside that range is used as passed',
    ],
    implementedBy: ['packages/engine/src/montecarlo/historicalReturns.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/historicalReturns.ts#portfolioReturnPct'],
    verifiedOn: '2026-09-17',
    provenance: { derivedBy: 'codex', implementedBy: 'grok', reviewedBy: 'cursor' },
  },
  'long-term-care-shock-sampling': {
    title: 'Per-person paid-care episode draw',
    purpose: 'Zero or one late-life care event per person on a Monte Carlo path, so existing care-event offsets apply.',
    kind: 'model',
    outputs: [],
    feeds: [
      'spending-care-cost-gross-annual',
      'long-term-care-benefit-annual',
      'monte-carlo-success-rate',
      'monte-carlo-ending-investable-histogram',
    ],
    statement:
      'For each person, draw one uniform U in [0, 1). If U >= incidence, emit no event for that person. If U < incidence, onset age is max(currentAge, minOnsetAge + nextInt(span + 1)) with span = max(0, maxOnsetAge − minOnsetAge), duration is drawn from the weighted discrete table, and annualCost is the configured today\'s-dollar cost. At most one episode per person. The worksheet case incidence = 0.5, U = 0.75 emits the empty list. Units: a care-event list. Rounding: onset is an integer age.',
    formula: {
      expression: 'if U >= incidence then no event; else onset = max(currentAge, minOnset + nextInt(span + 1)), duration ~ weighted table, cost = annualCost',
      variables: [
        { symbol: 'incidence', meaning: 'Probability a person has a paid-care episode', unit: '1', domain: '[0, 1]' },
        { symbol: 'U', meaning: 'First uniform draw for that person', unit: '1', domain: '[0, 1)' },
        { symbol: 'annualCost', meaning: 'Today\'s-dollar annual care cost of a sampled episode', unit: 'usd/year', domain: '>= 0' },
      ],
      timing: 'once per person per path, before the path ledger runs',
      rounding: 'onset age is an integer; cost is not rounded',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/long-term-care-shock-sampling.md',
    },
    limits: [
      'Defaults are planning approximations (HHS/ASPE incidence, Genworth-range cost) and do not predict individual need, local cost, or insurance eligibility',
      'Onset is clamped no earlier than current age so the spike lands in the future',
      'The comparison is U >= incidence (no event); reversing it to U > incidence is the worksheet\'s first wrong reading',
      'One draw per person, not one household draw per path',
    ],
    implementedBy: ['packages/engine/src/montecarlo/ltcShock.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/ltcShock.ts#sampleCareEvents'],
    verifiedOn: '2026-09-17',
    provenance: { derivedBy: 'codex', implementedBy: 'grok', reviewedBy: 'cursor' },
  },
  'market-model-ar1-shock': {
    title: 'AR(1) mean-reverting return shock',
    purpose: 'A serially correlated, mean-zero additive return shock for one Monte Carlo path.',
    kind: 'model',
    outputs: [],
    feeds: [...PATH_FAMILIES],
    statement:
      'Let phi be the configured persistence (default 0.25; a number strictly between −1 and 1, where the process is stationary, and anything else is refused with a message) and sigma = returnVolPct/100. Starting from prev = 0, each year draws an innovation eps ~ N(0, 1) and sets shock = phi · prev + sigma · eps, then prev = shock; the published return shock is 100 · shock percentage points. With phi = 0.2, sigma = 0.10 and a first-year eps that produces a 10-point shock, the next two zero-innovation years are 2 then 0.4 points. Units: percentage points. Rounding: none.',
    formula: {
      expression: 'x_0 = 0; x_t = phi x_{t-1} + sigma eps_t; returnShockPct_t = 100 x_t',
      variables: [
        { symbol: 'phi', meaning: 'AR(1) persistence', unit: '1', domain: 'strictly between −1 and 1, refused otherwise; worksheet case 0.2' },
        { symbol: 'sigma', meaning: 'Innovation scale, returnVolPct/100', unit: '1', domain: '>= 0' },
        { symbol: 'eps_t', meaning: 'Standard-normal innovation', unit: '1', domain: 'finite' },
        { symbol: 'returnShockPct_t', meaning: 'Additive return shock', unit: 'percentage points', domain: 'finite' },
      ],
      timing: 'annual; prev starts at 0 on every path',
      rounding: 'none',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/market-model-ar1-shock.md',
    },
    limits: [
      'A dynamics assumption, not evidence of a true return process',
      'The path always starts from prev = 0; a prior shock of 10 is realized as year 1 of the path, and the worksheet\'s "next two shocks" are years 2 and 3 with zero innovations',
      'The phi default comment was corrected to the code\'s 0.25 on 2026-09-18 (D-STUDENT-T-MIXTURE notes the related decision). The worksheet case passes 0.2 explicitly',
      'A phi of 1 or more in size, where the process is not stationary, is refused with a message; phi used to be clamped to [−0.9, 0.95] without a word, and values inside that range run exactly as before',
      'A return-inflation correlation outside [−1, 1], or not finite, is refused with a message rather than clamped',
    ],
    implementedBy: ['packages/engine/src/montecarlo/marketModels.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/marketModels.ts#createAR1Model'],
    verifiedOn: '2026-09-17',
    provenance: { derivedBy: 'codex', implementedBy: 'grok', reviewedBy: 'cursor' },
  },
  'market-model-cape-conditioned': {
    title: 'CAPE-conditioned shift of a mean-preserving lognormal shock',
    purpose: 'Lowers the path mean when starting CAPE is above 20, on top of a lognormal shock.',
    kind: 'model',
    outputs: [],
    feeds: [...PATH_FAMILIES],
    statement:
      'The lognormal base shock is 100 (exp(sigma Z − sigma^2/2) − 1). The CAPE adjustment is adj = clamp(−(startingCape − 20) · sensitivity, −4, 2) percentage points, and the published shock is the base plus adj. For startingCape = 25, sensitivity = 0.15 and sigma = 0, adj = −0.75 and the shock is −0.75 points. Units: percentage points. Rounding: none.',
    formula: {
      expression: 'adj = clamp(−(CAPE − 20) · s, −4, 2); shock = 100 (exp(sigma Z − sigma^2/2) − 1) + adj',
      variables: [
        { symbol: 'CAPE', meaning: 'Starting cyclically adjusted P/E', unit: '1', domain: 'positive; worksheet 25' },
        { symbol: 's', meaning: 'Sensitivity, percentage points per CAPE point above 20', unit: 'percentage points', domain: 'worksheet 0.15' },
        { symbol: 'sigma', meaning: 'Lognormal volatility as a decimal', unit: '1', domain: '>= 0; worksheet 0' },
        { symbol: 'adj', meaning: 'Additive mean shift', unit: 'percentage points', domain: '[−4, 2]' },
      ],
      timing: 'annual; the CAPE adjustment is constant across years of the path',
      rounding: 'none',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/market-model-cape-conditioned.md',
    },
    limits: [
      'A modeling hypothesis, not proof that CAPE causes the stated future-return change',
      'The adjustment is capped to [−4, 2] percentage points on purpose: the cap bounds a derived value, not an input the caller set; the worksheet case −0.75 does not bind',
      'The projection still adds the plan expected return separately; this record is the shock only',
      'A return-inflation correlation outside [−1, 1], or not finite, is refused with a message rather than clamped',
    ],
    implementedBy: ['packages/engine/src/montecarlo/marketModels.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/marketModels.ts#createCapeConditionedModel'],
    verifiedOn: '2026-09-17',
    provenance: { derivedBy: 'codex', implementedBy: 'grok', reviewedBy: 'cursor' },
  },
  'market-model-empirical-history': {
    title: 'Empirical historical shock, centered or raw',
    purpose: 'Samples blended historical returns as shocks, optionally subtracting the historical mean.',
    kind: 'model',
    outputs: [],
    feeds: [...PATH_FAMILIES],
    statement:
      'Each year samples a HISTORICAL_YEARS row: the first nextInt(n) sets the cursor, then a uniform coin of 0.5 redraws iid when next() < 0.5 and otherwise keeps the cursor. At equity weight w, centered mode (the default) publishes blend(row, w) − meanPortfolioReturnPct(w); raw mode (centered === false) publishes the blend itself. Inflation is the row value as-is. For w = 60, nextInt(96) = 0 and next() = 0.6, year 1 is 1928: blend 26.599999999999998%, dataset mean 8.937291666666669%, centered shock 17.662708333333327, raw shock 26.599999999999998, inflation −1.2%. Units: percentage points and percent/year. Rounding: none; absolute tolerance 1e-12 on the non-integer shocks.',
    formula: {
      expression: 'shock_centered = R(row, w) − mean(w); shock_raw = R(row, w); inflation = row.inflationPct',
      variables: [
        { symbol: 'row', meaning: 'Sampled HISTORICAL_YEARS entry', unit: 'year', domain: '1928–2023; worksheet 1928 via nextInt 0' },
        { symbol: 'w', meaning: 'Equity weight', unit: 'percent', domain: 'worksheet 60' },
        { symbol: 'mean(w)', meaning: 'Dataset blended mean at w, meanPortfolioReturnPct(w)', unit: 'percent/year', domain: 'worksheet 8.937291666666669' },
        { symbol: 'R(row, w)', meaning: 'Blended historical return of the sampled row', unit: 'percent/year', domain: 'worksheet 26.599999999999998' },
      ],
      timing: 'annual; one sampled row per path year',
      rounding: 'none',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/market-model-empirical-history.md',
    },
    limits: [
      'Centered mode studies dispersion about the historical mean; raw mode preserves the empirical level but can double-count because the projection adds expected return separately',
      'Neither mode predicts future returns',
      'The sampler mixes sequence-step and iid (a 0.5 coin) rather than drawing one iid index per year',
    ],
    implementedBy: ['packages/engine/src/montecarlo/marketModels.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/marketModels.ts#createEmpiricalModel'],
    verifiedOn: '2026-09-17',
    provenance: { derivedBy: 'codex', implementedBy: 'grok', reviewedBy: 'cursor' },
  },
  'market-model-garch-variance': {
    title: 'GARCH(1,1) variance recursion with variance targeting',
    purpose: 'A clustered-volatility additive return shock whose long-run standard deviation equals returnVolPct.',
    kind: 'model',
    outputs: [],
    feeds: [...PATH_FAMILIES],
    statement:
      'With sigmaBar = returnVolPct/100 (default 12), alpha (default 0.1) and beta (default 0.85), omega = sigmaBar^2 · (1 − alpha − beta), so the long-run variance omega / (1 − alpha − beta) is sigmaBar^2 (Bollerslev 1986, with the variance targeting of Engle and Mezrich 1996). The path starts at v_1 = sigmaBar^2. Each year t draws Z1_t = nextNormal, sets the innovation e_t = sqrt(v_t) · Z1_t and publishes returnShockPct = 100 · e_t; then Z2_t = nextNormal and inflation = inflationMeanPct + inflationVolPct · (rho · Z1_t + sqrt(1 − rho^2) · Z2_t); then v_{t+1} = omega + alpha · e_t^2 + beta · v_t, which feeds back the innovation itself. Because E[e_t^2 | past] = v_t and v_1 = sigmaBar^2, E[v_t] = sigmaBar^2 in every year, so every year\'s shock has mean 0 and unconditional standard deviation returnVolPct points. A negative or non-finite returnVolPct, a negative or non-finite alpha or beta, alpha + beta of 1 or more, and the retired keys omega (now derived) and returnVolScalePct (now returnVolPct) are refused with a message. For the defaults, zero inflation inputs and Z1 = 1, 0.5, −2, 0 (Z2 = 0), omega = 0.00072 and v = 0.0144, 0.0144, 0.01332, 0.01737, so the shocks are 12, 6, −23.082460874005616 and 0 points and inflation is 0. Units: percentage points. Rounding: none. Absolute tolerance 1e-12.',
    formula: {
      expression: 'v_1 = sigmaBar^2; e_t = sqrt(v_t) · Z1_t; shock_t = 100 · e_t; v_{t+1} = sigmaBar^2 (1 − alpha − beta) + alpha e_t^2 + beta v_t',
      variables: [
        { symbol: 'returnVolPct', meaning: 'Long-run standard deviation of the shock; sigmaBar = returnVolPct/100', unit: 'percentage points', domain: 'finite and >= 0, refused otherwise; worksheet 12' },
        { symbol: 'alpha, beta', meaning: 'Weights on the prior year\'s squared innovation and on its variance', unit: '1', domain: 'finite, >= 0 and alpha + beta < 1, refused otherwise; worksheet 0.1 and 0.85' },
        { symbol: 'v_t', meaning: 'Conditional variance of year t (internal, not published)', unit: 'fraction squared', domain: '>= omega; worksheet 0.0144, 0.0144, 0.01332, 0.01737' },
        { symbol: 'Z1_t, Z2_t', meaning: 'The year\'s two standard-normal draws', unit: '1', domain: 'worksheet Z1 = 1, 0.5, −2, 0 with Z2 = 0' },
        { symbol: 'shock_t', meaning: 'Published additive return shock', unit: 'percentage points', domain: 'worksheet 12, 6, −23.082460874005616, 0' },
      ],
      timing: 'annual; v persists along the path from v_1 = sigmaBar^2; draws are Z1, Z2, then the class normals',
      rounding: 'none',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/market-model-garch-variance.md',
    },
    limits: [
      'alpha 0.1 and beta 0.85 are illustrative values, not fitted to annual return data; no source fixes annual GARCH parameters',
      'Innovations are Gaussian: the fat tails come only from the changing variance (a stationary kurtosis of 3.77 at the defaults)',
      'Allocated accounts\' class shocks share one common variance with the market: each class keeps its configured long-run volatility and correlations, and each year every class is scaled by the same sqrt(v_t)/sigmaBar, so all classes cluster together rather than each following a variance of its own (at returnVolPct 0 the scale is 1)',
      'The variance v_t is internal and not published; only the shock, inflation and class series are',
      'An additive shock can take a year\'s return below −100 percent (the return is the plan\'s expected return plus the shock), and the ledger floors balances at zero (annualPostSolveAccountGrowth.ts); after a large shock the next year\'s variance, and with it that chance, is higher',
      'A return-inflation correlation outside [−1, 1], or not finite, is refused with a message rather than clamped',
    ],
    implementedBy: ['packages/engine/src/montecarlo/marketModels.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/marketModels.ts#createGarchModel'],
    verifiedOn: '2026-09-26',
    provenance: { derivedBy: 'claude', implementedBy: 'claude-subagent', reviewedBy: 'unreviewed' },
  },
  'market-model-gaussian-draw': {
    title: 'Additive Gaussian return shock',
    purpose: 'A symmetric, mean-zero normal return shock scaled to the configured volatility.',
    kind: 'model',
    outputs: [],
    feeds: [...PATH_FAMILIES],
    statement:
      'Each year draws Z ~ N(0, 1) and emits returnShockPct = returnVolPct · Z (equivalently 100 · (returnVolPct/100) · Z). For volatility 12 and Z = −2 the shock is −24 percentage points. Inflation is normal and correlated via a Gaussian copula. Units: percentage points. Rounding: none. Exact for these inputs.',
    formula: {
      expression: 'returnShockPct = sigma_pct · Z',
      variables: [
        { symbol: 'sigma_pct', meaning: 'Annual return volatility', unit: 'percentage points', domain: '>= 0; worksheet 12' },
        { symbol: 'Z', meaning: 'Standard-normal draw', unit: '1', domain: 'finite; worksheet −2' },
        { symbol: 'returnShockPct', meaning: 'Additive return shock', unit: 'percentage points', domain: 'unbounded; can imply returns below −100%' },
      ],
      timing: 'annual; iid given the draws',
      rounding: 'none',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/market-model-gaussian-draw.md',
    },
    limits: [
      'The unbounded lower tail can imply returns below −100%, an explicit limitation of additive normal shocks',
      'This is simulated dispersion, not a forecast',
      'A return-inflation correlation outside [−1, 1], or not finite, is refused with a message rather than clamped',
    ],
    implementedBy: ['packages/engine/src/montecarlo/marketModels.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/marketModels.ts#createGaussianModel'],
    verifiedOn: '2026-09-17',
    provenance: { derivedBy: 'codex', implementedBy: 'grok', reviewedBy: 'cursor' },
  },
  'market-model-historical-centered-bootstrap': {
    title: 'Centered historical bootstrap shock',
    purpose: 'A mean-zero shock from a sampled historical stock/bond/inflation year at the configured equity weight.',
    kind: 'model',
    outputs: [],
    feeds: [...PATH_FAMILIES],
    statement:
      'A year is sampled iid, in a fixed-length wrapping block, or as a wrapping sequence. The return shock is portfolioReturnPct(row, w) − meanPortfolioReturnPct(w); inflation is the row\'s inflation. With mode iid, w = 60, and nextInt(96) = 0, path year 1 is the 1928 row: blend 26.599999999999998%, dataset mean 8.937291666666669%, centered shock 17.662708333333327 percentage points, inflation −1.2%. Units: percentage points and percent/year. Rounding: none; absolute tolerance 1e-12 on the shock.',
    formula: {
      expression: 'shock = R(row, w) − mean(w); inflation = row.inflationPct',
      variables: [
        { symbol: 'row', meaning: 'Sampled HISTORICAL_YEARS entry', unit: 'year', domain: '1928–2023; worksheet 1928 via nextInt 0' },
        { symbol: 'w', meaning: 'Equity weight', unit: 'percent', domain: 'worksheet 60' },
        { symbol: 'mean(w)', meaning: 'Dataset blended mean at w, meanPortfolioReturnPct(w)', unit: 'percent/year', domain: 'worksheet 8.937291666666669' },
        { symbol: 'shock', meaning: 'Centered return shock', unit: 'percentage points', domain: 'worksheet 17.662708333333327' },
      ],
      timing: 'annual; iid / block / sequence chooses the row',
      rounding: 'none',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/market-model-historical-centered-bootstrap.md',
    },
    limits: [
      'Subtracting the sample-series mean makes the empirical shock mean zero; it does not claim history repeats or that a 60/40 blend fits every portfolio',
      'blockLengthYears (default 5) must be a whole number of at least 1, in every mode; anything else is refused with a message rather than rounded',
    ],
    implementedBy: ['packages/engine/src/montecarlo/marketModels.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/marketModels.ts#createHistoricalModel'],
    verifiedOn: '2026-09-17',
    provenance: { derivedBy: 'codex', implementedBy: 'grok', reviewedBy: 'cursor' },
  },
  'market-model-inflation-regime': {
    title: 'Bernoulli high-inflation regime mix',
    purpose: 'A two-regime inflation mean (normal vs high) with a lognormal return shock.',
    kind: 'model',
    outputs: [],
    feeds: [...PATH_FAMILIES],
    statement:
      'Each year a uniform U is compared with the high-inflation probability (from the low regime) or 0.7 (from the high regime) and the regime toggles on a hit. Inflation is the selected mean plus a 1.5-point copula term. For high-inflation probability 0.20, U = 0.10, high/base means 8/3 and zero innovations, the first year starts in the low regime, 0.10 < 0.20 toggles to high, and inflation is 8%. Units: percent/year. Rounding: none. Exact in this degenerate case.',
    formula: {
      expression: 'start low; if U < p_high then high; inflation = (high ? mu_high : mu_base) + 1.5 · copula(Z)',
      variables: [
        { symbol: 'p_high', meaning: 'Probability of switching into the high regime from low', unit: '1', domain: 'a probability from 0 to 1, refused otherwise; worksheet 0.20' },
        { symbol: 'U', meaning: 'Regime uniform draw', unit: '1', domain: '[0, 1); worksheet 0.10' },
        { symbol: 'mu_high, mu_base', meaning: 'High and base inflation means', unit: 'percent/year', domain: 'worksheet 8 and 3' },
      ],
      timing: 'annual; the regime bit persists along the path',
      rounding: 'none',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/market-model-inflation-regime.md',
    },
    limits: [
      'A stress model, not a forecast of regime incidence',
      'Inflation volatility is hardcoded at 1.5 percentage points; zero-innovation inflation requires Z = 0, not a configured vol of 0',
      'A high-inflation probability outside [0, 1] is refused with a message; it used to be clamped to [0.01, 0.3] without a word, and values inside that range run exactly as before',
      'A return-inflation correlation outside [−1, 1], or not finite, is refused with a message rather than clamped',
    ],
    implementedBy: ['packages/engine/src/montecarlo/marketModels.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/marketModels.ts#createInflationRegimeModel'],
    verifiedOn: '2026-09-17',
    provenance: { derivedBy: 'codex', implementedBy: 'grok', reviewedBy: 'cursor' },
  },
  'market-model-lognormal-draw': {
    title: 'Mean-preserving lognormal return shock',
    purpose: 'A multiplicative shock whose gross multiplier has mean one, plus correlated normal inflation.',
    kind: 'model',
    outputs: [],
    feeds: [...PATH_FAMILIES],
    statement:
      'Each year draws Z ~ N(0, 1) and emits returnShockPct = 100 (exp(sigma Z − sigma^2/2) − 1) with sigma = returnVolPct/100, so E[exp(sigma Z − sigma^2/2)] = 1. Inflation is inflationMeanPct + inflationVolPct · copula(Z). For return volatility 0 and inflation mean/vol 3/0 every year has return shock 0 and inflation 3%, regardless of Z. Units: percentage points and percent/year. Rounding: none. Exact apart from array representation.',
    formula: {
      expression: 'returnShockPct = 100 (exp(sigma Z − sigma^2/2) − 1); inflation = mu_i + vol_i · copula(Z)',
      variables: [
        { symbol: 'sigma', meaning: 'Return volatility as a decimal, returnVolPct/100', unit: '1', domain: '>= 0; worksheet 0' },
        { symbol: 'Z', meaning: 'Standard-normal draw', unit: '1', domain: 'finite; arbitrary when sigma = 0' },
        { symbol: 'mu_i, vol_i', meaning: 'Inflation mean and volatility', unit: 'percent/year', domain: 'worksheet 3 / 0' },
      ],
      timing: 'annual; iid given the draws',
      rounding: 'none',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/market-model-lognormal-draw.md',
    },
    limits: [
      'Defines simulated dispersion, not a forecast or a guarantee that returns are lognormal',
      'The plan expected return is added by the projection; putting it inside this model would double-count',
      'A return-inflation correlation outside [−1, 1], or not finite, is refused with a message rather than clamped',
    ],
    implementedBy: ['packages/engine/src/montecarlo/marketModels.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/marketModels.ts#createLognormalModel'],
    verifiedOn: '2026-09-17',
    provenance: { derivedBy: 'codex', implementedBy: 'grok', reviewedBy: 'cursor' },
  },
  'market-model-regime-switch': {
    title: 'Two-state bull/bear Markov return shock',
    purpose: 'Persistent bull and bear mean deviations with a per-year switch probability.',
    kind: 'model',
    outputs: [],
    feeds: [...PATH_FAMILIES],
    statement:
      'The path starts bull if U_start > 0.5. Each year, if U_switch < p_switch the state flips. The shock is 100 · (mu_state + sigma_state · Z) percentage points, with mu_bull / mu_bear the configured deviations (default +4 / −4 percent as decimals 0.04 / −0.04). For bull/bear +4/−4, zero volatilities, current state bull, switch draw 0.9 and p_switch = 0.05, the state remains bull and the shock is +4. Units: percentage points. Rounding: none. Exact for the degenerate volatility case.',
    formula: {
      expression: 'stay if U >= p; shock = 100 (mu_state + sigma_state Z)',
      variables: [
        { symbol: 'mu_bull, mu_bear', meaning: 'State mean deviations as percents, divided by 100 in the recursion', unit: 'percent', domain: 'worksheet +4 / −4' },
        { symbol: 'p', meaning: 'Annual switch probability', unit: '1', domain: 'a probability from 0 to 1, refused otherwise; worksheet 0.05' },
        { symbol: 'U', meaning: 'Switch uniform draw', unit: '1', domain: '[0, 1); worksheet 0.9' },
      ],
      timing: 'annual; the state bit persists along the path',
      rounding: 'none',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/market-model-regime-switch.md',
    },
    limits: [
      'A scenario model, not evidence markets have two regimes',
      'Symmetric +a/−a deviations are near zero only under equal long-run state mass',
      'The start state is a coin flip (U > 0.5), so the worksheet\'s "current state bull" is realized by scripting that first draw',
      'A switch probability outside [0, 1] is refused with a message; it used to be clamped to [0.001, 0.5] without a word, and values inside that range run exactly as before',
    ],
    implementedBy: ['packages/engine/src/montecarlo/marketModels.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/marketModels.ts#createRegimeSwitchModel'],
    verifiedOn: '2026-09-17',
    provenance: { derivedBy: 'codex', implementedBy: 'grok', reviewedBy: 'cursor' },
  },
  'market-model-reversed-history': {
    title: 'Reversed-history window replay',
    purpose: 'Replays a stochastically chosen historical window in reverse chronological order.',
    kind: 'model',
    outputs: [],
    feeds: [...PATH_FAMILIES],
    statement:
      'The window length L = windowLengthYears (default 10) must be a whole number of years from 5 to the historical row count n = 96; any other value is refused with a message and never clamped. The path draws start = nextInt(n − L + 1) once, then path year i replays row start + L − 1 − (i mod L). Each shock is the row\'s blend at equityWeightPct minus the dataset mean at that weight; inflation is the row value as-is. For windowLengthYears 5, w = 60, nextInt(92) = 72, and three path years, the replayed years are [2004, 2003, 2002], shocks [−0.7172916666666698, 8.26270833333333, −16.097291666666667], inflation [3.3, 1.9, 2.4]. Units: percentage points and percent/year. Rounding: none; absolute tolerance 1e-12 on the shocks.',
    formula: {
      expression: 'L = windowLengthYears (whole, 5 ≤ L ≤ n); idx_i = start + L − 1 − (i mod L); shock_i = R(row_idx, w) − mean(w)',
      variables: [
        { symbol: 'L', meaning: 'Window length, windowLengthYears', unit: 'years', domain: 'whole number from 5 to n = 96, refused otherwise; worksheet 5' },
        { symbol: 'start', meaning: 'Drawn start index, nextInt(n − L + 1)', unit: '1', domain: '0 to n − L; worksheet 72' },
        { symbol: 'w', meaning: 'Equity weight', unit: 'percent', domain: 'worksheet 60' },
      ],
      timing: 'annual; the reversed window repeats for longer paths',
      rounding: 'none',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/market-model-reversed-history.md',
    },
    limits: [
      'A stress transformation that preserves marginal observations while changing sequence risk, not a historical claim',
      'A windowLengthYears that is not a whole number from 5 to 96 is refused with a message rather than run as a different window; the planner always sends 10',
      'Paths longer than L repeat the same reversed window',
    ],
    implementedBy: ['packages/engine/src/montecarlo/marketModels.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/marketModels.ts#createReversedHistoryModel'],
    verifiedOn: '2026-09-26',
    provenance: { derivedBy: 'claude', implementedBy: 'claude-subagent', reviewedBy: 'unreviewed' },
  },
  'market-model-stationary-bootstrap': {
    title: 'Stationary (geometric-block) historical bootstrap',
    purpose: 'Samples historical years in wrapping contiguous blocks whose length is geometric with mean L.',
    kind: 'model',
    outputs: [],
    feeds: [...PATH_FAMILIES],
    statement:
      'With L = meanBlockLength ?? 5 (a finite number of at least 2; anything else is refused with a message), each historical block draws one length remaining = floor(−ln(1 − U) · L) || 1 after cursor = nextInt(n) (U = next()). The path publishes that many contiguous rows with wrap, then draws a new start row and length only when remaining reaches 0. Draw order per block is nextInt then next. A per-year continuation coin with probability 1/L is a different law. For L = 5, equityWeightPct = 60, nextInt 72, U = 0.50, then at restart nextInt 0, U = 0.90, a five-year path replays 2000, 2001, 2002, 1928, 1929: inflation 3.4, 1.6, 2.4, −1.2, 0.6 and shocks −7.657291666666668, −13.837291666666669, −16.097291666666667, 17.662708333333327, −12.23729166666667. Units: percentage points and percent/year. Rounding: block length is floored and a zero floor becomes 1. Absolute tolerance 1e-12 on the shocks; inflation exact.',
    formula: {
      expression: 'at each block start: remaining = floor(−ln(1 − U) · L) || 1; each year publishes the row at cursor, then cursor = (cursor + 1) mod n and remaining decrements; when remaining reaches 0 a new cursor = nextInt(n) and a new length are drawn',
      variables: [
        { symbol: 'L', meaning: 'Mean block length', unit: 'years', domain: 'finite and >= 2, refused otherwise; worksheet 5' },
        { symbol: 'U', meaning: 'Uniform length draw, once per block', unit: '1', domain: '[0, 1); worksheet 0.50 then 0.90' },
        { symbol: 'remaining', meaning: 'Years left in the current block', unit: 'years', domain: '>= 1 after the || 1 clamp; worksheet 3 then 11' },
      ],
      timing: 'annual; a new block is drawn when remaining hits 0',
      rounding: 'block length is floored; a zero floor becomes 1',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/market-model-stationary-bootstrap.md',
    },
    limits: [
      'Preserves observations while randomizing dependence lengths; it does not establish that historical dependence recurs',
      'One block-length draw per block: remaining = floor(−ln(1 − U) · L) || 1. A per-year continuation coin with p = 1/L is a different law even when its mean is near L',
      'A meanBlockLength below 2, or not finite, is refused with a message rather than raised to 2',
      'Draw order per block is nextInt(n) then next(); a new pair is drawn only when remaining reaches 0',
      'The || 1 is kept on purpose: it rejects an empty block, since at U = 0.01 and L = 5, floor(−ln(0.99) · 5) = 0 would otherwise be length 0',
    ],
    implementedBy: ['packages/engine/src/montecarlo/marketModels.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/marketModels.ts#createStationaryBootstrapModel'],
    verifiedOn: '2026-09-17',
    provenance: { derivedBy: 'codex', implementedBy: 'grok', reviewedBy: 'cursor' },
  },
  'market-model-student-t-draw': {
    title: 'Student-t return shock scaled to the configured volatility',
    purpose: 'A fat-tailed additive return shock whose standard deviation equals returnVolPct.',
    kind: 'model',
    outputs: [],
    feeds: [...PATH_FAMILIES],
    statement:
      'For degrees of freedom df > 2 (default 5) and sigma = returnVolPct/100, each year draws Z ~ N(0, 1) with nextNormal; then V ~ chi-square(df) as 2G with G ~ Gamma(df/2, 1) by the method of Marsaglia and Tsang (2000, ACM TOMS 26(3):363-372), which uses uniforms only (its normal is built by Box-Muller from its own two uniforms, cosine branch); then m = sqrt((df − 2)/V) and t = m · Z, and the published shock is returnShockPct = sigma · t · 100; then z2 = nextNormal and inflation = inflationMeanPct + inflationVolPct · (rho · t + sqrt(1 − rho^2) · z2). Because E[1/V] = 1/(df − 2), E[t^2] = 1, so the shock has mean 0 and standard deviation returnVolPct points, and Corr(shock, inflation) = rho exactly. A df of 2 or less, or not finite, is refused with a message. For df = 5, returnVolPct = 12, Z = 1 and uniforms 0.5, 0, 0.5: the method\'s normal is x = sqrt(2 ln 2) = 1.1774100225154747, d = 13/6, c = 1/sqrt(19.5), v = (1 + c x)^3 = 2.0321239789552172, and 0.5 is below the squeeze bound 1 − 0.0331 x^4 = 0.9363880209572302, so V = 2 d v = 8.805870575472607, m = 0.5836795510996967 and the shock is 7.00415461319636 points; inflation inputs are zero, so inflation is 0. Units: percentage points. Rounding: none. Absolute tolerance 1e-12.',
    formula: {
      expression: 't = Z · sqrt((df − 2)/V), V ~ chi-square(df) by Marsaglia and Tsang; returnShockPct = returnVolPct · t',
      variables: [
        { symbol: 'df', meaning: 'Degrees of freedom of the t variate', unit: '1', domain: 'finite and > 2, refused otherwise; worksheet 5' },
        { symbol: 'Z', meaning: 'The year\'s first standard-normal draw', unit: '1', domain: 'worksheet 1' },
        { symbol: 'V', meaning: 'Chi-square(df) draw, from uniforms only', unit: '1', domain: '> 0; worksheet 8.805870575472607' },
        { symbol: 'm', meaning: 'Unit-variance mixing scale sqrt((df − 2)/V), also shared by the class shocks', unit: '1', domain: '> 0; worksheet 0.5836795510996967' },
        { symbol: 'returnVolPct', meaning: 'Standard deviation of the shock', unit: 'percentage points', domain: 'worksheet 12' },
        { symbol: 'returnShockPct', meaning: 'Additive return shock', unit: 'percentage points', domain: 'worksheet 7.00415461319636' },
      ],
      timing: 'annual; draws are nextNormal, uniforms for V (2 per attempt rejected at s <= 0, else 3), nextNormal',
      rounding: 'none',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/market-model-student-t-draw.md',
    },
    limits: [
      'A tail model, not a claim that future returns follow a t distribution; the planner always uses df 5, and no source fixes df',
      'A df of 2 or less is refused, because the variance is infinite there; for df of 4 or less the fourth moment is infinite',
      'Allocated accounts\' class shocks share V with the market, so the class vector is a multivariate t: large moves in magnitude co-occur across classes (tail dependence) even where the class correlation is low, while their direction follows the correlation; cash gets t tails too',
      'Inflation reads t rather than Z, so it inherits a little of the tail at the default correlation of −0.2; this keeps the configured correlation exact',
      'An additive shock can take a year\'s return below −100 percent (the return is the plan\'s expected return plus the shock), and the ledger floors balances at zero (annualPostSolveAccountGrowth.ts). At df 5 the chance per year of a shock at or below −105 points is 4.8e-5 at 12 percent volatility, 5.3e-4 at 20 percent and 1.4e-3 at 25 percent; the earlier mixture was about 2.3e-3 at 25 percent',
      'The normal inside the chi-square draw floors its first uniform at 1e-12, as rng.ts does, which caps its size near 7.43',
      'A return-inflation correlation outside [−1, 1], or not finite, is refused with a message rather than clamped',
    ],
    implementedBy: ['packages/engine/src/montecarlo/marketModels.ts'],
    implementedByFunctions: [
      'packages/engine/src/montecarlo/marketModels.ts#createStudentTModel',
      'packages/engine/src/montecarlo/marketModels.ts#sampleChiSquare',
    ],
    verifiedOn: '2026-09-26',
    provenance: { derivedBy: 'claude', implementedBy: 'claude-subagent', reviewedBy: 'unreviewed' },
  },
  'market-model-user-shock': {
    title: 'One-year additive user shock on a lognormal base',
    purpose: 'Applies a specified percentage-point shock in one 1-based path year and a mean-preserving lognormal base in the others.',
    kind: 'model',
    outputs: [],
    feeds: [...PATH_FAMILIES],
    statement:
      'Year indices are 1-based. In the shock year the return shock is the configured shockPct; in other years it is the zero-mean lognormal 100 (exp(sigma Z − sigma^2/2) − 1). For shock year 2, shock −20, base volatility 0 and three years, the vector is [0, −20, 0]. Units: percentage points. Rounding: none. Exact.',
    formula: {
      expression: 'returnShockPct_i = shockPct if i+1 = shockYear else 100 (exp(sigma Z_i − sigma^2/2) − 1)',
      variables: [
        { symbol: 'shockYear', meaning: '1-based path year of the user shock', unit: 'years', domain: 'whole number >= 1, refused otherwise; worksheet 2' },
        { symbol: 'shockPct', meaning: 'Additive shock in that year', unit: 'percentage points', domain: 'worksheet −20' },
        { symbol: 'sigma', meaning: 'Base lognormal volatility as a decimal', unit: '1', domain: 'worksheet 0' },
      ],
      timing: 'annual; the shock year is compared as i+1',
      rounding: 'none',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/market-model-user-shock.md',
    },
    limits: [
      'A what-if, not an assigned crash probability',
      'A shockYear that is not a whole number of at least 1 is refused with a message (a value below 1 used to be raised to 1, and a fractional one gave no shock year); treating it as zero-based is the worksheet\'s first wrong reading',
      'The shock year still consumes the year\'s RNG draws; only the return-shock formula is replaced',
    ],
    implementedBy: ['packages/engine/src/montecarlo/marketModels.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/marketModels.ts#createUserShockModel'],
    verifiedOn: '2026-09-17',
    provenance: { derivedBy: 'codex', implementedBy: 'grok', reviewedBy: 'cursor' },
  },
  'monte-carlo-depletion-distribution': {
    title: 'Depletion-year histogram across paths',
    purpose: 'How many paths first ran out of investable assets in each year, with the paths that never depleted left out.',
    kind: 'formula',
    outputs: ['monte-carlo-depletion-year-histogram'],
    statement:
      'Every path with a non-null depletionYear adds 1 to that year\'s bucket; a path whose depletionYear is null never depleted and contributes nothing at all. The buckets are published as { year, count } rows in ascending year order, one row per year that occurs. For the worksheet\'s five paths (null, 2031, 2031, 2033, null) the rows are [{ year: 2031, count: 2 }, { year: 2033, count: 1 }]. Units: paths per calendar year. Rounding: none; integer counts and years, compared exactly.',
    formula: {
      expression: 'count(y) = |{ p : p.depletionYear = y }| for each y that occurs, rows sorted by ascending y',
      variables: [
        { symbol: 'p.depletionYear', meaning: 'Year a path first depleted; null when it never did', unit: 'calendar year or null', domain: 'worksheet null, 2031, 2031, 2033, null' },
        { symbol: 'count(y)', meaning: 'Paths first depleting in year y', unit: 'paths', domain: 'integer >= 1 for a year that appears' },
      ],
      timing: 'once per aggregation, over the completed path set',
      rounding: 'none; integer counts',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/monte-carlo-depletion-distribution.md',
    },
    limits: [
      'Successful paths are absent entirely: there is no null bucket, and no zero row for a year in which nothing depleted',
      'The rows are grouped, not one row per failing path; the two 2031 failures are one row of count 2',
      'depletionProbabilityByYear divides these counts by a base this record does not publish; monte-carlo-depletion-probability-by-year states that rule',
      'The evidence supplies depletionYear only; every other MonteCarloPath field is at the fixture\'s neutral (zero balances and shortfalls, floor and target met, attainment 1, no goal or guardrail activity, no bequest target)',
    ],
    implementedBy: ['packages/engine/src/montecarlo/run.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/run.ts#aggregateMonteCarlo'],
    verifiedOn: '2026-09-18',
    provenance: { derivedBy: 'codex', implementedBy: 'claude', reviewedBy: 'cursor' },
  },
  'monte-carlo-depletion-probability-by-year': {
    title: 'Depletion probability and cumulative probability by year',
    purpose: 'The chance the plan first runs out in each depletion year, taken over every path, with the running total.',
    kind: 'formula',
    outputs: ['monte-carlo-depletion-probability-by-year'],
    statement:
      'One row per depletion year, in depletionYearCounts order: probability = count / pathCount, with the successful paths kept in the denominator, and cumulativeProbability = the running sum of those probabilities through that row. For six paths depleting in 2031 (1), 2033 (2) and 2035 (1) alongside two successes, the rows are 1/6, 1/3 and 1/6 with cumulative 1/6, 1/2 and 2/3. Units: probability in [0, 1]. Rounding: none; absolute tolerance 1e-9, since each figure is a ratio of small integers accumulated in floating point.',
    formula: {
      expression: 'probability(y) = count(y) / N; cumulativeProbability(y_k) = sum_{j <= k} probability(y_j), N = total paths',
      variables: [
        { symbol: 'N', meaning: 'Total paths, successes included', unit: 'paths', domain: 'worksheet 6' },
        { symbol: 'count(y)', meaning: 'Paths first depleting in year y', unit: 'paths', domain: 'worksheet 1, 2, 1' },
        { symbol: 'probability(y)', meaning: 'Share of all paths first depleting in year y', unit: '1', domain: '[0, 1]' },
      ],
      timing: 'once per aggregation, after the depletion-year rows are ordered',
      rounding: 'none',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/monte-carlo-depletion-probability-by-year.md',
    },
    limits: [
      'The denominator is every path, successes included; dividing by the four failures alone would give 1/4, 1/2, 1/4 with a final cumulative of 1',
      'Successes never form a row of their own; they act only on the denominator',
      'cumulativeProbability is accumulated left to right in floating point, so the last row equals the failure rate only up to that accumulation',
      'The evidence supplies depletionYear only; every other MonteCarloPath field is at the fixture\'s neutral (zero balances and shortfalls, floor and target met, attainment 1, no goal or guardrail activity, no bequest target)',
    ],
    implementedBy: ['packages/engine/src/montecarlo/run.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/run.ts#aggregateMonteCarlo'],
    verifiedOn: '2026-09-18',
    provenance: { derivedBy: 'codex', implementedBy: 'claude', reviewedBy: 'cursor' },
  },
  'monte-carlo-ending-distributions': {
    title: 'Ending investable histogram and ending after-tax estate percentiles',
    purpose: 'The shape of where the paths end: a binned spread of ending investable balances and the estate percentiles beside it.',
    kind: 'formula',
    outputs: ['monte-carlo-ending-investable-histogram', 'monte-carlo-ending-after-tax-estate-percentiles'],
    statement:
      'The ending investable balances are binned into histogramBins equal-width bins: min is the sample minimum, binWidth = (max - min) / bins (1 when every value is equal), and each value lands in bin min(bins - 1, floor((v - min) / binWidth)), so the maximum is clamped into the last bin rather than opening one past the end. The ending after-tax estates use the same five-level interpolated percentile set as every other distribution. For endings [0, 20, 40, 80, 100] at 4 bins this is min $0, binWidth $25 and counts [2, 1, 0, 2]; for estates [0, 100, 300, 600, 1000] the percentiles are $40, $100, $300, $600 and $840. Units: usd for min, binWidth and percentiles; paths for counts. Rounding: none; counts compared exactly and the dollar figures at absolute 1e-9.',
    formula: {
      expression: 'min = v_(1); binWidth = (v_(n) - v_(1)) / bins; bin(v) = min(bins - 1, floor((v - min) / binWidth)); p-th percentile by interpolation at (p/100)(n-1)',
      variables: [
        { symbol: 'v_(1), v_(n)', meaning: 'Smallest and largest ending value of the ascending sample', unit: 'usd', domain: 'worksheet 0 and 100' },
        { symbol: 'bins', meaning: 'Histogram bin count passed to aggregateMonteCarlo', unit: 'bins', domain: 'worksheet 4; production default 30' },
        { symbol: 'counts', meaning: 'Paths landing in each bin', unit: 'paths', domain: 'worksheet [2, 1, 0, 2]' },
      ],
      timing: 'once per aggregation, over the ascending ending samples',
      rounding: 'none; the maximum is clamped into the last bin',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/monte-carlo-ending-distributions.md',
    },
    limits: [
      'The fixture passes 4 bins; production defaults to 30, where the same clamp puts the maximum in bin 29',
      'binWidth is 1, not 0, when every ending value is equal, so a degenerate sample still bins',
      'The percentiles are interpolated, so p90 of these five estates is $840 and not the observed $1,000',
      'aggregateMonteCarlo also publishes a histogram of ending estates and percentiles of ending investable; this record\'s worksheet pins one of each, and the ending net worth pair is outside it',
      'The evidence supplies endingInvestable and endingAfterTaxEstate only; every other MonteCarloPath field is at the fixture\'s neutral (no depletion or shortfall, floor and target met, attainment 1, no goal or guardrail activity, no bequest target)',
    ],
    implementedBy: ['packages/engine/src/montecarlo/run.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/run.ts#aggregateMonteCarlo'],
    verifiedOn: '2026-09-18',
    provenance: { derivedBy: 'codex', implementedBy: 'claude', reviewedBy: 'cursor' },
  },
  'monte-carlo-funding-rates': {
    title: 'Ideal and excess funding rates, and the flexible-goal totals',
    purpose: 'How much of the intended ideal and excess spending the paths actually funded, and the flexible-goal outcomes summed over them.',
    kind: 'formula',
    outputs: ['monte-carlo-ideal-funding-rate', 'monte-carlo-excess-funding-rate', 'monte-carlo-flexible-goal-outcome-counts'],
    statement:
      'idealFundingRate = sum(idealFunded) / sum(idealIntended) and excessFundingRate = sum(excessFunded) / sum(excessIntended), each a ratio of totals across paths and each 1 when its total intended amount is not positive. Every flexibleGoals count and amount is the plain sum across every path, never an average. For the worksheet\'s four paths the ideal totals are $150 / $500 = 0.3 and the excess totals $350 / $500 = 0.7, with flexibleGoals { funded 3, partiallyFunded 2, deferred 2, skipped 1, fundedAmount $500, unfundedAmount $300 }; when every path intends nothing, both rates are 1. Units: rates dimensionless, counts in goals, amounts in usd. Rounding: none; rates at absolute 1e-9 and every sum exactly.',
    formula: {
      expression: 'rate = sum_p funded_p / sum_p intended_p when sum_p intended_p > 0, else 1; goal_field = sum_p goal_field_p',
      variables: [
        { symbol: 'intended_p, funded_p', meaning: 'One path\'s intended and funded amount for the ideal or excess layer', unit: 'usd', domain: 'worksheet ideal 100/300/100/0 intended' },
        { symbol: 'rate', meaning: 'Funding rate for that layer', unit: '1', domain: 'worksheet 0.3 and 0.7; 1 with nothing intended' },
        { symbol: 'goal_field', meaning: 'One flexible-goal count or amount', unit: 'goals or usd', domain: 'worksheet 3, 2, 2, 1, $500, $300' },
      ],
      timing: 'once per aggregation, over the completed path set',
      rounding: 'none',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/monte-carlo-funding-rates.md',
    },
    limits: [
      'A ratio of totals, not a mean of per-path rates: averaging the four per-path rates under the zero-intended convention would give 0.625 for both',
      'A path that intended nothing adds nothing to either side of its ratio, so it moves neither rate',
      'The convention tests "intended total > 0", so a zero or negative total intended amount yields 1 rather than a division',
      'The flexible-goal figures are sums; averaging them would give 0.75, 0.5, 0.5, 0.25, $125 and $75',
      'The evidence supplies the four funding fields and flexibleGoals only; every other MonteCarloPath field is at the fixture\'s neutral (no depletion or shortfall, floor and target met, attainment 1, no guardrail activity, zero balances, no bequest target)',
    ],
    implementedBy: ['packages/engine/src/montecarlo/run.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/run.ts#aggregateMonteCarlo'],
    verifiedOn: '2026-09-18',
    provenance: { derivedBy: 'codex', implementedBy: 'claude', reviewedBy: 'cursor' },
  },
  'monte-carlo-guardrail-action-counts': {
    title: 'Guardrail action counts summed across paths',
    purpose: 'How many guardrail cuts, raises and holds the whole run took, counted as actions rather than as paths.',
    kind: 'formula',
    outputs: ['monte-carlo-guardrail-action-counts'],
    statement:
      'guardrailActionCounts.cut, .raise and .hold are each that action\'s count summed over every path, with idle paths still in the set. For per-path records (2, 1, 3), (0, 2, 1), (4, 0, 2) and (0, 0, 0) the totals are cut 6, raise 3 and hold 6. Units: guardrail actions. Rounding: none; sums of integers, compared exactly.',
    formula: {
      expression: 'total(action) = sum_p p.guardrailActionCounts[action] for action in {cut, raise, hold}',
      variables: [
        { symbol: 'p.guardrailActionCounts', meaning: 'One path\'s cut, raise and hold counts', unit: 'actions', domain: 'worksheet (2,1,3), (0,2,1), (4,0,2), (0,0,0)' },
        { symbol: 'total(action)', meaning: 'Run-wide count of that action', unit: 'actions', domain: 'worksheet 6, 3, 6' },
      ],
      timing: 'once per aggregation, over the completed path set',
      rounding: 'none; integer sums',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/monte-carlo-guardrail-action-counts.md',
    },
    limits: [
      'A sum, not an average and not a path incidence count: averaging over four paths gives 1.5, 0.75 and 1.5, and counting the paths with at least one action of each kind gives 2, 2 and 3',
      'A path with no actions stays in the set; it adds zero, and it is not dropped from any denominator here because there is none',
      'The path-share and conditional-magnitude statistics live in the adjustments block (monte-carlo-guardrail-adjustments), which counts paths, not actions',
      'The evidence supplies guardrailActionCounts only; every other MonteCarloPath field is at the fixture\'s neutral (no depletion or shortfall, floor and target met, attainment 1, no cut years, zero balances, no bequest target)',
    ],
    implementedBy: ['packages/engine/src/montecarlo/run.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/run.ts#aggregateMonteCarlo'],
    verifiedOn: '2026-09-18',
    provenance: { derivedBy: 'codex', implementedBy: 'claude', reviewedBy: 'cursor' },
  },
  'monte-carlo-guardrail-adjustments': {
    title: 'Guardrail adjustment shares, depths, durations and ending surplus',
    purpose: 'How likely a mid-course spending cut is, how deep and how long it runs among the paths that cut, and how often the plan still ends with something left.',
    kind: 'formula',
    outputs: [
      'monte-carlo-paths-with-cut-share',
      'monte-carlo-paths-with-raise-share',
      'monte-carlo-cut-years',
      'monte-carlo-max-cut-depth-percentiles',
      'monte-carlo-average-longest-cut-spell-years',
      'monte-carlo-prob-ending-surplus',
      'monte-carlo-prob-ending-above-bequest-target',
    ],
    statement:
      'pathsWithCut is the share of all paths with at least one cut year and pathsWithRaise the share with at least one raise action. Among the cut paths only: averageCutYears is their mean guardrailCutYears, p90CutYears its interpolated 90th percentile, medianMaxCutDepth and p90MaxCutDepth the interpolated 50th and 90th of their maxGuardrailCutDepth, and averageLongestCutSpellYears their mean longestGuardrailCutSpellYears. probEndingSurplus is the share of all paths whose ending after-tax estate is strictly positive; probEndingAboveBequestTarget is the share of the paths carrying a target that cleared it, and exactly null when no path carries one. For the worksheet\'s five paths: 0.6, 0.6, 14/3, 7.2, 0.3, 0.54, 3 years, 0.6 and 0.6. Units: shares in [0, 1], years, and depth as a fraction of the discretionary layer. Rounding: none; absolute 1e-9, with the no-target null compared exactly.',
    formula: {
      expression: 'pathsWithCut = |{p : cutYears_p > 0}| / N; among those paths, averageCutYears = mean(cutYears), p90 by interpolation at 0.9(m-1); probEndingSurplus = |{p : estate_p > 0}| / N',
      variables: [
        { symbol: 'N', meaning: 'All paths', unit: 'paths', domain: 'worksheet 5' },
        { symbol: 'm', meaning: 'Paths with at least one cut year', unit: 'paths', domain: 'worksheet 3' },
        { symbol: 'cutYears_p, spell_p, depth_p', meaning: 'One path\'s cut years, longest consecutive cut spell and deepest cut', unit: 'years, years, fraction', domain: 'worksheet [2,4,8], [1,3,5], [0.10,0.30,0.60] among cut paths' },
        { symbol: 'estate_p', meaning: 'One path\'s ending after-tax estate', unit: 'usd', domain: 'worksheet 0, 100, 200, 0, 50' },
      ],
      timing: 'once per aggregation, over the completed path set',
      rounding: 'none',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/monte-carlo-guardrail-adjustments.md',
    },
    limits: [
      'Every cut statistic divides by the cut paths, not by all paths: over all five, average cut years would be 2.8 and the average longest spell 1.8 years',
      'A zero ending estate is not a surplus; probEndingSurplus takes a strictly positive estate, so counting zeros would give 1 rather than 0.6',
      'probEndingAboveBequestTarget divides by the paths whose comparison is non-null, and is null only when no path carries a target; all five worksheet paths carry one, so that denominator and the path count coincide here',
      'The cut statistics are all zero for a plan with no guardrail policy, which is not the same claim as a plan that cut nothing',
      'guardrailActionCounts is summed elsewhere (monte-carlo-guardrail-action-counts); this record publishes path shares and conditional magnitudes only',
      'The evidence supplies the guardrail fields, endingAfterTaxEstate and endingAboveBequestTarget only; every other MonteCarloPath field is at the fixture\'s neutral (no depletion or shortfall, floor and target met, attainment 1, no goal activity)',
    ],
    implementedBy: ['packages/engine/src/montecarlo/run.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/run.ts#aggregateMonteCarlo'],
    verifiedOn: '2026-09-18',
    provenance: { derivedBy: 'codex', implementedBy: 'claude', reviewedBy: 'cursor' },
  },
  'monte-carlo-investable-fan-percentiles': {
    title: 'Per-year investable balance fan',
    purpose: 'The 10th through 90th percentile band of investable balances in each projection year, taken across paths.',
    kind: 'formula',
    outputs: ['monte-carlo-investable-fan-percentiles'],
    statement:
      'For each projection year the end-of-year investable balances of every path form one sample, sorted ascending; the fan row is that year with the 10th, 25th, 50th, 75th and 90th percentiles by linear interpolation at index (p/100)(n-1): with lo = floor(idx), hi = ceil(idx) and frac = idx - lo, the value is sorted[lo](1 - frac) + sorted[hi]frac, so an even-length sample has the mean of its two middle values as the median. The row\'s year is startYear + the column index. For the worksheet\'s four paths, 2030 is { p10 $30, p25 $75, p50 $200, p75 $475, p90 $790 }, and the medians are $500 in 2031 and $3,000 in 2032. Units: usd. Rounding: none; absolute 1e-9 on the percentiles and the years exactly.',
    formula: {
      expression: 'idx = (p/100)(n-1); fan_y(p) = sorted_y[floor(idx)](1 - frac) + sorted_y[ceil(idx)]frac, frac = idx - floor(idx)',
      variables: [
        { symbol: 'sorted_y', meaning: 'That year\'s investable balances across paths, ascending', unit: 'usd', domain: 'worksheet 2030 [0, 100, 300, 1000]' },
        { symbol: 'n', meaning: 'Paths in the sample', unit: 'paths', domain: 'worksheet 4' },
        { symbol: 'p', meaning: 'Percentile level', unit: 'percent', domain: '10, 25, 50, 75, 90' },
      ],
      timing: 'once per projection year of the aggregation',
      rounding: 'none',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/monte-carlo-investable-fan-percentiles.md',
    },
    limits: [
      'The sample runs down one year\'s column across paths, never along one path\'s years: reading the rows as years would make the first sample [0, 100, 1000] with median $100',
      'Interpolated, not nearest-rank: nearest rank would read 2030 as $0, $0, $100, $300 and $1,000',
      'The fan is as long as the first path\'s investableByYear, so every path in a result must carry the same year grid',
      'An empty sample gives 0 at every level rather than an undefined percentile',
      'The evidence supplies investableByYear only; every other MonteCarloPath field is at the fixture\'s neutral (no depletion or shortfall, floor and target met, attainment 1, no goal or guardrail activity, no bequest target)',
    ],
    implementedBy: ['packages/engine/src/montecarlo/run.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/run.ts#aggregateMonteCarlo'],
    verifiedOn: '2026-09-18',
    provenance: { derivedBy: 'codex', implementedBy: 'claude', reviewedBy: 'cursor' },
  },
  'monte-carlo-shortfall-statistics': {
    title: 'Shortfall averages, p90s, and expected shortfall on failing paths',
    purpose: 'How much spending went unfunded across the run, and how much worse it was on the paths that actually ran out.',
    kind: 'formula',
    outputs: [
      'monte-carlo-average-total-shortfall',
      'monte-carlo-p90-total-shortfall',
      'monte-carlo-expected-shortfall-on-failing-paths',
      'monte-carlo-average-years-below-target',
      'monte-carlo-p90-average-annual-target-shortfall',
      'monte-carlo-target-attainment-median',
    ],
    statement:
      'spendingShortfall.averageTotalShortfallDollars and averageYearsBelowTarget are means over all paths; spendingShortfall.p90TotalShortfallDollars and p90AverageAnnualTargetShortfall are interpolated 90th percentiles of the all-path samples; targetAttainmentPct.p50 is the interpolated median of the per-path attainment. downsideRisk.expectedShortfallDollars is the mean total shortfall over the failing paths alone, a failing path being one whose depletionYear is non-null. For the worksheet\'s five paths these are $36, $80, 3 years, $32, 0.8 and $35. Units: usd, years, and attainment as a fraction of target spending. Rounding: none; absolute 1e-9.',
    formula: {
      expression: 'mean = (1/N) sum_p x_p; p90 by interpolation at 0.9(N-1); expectedShortfall = (1/F) sum_{p : depletionYear_p != null} totalShortfall_p',
      variables: [
        { symbol: 'N', meaning: 'All paths', unit: 'paths', domain: 'worksheet 5' },
        { symbol: 'F', meaning: 'Failing paths (non-null depletionYear)', unit: 'paths', domain: 'worksheet 2' },
        { symbol: 'totalShortfall_p', meaning: 'One path\'s unfunded spending', unit: 'usd', domain: 'worksheet 0, 10, 20, 50, 100' },
        { symbol: 'targetAttainmentPct_p', meaning: 'One path\'s share of target spending funded', unit: '1', domain: 'worksheet 1.00, 0.90, 0.80, 0.60, 0.20' },
      ],
      timing: 'once per aggregation, over the completed path set',
      rounding: 'none',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/monte-carlo-shortfall-statistics.md',
    },
    limits: [
      'Expected shortfall conditions on depletion, not on a positive shortfall: path E\'s $100 shortfall without depletion stays outside it, and averaging all five would give $36 rather than $35',
      'The p90 figures are interpolated, so they need not be an observed value: nearest rank would give $100 and $40',
      'The requested attainment figure is the median of the percentile set, not the mean, which is 0.70 on these paths',
      'The worksheet allows absolute $0.005 on a dollar average whose division is inexact; these five inputs divide exactly, so the fixture holds every figure to 1e-9',
      'downsideRisk.expectedShortfallDollars and the required and target variants beside it are zero, not undefined, when no path fails',
      'The evidence supplies depletionYear, totalShortfall, yearsBelowTarget, averageAnnualTargetShortfall and targetAttainmentPct only; every other MonteCarloPath field is at the fixture\'s neutral (floor and target met, no goal or guardrail activity, zero balances, no bequest target)',
    ],
    implementedBy: ['packages/engine/src/montecarlo/run.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/run.ts#aggregateMonteCarlo'],
    verifiedOn: '2026-09-18',
    provenance: { derivedBy: 'codex', implementedBy: 'claude', reviewedBy: 'cursor' },
  },
  'monte-carlo-stochastic-frontier-axis': {
    title: 'Stochastic frontier axis value per variant',
    purpose: 'What the x of a frontier point means: the caller\'s own grid value, not anything read back out of the stochastic result.',
    kind: 'data',
    outputs: ['stochastic-frontier-variant-axis'],
    statement:
      'Each frontier point\'s x is the caller\'s grid value for that variant. For buildSpendingSuccessFrontier it is the variant plan\'s expenses.baseAnnual, that is max(0, round(baseAnnual x multiplier)); for buildRetirementAgeSuccessFrontier it is the lowest resulting retirement age among the people who have one, each moved by the caller\'s delta and clamped to [30, 80], or 0 when nobody has a retirement age. Neither is inferred from the point\'s stochastic row. For baseAnnual $60,000 with multipliers 0.8, 1.0 and 1.2 the axis is $48,000, $60,000 and $72,000; for deltas -2, 0 and 3 on a one-person plan retiring at 65 the axis is 63, 65 and 68; for two people at 65 and 62 with delta +3 it is 65; for age 31 with delta -2 it is 30. Units: usd on the spending axis, years on the retirement-age axis. Rounding: the spending variant rounds to whole dollars and floors at 0; the retirement-age variant clamps to [30, 80] and is not otherwise rounded.',
    formula: {
      expression: 'x_spending(i) = max(0, round(baseAnnual x multiplier_i)); x_retirementAge(i) = min over people p with a retirement age of clamp(age_p + delta_i, 30, 80), or 0 when no person has one',
      variables: [
        { symbol: 'baseAnnual', meaning: 'Base plan\'s annual base expenses', unit: 'usd', domain: 'worksheet 60,000' },
        { symbol: 'multiplier_i', meaning: 'Caller\'s spending multiplier for variant i', unit: '1', domain: 'worksheet 0.8, 1.0, 1.2' },
        { symbol: 'delta_i', meaning: 'Caller\'s signed retirement-age change for variant i, applied to each person\'s planned retirement age', unit: 'years', domain: 'worksheet -2, 0, 3' },
        { symbol: 'age_p', meaning: 'Person p\'s planned retirement age', unit: 'years', domain: 'worksheet 65; 65 and 62; 31' },
      ],
      timing: 'once per variant, when the frontier builds its plan set',
      rounding: 'spending rounds to whole dollars and floors at 0; the retirement-age result is clamped to [30, 80] and not otherwise rounded',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/monte-carlo-stochastic-frontier-axis.md',
    },
    limits: [
      'The two axes are not the same quantity: the spending axis is the variant\'s absolute base annual expense, while the retirement-age axis is the resulting planned age, so neither axis is a delta from the base: not -12,000, 0 and 12,000, and not -2, 0 and 3',
      'A frontier sweep is capped at MAX_FRONTIER_POINTS (15) points and neither builder searches; the grid is always the caller\'s',
      'A point\'s other figures (the three success rates, the ending-estate percentiles, expected shortfall) come from the variant\'s Monte Carlo summary and are not this record\'s claim; the evidence asserts x only',
      'The evidence runs the real builders on a small one-person plan at pathCount 2 and seed 123, the smallest reproducible run the options allow, because the axis does not depend on the stochastic result at all',
      'The retirement-age axis was first derived as the caller\'s delta from the StochasticFrontierPoint.x doc comment, which stated the delta; on 2026-09-18 the comment was corrected to the resulting clamped age the builder publishes, the worksheet was re-derived and re-approved on it, and the evidence pins 63, 65 and 68 for deltas -2, 0 and 3 on a plan retiring at 65, 65 for the two-person case and 30 at the lower clamp',
    ],
    implementedBy: ['packages/engine/src/montecarlo/frontiers.ts'],
    implementedByFunctions: [
      'packages/engine/src/montecarlo/frontiers.ts#buildSpendingSuccessFrontier',
      'packages/engine/src/montecarlo/frontiers.ts#buildRetirementAgeSuccessFrontier',
    ],
    verifiedOn: '2026-09-18',
    provenance: { derivedBy: 'codex', implementedBy: 'claude', reviewedBy: 'cursor' },
  },
  'monte-carlo-success-and-failure-rates': {
    title: 'Success, required-floor, target-lifestyle and failure rates',
    purpose: 'The headline shares: how often the money lasted, how often the essentials were funded, how often the full lifestyle was, and how often it ran out.',
    kind: 'formula',
    outputs: [
      'monte-carlo-success-rate',
      'monte-carlo-required-floor-success-rate',
      'monte-carlo-target-lifestyle-success-rate',
      'monte-carlo-failure-rate',
      'monte-carlo-failing-path-count',
    ],
    statement:
      'successRate is the share of paths whose depletionYear is null; requiredFloorSuccessRate the share with requiredFloorMet; targetLifestyleSuccessRate the share with targetLifestyleMet; downsideRisk.failureRate the share whose depletionYear is non-null, and downsideRisk.failingPathCount how many those are. Failure is the complement of success over the same depletion test. For the worksheet\'s five deliberately crossed paths these are 0.6, 0.6, 0.2, 0.4 and 2. Units: shares in [0, 1] and a path count. Rounding: none; absolute 1e-9 on the rates and the count exactly.',
    formula: {
      expression: 'successRate = |{p : depletionYear_p = null}| / N; failureRate = 1 - successRate; failingPathCount = N - |{p : depletionYear_p = null}|',
      variables: [
        { symbol: 'N', meaning: 'All paths', unit: 'paths', domain: 'worksheet 5' },
        { symbol: 'depletionYear_p', meaning: 'Year a path first depleted; null when it never did', unit: 'calendar year or null', domain: 'worksheet null, null, null, 2031, 2032' },
        { symbol: 'requiredFloorMet_p, targetLifestyleMet_p', meaning: 'Whether a path funded the required floor, and the full target, every year', unit: 'boolean', domain: 'worksheet crossed against depletion' },
      ],
      timing: 'once per aggregation, over the completed path set',
      rounding: 'none',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/monte-carlo-success-and-failure-rates.md',
    },
    limits: [
      'The three tests are distinct, and the shared 0.6 of successRate and requiredFloorSuccessRate is a coincidence of this path set; the evidence therefore also asserts each path\'s own three verdicts',
      'Depletion alone defines failure here: a path that missed its target every year without depleting still counts as a success, and treating any target miss as depletion would give failure rate 0.8 on four paths',
      'An empty path set gives 0 for every share rather than an undefined rate',
      'The evidence supplies depletionYear, requiredFloorMet and targetLifestyleMet only; every other MonteCarloPath field is at the fixture\'s neutral (no shortfall, attainment 1, no goal or guardrail activity, zero balances, no bequest target)',
    ],
    implementedBy: ['packages/engine/src/montecarlo/run.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/run.ts#aggregateMonteCarlo'],
    verifiedOn: '2026-09-18',
    provenance: { derivedBy: 'codex', implementedBy: 'claude', reviewedBy: 'cursor' },
  },
  'qlac-deferred-payout-placeholder': {
    title: 'Temporary deferred-QLAC payout-rate placeholder',
    purpose: 'A conservative annual payout-rate fraction used for a deferred QLAC starting at 80–85 until a quoted deferred rate is sourced.',
    kind: 'model',
    outputs: [],
    feeds: ['annuitization-payout-rate-pct', 'annuitization-sweep-annual-income'],
    statement:
      'QLAC_DEFERRED_PAYOUT_RATE = 0.16 (annual payout / premium) with no interpolation or rounding. For a $100,000 premium the annual payout is $16,000 and the monthly illustration is 16,000/12 = $1,333.33333333333. Units: fraction of premium per year. Rounding: none on the rate; exact cents on the annual figure; monthly illustration tolerance 1e-9 dollars.',
    formula: {
      expression: 'payout_annual = 0.16 · premium; payout_monthly = payout_annual / 12',
      variables: [
        { symbol: 'premium', meaning: 'QLAC premium', unit: 'usd', domain: 'worksheet 100,000' },
        { symbol: '0.16', meaning: 'Placeholder annual payout-rate fraction', unit: '1/year', domain: 'constant' },
      ],
      timing: 'a constant; applied when a deferred QLAC candidate is sized',
      rounding: 'none',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/qlac-deferred-payout-placeholder.md',
    },
    limits: [
      'No direct deferred quote was obtained; the constant is a placeholder to be replaced at the next parameter-pack refresh',
      'It is intended to understate candidates (16% vs the age-85 immediate 15.3%), not to represent a sourced market quote',
      'The census has no QLAC-specific family; annuitization-payout-rate-pct and annuitization-sweep-annual-income are named as the nearest families this placeholder feeds when a deferred QLAC candidate is sized, and are not this constant\'s published number',
    ],
    implementedBy: ['packages/engine/src/decisions/spiaQuotes.ts'],
    implementedByFunctions: ['packages/engine/src/decisions/spiaQuotes.ts#QLAC_DEFERRED_PAYOUT_RATE'],
    verifiedOn: '2026-09-17',
    provenance: { derivedBy: 'codex', implementedBy: 'grok', reviewedBy: 'cursor' },
  },
  'risk-based-guardrail-threshold-solver': {
    title: 'Risk-based guardrail thresholds and suggested adjustments by bisection',
    purpose: 'Finds the portfolio levels where the plan\'s Monte Carlo success meets each edge of its success band, and the spending change that brings success back to the middle of the band there.',
    kind: 'model',
    outputs: ['risk-based-guardrail-solved-balance-thresholds', 'risk-based-guardrail-suggested-adjustment-monthly'],
    feeds: ['display-guardrail-balance-thresholds', 'spending-guardrail-factor-annual'],
    statement:
      'Let S(f, m) be the success rate of the plan with fixed target spending, with every investable balance scaled by f and target spending by m, on shared seeded Monte Carlo paths (or the value of the optional successProbe(f, m), which replaces that run). The band is L/U percent (the plan\'s policy, else 70/95) and the recovery target is (L + U)/200. For each edge p = L/100 and U/100: if S(0.02, 1) >= p the outcome is always-above-band; if S(4, 1) < p it is never-reaches-band; otherwise ten bisections on [0.02, 4] move hi to the midpoint when S(mid, 1) >= p and lo otherwise, and the edge is hi, a point 0.02 + k · 3.98/1024 with k a whole number from 1 to 1024 (the smallest such point meeting p), with balanceDollars = hi · starting investable. At a solved lower edge f_L the suggested cut bisects m eight times on [max(0.3, required/base), 1], moving lo when S(f_L, m) >= the recovery target, and returns lo, provided the best case S(f_L, lowest m) reaches the target; at a solved upper edge the raise does the same on [1, 2], and reports m = 2 when even S(f_U, 2) stays above the target. annualDollars = |1 − m| · base spending and monthlyDollars = annualDollars / 12. For S(f, m) = min(1, f/(2m)), one $500,000 taxable account, base spending $40,000, no required spending and a 70/95 band: successAtCurrent is 0.5; the edges are k = 356 and 484, balanceFrac 1.403671875 and 1.901171875, balanceDollars $701,835.9375 and $950,585.9375; the cut multiplier is 0.849609375 ($6,015.625 a year, $501.3020833333333 a month, success after 0.8260689655172412) and the raise multiplier 1.1484375 ($5,937.50 a year, $494.7916666666667 a month, success after 0.8277210884353741); the probe is called 40 times. Units: fractions of starting investable, multipliers, today\'s dollars. Rounding: none; absolute tolerance 1e-12 on fractions and multipliers and 1e-6 on dollars.',
    formula: {
      expression: 'edge(p) = min { 0.02 + k · 3.98/1024 : k = 1..1024, S(·, 1) >= p } by ten bisections; m_cut = max { m on the eight-step lattice of [m_min, 1] : S(f_L, m) >= (L + U)/200 }; m_raise likewise on [1, 2]; monthly = |1 − m| · base / 12',
      variables: [
        { symbol: 'S(f, m)', meaning: 'Fixed-target success rate at balance scale f and spending multiplier m', unit: '1', domain: '[0, 1], nondecreasing in f and nonincreasing in m; worksheet min(1, f/(2m))' },
        { symbol: 'L, U', meaning: 'Success band edges', unit: 'percent', domain: 'L < U; worksheet 70 and 95' },
        { symbol: 'k', meaning: 'Lattice index of a solved edge', unit: '1', domain: 'whole number 1 to 1024; worksheet 356 and 484' },
        { symbol: 'm', meaning: 'Spending multiplier of a suggested adjustment', unit: '1', domain: 'cut [max(0.3, required/base), 1], raise [1, 2]; worksheet 0.849609375 and 1.1484375' },
        { symbol: 'base', meaning: 'Base annual target spending', unit: 'usd', domain: '> 0; worksheet 40,000' },
        { symbol: 'B', meaning: 'Starting investable (risk-based-starting-investable)', unit: 'usd', domain: 'worksheet 500,000' },
      ],
      timing: 'once per solve, on today\'s balances and today\'s spending',
      rounding: 'none',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/risk-based-guardrail-threshold-solver.md',
    },
    limits: [
      'Monotonicity of success in f and m is assumed, not checked; with a curve that is not monotone the result is a lattice point but not a meaningful edge',
      'The Spending card always solves under the lognormal model at 12 percent volatility around the plan\'s own expected returns (allocated accounts use their asset classes\' volatilities), with 200 paths and the plan-id seed, whatever model the Monte Carlo page shows',
      'Successes are cached by the balance scale rounded to 1e-6; the lattice step is about 3.9e-3, so two lattice points never share a key. The spending phase is not cached, so successAfter re-evaluates a multiplier the bisection has usually tried already',
      'With a single path S is 0 or 1, so the two band edges are the same test and return the same point',
      'The planner persists each edge as a percent of today\'s investable rounded to two decimals (140.37 and 190.12 in the worked example), and the callouts print that percent of today\'s balances, so the printed dollars can differ from balanceDollars by that rounding and by any balance change since the solve',
    ],
    implementedBy: ['packages/engine/src/montecarlo/riskBasedGuardrails.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/riskBasedGuardrails.ts#solveRiskBasedGuardrails'],
    verifiedOn: '2026-09-26',
    provenance: { derivedBy: 'claude', implementedBy: 'claude-subagent', reviewedBy: 'unreviewed' },
  },
  'risk-based-starting-investable': {
    title: 'Starting investable: sum of listed account balances',
    purpose: 'The dollar base the risk-based guardrail fractions and SWR initial spend apply to.',
    kind: 'formula',
    outputs: [],
    feeds: [
      'display-guardrail-balance-thresholds',
      'monte-carlo-success-rate',
      'swr-rule-result-initial-annual-spend',
    ],
    statement:
      'Sum current balances of accounts whose type is taxable, equityComp, traditional, Roth, HSA, or cash. Property (home) and other noninvestable types are excluded, including when they carry a value rather than a balance. For taxable $100,000, cash $20,000 and home/property $300,000 the sum is $120,000. Units: dollars. Rounding: none. Exact dollars.',
    formula: {
      expression: 'B = sum { balance(a) : type(a) in {taxable, equityComp, traditional, roth, hsa, cash} }',
      variables: [
        { symbol: 'a', meaning: 'A plan account', unit: 'account', domain: 'finite nonnegative balances' },
        { symbol: 'B', meaning: 'Starting investable', unit: 'usd', domain: '>= 0' },
      ],
      timing: 'once at the start of a solve, on today\'s balances',
      rounding: 'none',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/risk-based-starting-investable.md',
    },
    limits: [
      'The listed investable set is the same dollar base market paths can fund; property is excluded by type and because it stores value rather than balance',
      'Basis fields are not added; only the balance field of an investable account contributes',
    ],
    implementedBy: ['packages/engine/src/montecarlo/riskBasedGuardrails.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/riskBasedGuardrails.ts#startingInvestableOf'],
    verifiedOn: '2026-09-17',
    provenance: { derivedBy: 'codex', implementedBy: 'grok', reviewedBy: 'cursor' },
  },
  'rng-derived-path-seed': {
    title: 'SplitMix32-style per-path seed',
    purpose: 'A 32-bit path seed from (base seed, zero-based path index) so a path\'s stream does not depend on how preceding paths consume draws.',
    kind: 'model',
    outputs: [],
    feeds: [...PATH_FAMILIES],
    statement:
      'A four-step 32-bit recurrence of the two arguments: h0 = (seed XOR imul(pathIndex + 1, 0x9e3779b9)) >>> 0; h1 = imul(h0 XOR (h0 >>> 16), 0x21f0aaad); h2 = imul(h1 XOR (h1 >>> 15), 0x735a2d97); result = (h2 XOR (h2 >>> 15)) >>> 0. For (42, 7), (42, 8) and (1, 0) the unsigned words are 1351098177, 2450979136 and 3950124170, and 1351098177 != 2450979136 so adjacent paths receive distinct seeds. Units: unsigned 32-bit integer. Rounding: none. Exact integer.',
    formula: {
      expression:
        'h0 = (seed XOR imul(pathIndex+1, 0x9e3779b9)) >>> 0; h1 = imul(h0 XOR (h0>>>16), 0x21f0aaad); h2 = imul(h1 XOR (h1>>>15), 0x735a2d97); result = (h2 XOR (h2>>>15)) >>> 0',
      variables: [
        { symbol: 'seed', meaning: 'Base Monte Carlo seed', unit: 'uint32', domain: 'worksheet 42 and 1' },
        { symbol: 'pathIndex', meaning: 'Zero-based path index', unit: '1', domain: 'integer >= 0; worksheet 7, 8, 0' },
        { symbol: 'result', meaning: 'Derived path seed', unit: 'uint32', domain: '0..2^32−1; worksheet 1351098177, 2450979136, 3950124170' },
      ],
      timing: 'once per path, before that path\'s RNG is created',
      rounding: 'none; 32-bit wrapping arithmetic',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/rng-derived-path-seed.md',
    },
    limits: [
      'Hashing path identity rather than advancing one shared stream makes worker partitioning irrelevant',
      'Not cryptographic; adjacent indices are mixed by pathIndex+1 and the lowbias32 finalizer, not used as seed + index',
    ],
    implementedBy: ['packages/engine/src/montecarlo/rng.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/rng.ts#derivePathSeed'],
    verifiedOn: '2026-09-18',
    // The independent reviewer rejected two of the three seed words on its own
    // shift arithmetic; the orchestrator's script check is not the independent
    // recomputation this field reports, so the record stays unreviewed until an
    // independent lane approves one (see the worksheet's Provenance section).
    provenance: { derivedBy: 'codex', implementedBy: 'grok', reviewedBy: 'unreviewed' },
  },
  'rng-mulberry32-reference-stream': {
    title: 'Mulberry32 uniform reference stream',
    purpose: 'The deterministic [0, 1) stream every Monte Carlo path draw is taken from.',
    kind: 'model',
    outputs: [],
    feeds: [...PATH_FAMILIES],
    statement:
      'State a is the seed as uint32. Each uniform draw: a = (a + 0x6d2b79f5) | 0; t = a; t = imul(t XOR (t >>> 15), t | 1); t ^= t + imul(t XOR (t >>> 7), t | 61); u = ((t XOR (t >>> 14)) >>> 0) / 2^32. For seed 1 the first five unsigned words are 2693262067, 11749833, 2265367787, 4213581821, 4159151403; dividing by 2^32 gives 0.6270739405881613, 0.002735721180215478, 0.5274470399599522, 0.9810509674716741, 0.9683778982143849. Standard-normal draws use Box–Muller (with a cached spare) and integers are floor(u · n) clamped into [0, n). Units: dimensionless in [0, 1). Rounding: none. Exact integer words; relative tolerance 1e-15 if doubles are compared.',
    formula: {
      expression: 'a ← (a + 0x6d2b79f5) | 0; avalanche; word = uint32(t); u = word / 2^32',
      variables: [
        { symbol: 'seed', meaning: 'Initial state', unit: 'uint32', domain: 'worksheet 1' },
        { symbol: 'word', meaning: 'Unsigned 32-bit Mulberry32 output', unit: 'uint32', domain: 'worksheet [2693262067, 11749833, 2265367787, 4213581821, 4159151403]' },
        { symbol: 'u', meaning: 'Uniform draw in [0, 1)', unit: '1', domain: '[0, 1)' },
      ],
      timing: 'one state advance per uniform; Box–Muller consumes two uniforms per pair of normals',
      rounding: 'none; divide by 2^32 not 2^32−1',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/rng-mulberry32-reference-stream.md',
    },
    limits: [
      'A small noncryptographic generator suitable for reproducible simulation sampling, not security',
      'Normal draws cache the paired Box–Muller value; the worksheet vector is the uniform stream, not the normal stream',
    ],
    implementedBy: ['packages/engine/src/montecarlo/rng.ts'],
    implementedByFunctions: ['packages/engine/src/montecarlo/rng.ts#createRng'],
    verifiedOn: '2026-09-17',
    provenance: { derivedBy: 'codex', implementedBy: 'grok', reviewedBy: 'cursor' },
  },
  'spia-payout-rate-interpolation': {
    title: 'Life-only SPIA payout-rate linear interpolation',
    purpose: 'The default annual payout-rate fraction of premium at a start age, from the embedded female-conservative anchors.',
    kind: 'data',
    outputs: ['annuitization-payout-rate-pct'],
    feeds: ['annuitization-sweep-annual-income', 'annuitization-sweep-premium'],
    statement:
      'Anchors (age, annual payout/premium): (60, 0.060), (65, 0.070), (70, 0.084), (75, 0.103), (80, 0.129), (85, 0.153), the last extrapolated. Below 60 the rate is 0.060; above 85 it is 0.153; otherwise linear in age between the bracketing anchors. At 67.5, t = (67.5 − 65)/5 = 1/2 and the rate is 0.070 + 0.5 · (0.084 − 0.070) = 0.077, so $100,000 of premium pays $7,700 per year. Units: fraction of premium per year. Rounding: none after the table\'s source-side 0.1-percentage-point rounding down. Absolute tolerance 1e-12 on the rate.',
    formula: {
      expression: 'r(a) = r_i + (a − a_i)(r_{i+1} − r_i)/(a_{i+1} − a_i) for a_i <= a <= a_{i+1}; r(a) = r_60 for a <= 60; r(a) = r_85 for a >= 85',
      variables: [
        { symbol: 'a', meaning: 'Payment start age', unit: 'years', domain: 'worksheet 67.5' },
        { symbol: 'r(a)', meaning: 'Annual life-only payout as a fraction of premium', unit: '1/year', domain: 'between the bracketing anchors' },
        { symbol: 'premium', meaning: 'Illustrative premium', unit: 'usd', domain: 'worksheet 100,000' },
      ],
      timing: 'a time-invariant lookup on the dated quote table',
      rounding: 'none after the source-side 0.1-percentage-point rounding down of the anchors',
    },
    justification: {
      kind: 'dataset',
      source: {
        citation:
          'annuity.org April 2026 $100,000 monthly-payout table by age/sex; anchors take the female column conservatively, rounded down to 0.1%; age 85 is extrapolated',
        url: 'https://www.annuity.org/annuities/how-much-does-a-100000-annuity-pay-per-month/',
        asOf: '2026-04-01',
        retrievedOn: '2026-07-15',
        rights:
          'Facts and small numeric extracts are restated with attribution; no source prose or table is reproduced',
      },
      transformation:
        'Stored as annual payout-rate fractions at ages 60, 65, 70, 75, 80, 85. Consumers interpolate linearly in age and clamp outside the table. The digest is sha256 over the canonical JSON of [[60,0.06],[65,0.07],[70,0.084],[75,0.103],[80,0.129],[85,0.153]] as UTF-8.',
      digest: 'sha256:9649a7c32fecae384d940f2cf7b87675a42236a71329de30dd7535335edf20ad',
    },
    limits: [
      'Planning defaults only; a user-entered quote always wins',
      'The age-85 anchor is extrapolated, not quoted',
      'Female column is the conservative unisex default; male quotes are higher',
      'annuitization-sweep-annual-income is premium times this rate and annuitization-sweep-premium is the grid premium; those families are fed, not this interpolation\'s published number',
    ],
    implementedBy: ['packages/engine/src/decisions/spiaQuotes.ts'],
    implementedByFunctions: ['packages/engine/src/decisions/spiaQuotes.ts#spiaPayoutRate'],
    verifiedOn: '2026-09-17',
    provenance: { derivedBy: 'codex', implementedBy: 'grok', reviewedBy: 'cursor' },
  },
  'historical-stress-window-total-shortfall': {
    title: 'Historical stress window total shortfall',
    purpose: 'How many dollars of spending one replayed history left unfunded across the whole projection.',
    kind: 'composition',
    outputs: ['historical-stress-window-total-shortfall'],
    statement:
      'montecarlo/historicalSuites.ts#HistoricalStressWindow.totalShortfall is the sum, over EVERY projection year of that window\'s replayed run, of the year\'s YearResult.shortfall — the funding shortfall left after every withdrawal and any HECM backstop draw. Every year is counted, including years after the portfolio first empties, and each year\'s shortfall is that year\'s own unfunded spending rather than a cumulative miss. The annual shortfalls remain outputs of the full shared ledger: returns, inflation, taxes, income, withdrawals and HECM draws are priced by simulatePlan on the window\'s market series, not by a side model. Units: nominal dollars. Rounding: none.',
    formula: {
      expression: 'totalShortfall = sum over projection years of shortfall_y',
      variables: [
        { symbol: 'shortfall_y', meaning: 'Year y\'s funding shortfall after every withdrawal and any HECM backstop draw', unit: 'usd', domain: 'shortfall_y >= 0' },
        { symbol: 'window', meaning: 'One rolling (or reversed) historical return/inflation series replayed through the ledger', unit: 'years', domain: 'windowLengthYears from HISTORICAL_YEARS' },
      ],
      timing: 'once per window, over every published projection year',
      rounding: 'none',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/historical-stress-window-total-shortfall.md',
    },
    limits: [
      'Beyond the worksheet\'s inputs the evidence plan fixes a plan the replay cannot move on either axis: one CASH account, which annualPostSolveAccountGrowth exempts from the market return shock, so the replayed returns really are the worksheet\'s 0, 0, 0; and the $60,000 a year delivered as the scheduled payment of an interest-free note, because debt service is NOMINAL while base spending and one-time goals are both inflated to the year — the 2000-2002 window\'s own 3.4% and 1.6% CPI prints rescale a goal-based fixture to a 2027 shortfall of $22,040. Beside that: a 58-year-old single filer (no Medicare month, no traditional account to penalize), no income, no tax, no HECM, and a three-year horizon from the planning age',
      'Because that plan is replay-invariant, every rolling window in the suite reports the same total; the evidence reads the 2000-2002 window by label, and the identity it pins is the sum, not a property of those particular historical years',
      'Suites are run with suites: [\'rolling\'] and windowLengthYears 3 to keep the fixture to one pass over the rolling windows',
      'An explicit windowLengthYears must be a whole number of years from 1 to 96; anything else is refused with a message before any window is simulated, rather than clamped',
      'The sum does not distinguish a discretionary miss from a floor miss: totalRequiredShortfall and totalTargetShortfall are separate fields over the same years',
    ],
    implementedBy: ['packages/engine/src/montecarlo/historicalSuites.ts'],
    implementedByFunctions: [
      'packages/engine/src/montecarlo/historicalSuites.ts#runHistoricalStressSuites',
      'packages/engine/src/montecarlo/historicalSuites.ts#HistoricalStressWindow',
    ],
    verifiedOn: '2026-09-18',
    provenance: { derivedBy: 'codex', implementedBy: 'claude', reviewedBy: 'cursor' },
  },
  'annuitization-sweep-point': {
    title: 'Annuitization sweep point: premium, annual income and effective allocation',
    purpose: 'What one point of the SPIA allocation sweep actually buys, and the share of the portfolio it costs.',
    kind: 'model',
    outputs: [
      'annuitization-sweep-premium',
      'annuitization-sweep-annual-income',
      'annuitization-sweep-effective-allocation-pct',
    ],
    statement:
      'For each non-zero grid percent, decisions/annuitization.ts#buildAnnuitizationSweep sets premium = min(gridPct / 100 x the plan\'s total investable balance, 0.95 x the largest cash-or-taxable account\'s balance) and SKIPS the point when that premium is under $5,000. A retained point publishes annualIncome = premium x the payout rate — the user\'s quoted rate / 100 when given, else decisions/spiaQuotes.ts#spiaPayoutRate interpolated at startAge = min(95, max(current age, 65)) — and effectiveAllocationPct = premium / total investable x 100, which is below the requested grid percent exactly when the funding cap bound the purchase. Units: nominal dollars; percent of investable. Rounding: none.',
    formula: {
      expression: 'premium = min(g/100 x V, 0.95 x F); income = premium x rate(startAge); effectivePct = premium / V x 100; skip when premium < 5000',
      variables: [
        { symbol: 'V', meaning: 'Total investable balance (cash, taxable, equity comp, traditional, Roth, HSA)', unit: 'usd', domain: 'positive' },
        { symbol: 'F', meaning: 'Largest cash or taxable account balance — the funding account', unit: 'usd', domain: 'nonnegative' },
        { symbol: 'g', meaning: 'Requested grid allocation', unit: 'percent of investable', domain: 'g > 0' },
        { symbol: 'rate(startAge)', meaning: 'Life-only SPIA payout rate, linearly interpolated between the table anchors and clamped outside them', unit: 'fraction of premium per year', domain: '0.06 to 0.153' },
      ],
      timing: 'once per sweep, at the comparison\'s start year',
      rounding: 'none; the $5,000 floor is a strict under-test',
    },
    justification: {
      kind: 'derivation',
      worksheet: 'DOCS/calculations/monte-carlo/annuitization-sweep-point.md',
    },
    limits: [
      'A point only reaches the published list when the shared-path Monte Carlo returns a row for its variant, so the evidence has to run one: it uses a two-path comparison over a two-year horizon (a 1954 birth with planning age 73 from 2026 runs 2026 and 2027) with a fixed seed. The three figures themselves are decided before that run and do not depend on its outcome',
      'Beyond the worksheet\'s inputs the evidence plan fixes: a 1954-born single filer so the 2026 current age is 72, a $100,000 cash funding account beside a $100,000 traditional account for a $200,000 investable total, a grid of exactly [2, 60] so the skipped and retained points are both present, no user quote, zero return and zero inflation',
      'The funding account carries no static allocation, so the sweep\'s Kitces glidepath controls are not constructible here and attributionAvailable is false; the sweep records that as a note rather than an error',
      'The skipped $4,000 point is asserted as an ABSENT allocationPct, since a skipped point is never pushed rather than published at zero',
    ],
    implementedBy: [
      'packages/engine/src/decisions/annuitization.ts',
      'packages/engine/src/decisions/spiaQuotes.ts',
    ],
    implementedByFunctions: [
      'packages/engine/src/decisions/annuitization.ts#buildAnnuitizationSweep',
      'packages/engine/src/decisions/annuitization.ts#AnnuitizationSweepPoint',
      'packages/engine/src/decisions/spiaQuotes.ts#spiaPayoutRate',
    ],
    verifiedOn: '2026-09-18',
    provenance: { derivedBy: 'codex', implementedBy: 'claude', reviewedBy: 'cursor' },
  },
} satisfies Record<string, CalculationRecord>
