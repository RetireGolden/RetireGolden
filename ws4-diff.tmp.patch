diff --git a/DOCS/domain/tax-strategy-coverage-inventory.md b/DOCS/domain/tax-strategy-coverage-inventory.md
index 15c521a..3559967 100644
--- a/DOCS/domain/tax-strategy-coverage-inventory.md
+++ b/DOCS/domain/tax-strategy-coverage-inventory.md
@@ -126,7 +126,7 @@ the table below states both.
 | NIIT | **Partial.** The federal calculator applies 3.8% to the lesser of modeled investment income and MAGI above the threshold. Modeled investment income includes positive capital gains, qualified and ordinary dividends, and taxable interest. | [Rules §2](domain-rules-reference.md#2-long-term-capital-gains-and-niit-2026); [federal calculator](../../packages/engine/src/tax/federalTax.ts) | [Federal-tax tests](../../packages/engine/src/tax/federalTax.test.ts) | This is not Form 8960: it does not model every rental, royalty, passive-business, trader, foreign, expense, disposition, or deduction adjustment that can enter net investment income. | “Estimate NIIT from the investment-income categories modeled in this plan.” Never describe it as complete NIIT or Form 8960 liability. | Full-NIIT claims require broader income, deduction, and activity-status coverage with external fixtures. |
 | ACA premium tax credit | **Shipped, planning-grade with typed readiness.** A per-year contract drives a current-year exact-ledger fixed point across healthcare, withdrawals, tax, and ACA household MAGI. The ledger distinguishes gross enrollment premium, SLCSP benchmark, modeled allowable PTC, and economic net premium; tax family/dependent filing facts remain separate from covered members/months. | [Rules §8](domain-rules-reference.md#8-aca-premium-tax-credit-pre-65-retirees); [ACA calculator/MAGI builder](../../packages/engine/src/tax/aca.ts); [annual ledger](../../packages/engine/src/projection/simulate.ts); [result evidence](../../packages/engine/src/projection/types.ts) | [Unit/boundary tests](../../packages/engine/src/tax/aca.test.ts); [golden tests](../../packages/engine/src/tax/aca.golden.test.ts); [external golden tests](../../packages/engine/src/tax/aca.external.golden.test.ts); [integrated fixed-point tests](../../packages/engine/src/projection/simulate.test.ts); [optimizer gate tests](../../packages/engine/src/projection/optimizePlan.test.ts); [comparison tests](../../packages/engine/src/scenarios/comparison.test.ts) | This plans current-year allowable PTC/economic premium, not actual APTC cash timing, refund, repayment, balance due, or filing-grade Form 8962. Below-100% exception pathways, Form 8814, special allocations, MFS exceptions, self-employed health-insurance-deduction interactions, and other unsupported material facts are not estimated. Missing/unknown facts or fixed-point failure fund gross premium and return non-actionable evidence. | “Compare the modeled current-year ACA household-MAGI, allowable-credit, and economic-premium effects when the annual result is `actionable`, with all tax-family, coverage, benchmark, and addback evidence disclosed.” Do not call it an APTC reconciliation or Form 8962 result. Do not turn a non-actionable year into a cliff-safe action. | No blocker for disclosed planning comparisons with actionable evidence. Any implementation action still requires exact-ledger readiness plus executable account/person sourcing and professional confirmation of Marketplace/tax facts. |
 | IRMAA | **Shipped, planning-grade.** Part B and D surcharges use MAGI from two years before the premium year, parameterized filing-status tiers, historical/recent MAGI seeds, survivor filing treatment, and an opt-in SSA-44 relief approximation. | [Rules §7](domain-rules-reference.md#7-medicare-and-irmaa-2026); [Medicare calculator](../../packages/engine/src/tax/medicare.ts); [annual ledger](../../packages/engine/src/projection/simulate.ts) | [Unit tests](../../packages/engine/src/tax/medicare.test.ts); [golden tests](../../packages/engine/src/tax/medicare.golden.test.ts); [external golden tests](../../packages/engine/src/tax/medicare.external.golden.test.ts) | SSA-44 is a documented prior-year stand-in and models only selected life-changing events; it neither determines appeal eligibility nor files the form. Early projection years depend on user-provided history or the recent-MAGI fallback. | “Estimate IRMAA tier and surcharge effects from the two-year lookback and compare modeled strategies; flag SSA-44 as an assumption requiring professional/SSA confirmation.” | No blocker for disclosed tier estimates. SSA-44 filing and exact redetermination remain outside cockpit v1. |
-| Social Security benefit taxation | **Partial.** The federal calculator applies the unindexed 0/50/85% provisional-income tiers, and taxable benefits flow into AGI/MAGI and downstream tax calculations. Its tax input now accepts tax-exempt interest; an actionable ACA annual contract threads its characterized amount through provisional income and ACA MAGI. | [Rules §3](domain-rules-reference.md#3-social-security-benefit-taxation); [federal calculator](../../packages/engine/src/tax/federalTax.ts); [annual ledger](../../packages/engine/src/projection/simulate.ts) | [Federal-tax tests](../../packages/engine/src/tax/federalTax.test.ts); [external Social Security tax tests](../../packages/engine/src/tax/federalTaxSocialSecurity.external.golden.test.ts); [ACA addback integration](../../packages/engine/src/projection/simulate.test.ts) | The general plan still lacks a standalone characterized tax-exempt-interest income stream. Outside an explicit ACA year contract, municipal-bond interest is not available to the projection and can understate taxable benefits and other MAGI cascades. | “Estimate taxable Social Security from modeled taxable income, gains, benefits, and—inside an explicit ACA contract—characterized tax-exempt interest.” Do not rely on non-ACA results for an action when material tax-exempt interest is absent from the plan. | **Required for general-plan complete/action claims:** *Tax-exempt-interest Social Security/MAGI cascade*. |
+| Social Security benefit taxation | **Shipped end to end.** The federal calculator applies the unindexed 0/50/85% provisional-income tiers, and taxable benefits flow into AGI/MAGI and downstream tax calculations. General account-generated tax-exempt interest ships end to end: schema yield → ledger cash/characterization → section 86 provisional income → ACA/IRMAA MAGI → Roth sizing and bracket-targeted withdrawals → optimizer IRMAA lookback → scenario comparison income lines. | [Rules §3](domain-rules-reference.md#3-social-security-benefit-taxation); [federal calculator](../../packages/engine/src/tax/federalTax.ts); [annual ledger](../../packages/engine/src/projection/simulate.ts); [scenario comparison](../../packages/engine/src/scenarios/comparison.ts) | [Federal-tax tests](../../packages/engine/src/tax/federalTax.test.ts); [external Social Security tax tests](../../packages/engine/src/tax/federalTaxSocialSecurity.external.golden.test.ts); [ACA/IRMAA/yield integration](../../packages/engine/src/projection/simulate.test.ts); [comparison tests](../../packages/engine/src/scenarios/comparison.test.ts) | Private-activity-bond AMT preference is not modeled (registry record names it, understates tax for PAB holders); state sourcing is not modeled (never added to a state base; no universal exemption claim); attested ACA contract amounts govern their year via max(attested, generated). | “Estimate taxable Social Security from modeled income including account-generated and contract-attested tax-exempt interest; disclosed limitations above.” | No blocker for disclosed taxable-SS estimates that include account-generated and contract-attested tax-exempt interest. |
 | State and local income tax | **Shipped, planning-grade.** Packs for all 50 states and DC model brackets or flat rates, deductions, Social Security treatment, major private/public retirement exclusions, and encoded capital-gain treatment. State moves can split a year by month. Local tax is an optional flat percentage of modeled state taxable income. | [Rules §9](domain-rules-reference.md#9-state-and-local-income-tax); [state research](state-tax-research/); [state calculator](../../packages/engine/src/tax/stateTax.ts); [relocation engine](../../packages/engine/src/projection/relocation.ts) | [State-tax tests](../../packages/engine/src/tax/stateTax.test.ts); [state external golden tests](../../packages/engine/src/tax/stateTax.external.golden.test.ts); [relocation tests](../../packages/engine/src/projection/relocation.test.ts) | State packs cover big levers, not every credit, exclusion phase-out, service-date rule, surtax, reciprocity rule, or form. Local tax has no jurisdiction pack and does not model residency, sourcing, deductions, or local brackets. Property, sales, and cost-of-living effects are not income tax. | “Compare planning estimates of state income-tax effects and a user-entered flat local rate.” Do not call the result jurisdiction-complete or filing-grade. | Exact state/local action claims require jurisdiction-specific rule coverage and current source verification. |
 | Inherited accounts and heir tax | **Partial, two distinct estimates.** Inherited traditional accounts are non-convertible, can enforce annual distributions in years 1–9 on a Single Life Table divisor when the decedent had started RMDs, and sweep the account in year 10. Separately, ending-account beneficiary fields and user-entered heir rates estimate after-tax estate by account class. | [Inherited-account schema](../../packages/engine/src/model/plan.ts); [inherited distribution strategy](../../packages/engine/src/strategies/inheritedIra.ts); [account eligibility](../../packages/engine/src/strategies/accountEligibility.ts); [estate depth](../features/taxes.md#guaranteed-income-and-estate-depth) | [Inherited-strategy tests](../../packages/engine/src/strategies/inheritedIra.test.ts); [eligibility tests](../../packages/engine/src/strategies/accountEligibility.test.ts); [estate tests](../../packages/engine/src/projection/guaranteedIncomeEstate.test.ts) | The annual divisor is the IRS Single Life Table figure fixed at the age attained in the year after death and reduced by one for each later year, which is the rule (`treas-reg-1-401-a-9-5-d-3-beneficiary-single-life-denominator`). Two gaps remain, both registered as overstating tax: the greater-of-employee-life-expectancy test is not applied, because the schema carries no decedent age (`treas-reg-1-401-a-9-5-d-1-ii-greater-of-employee-life-expectancy`), and eligible designated beneficiaries are not modeled at all, so every inherited traditional account is put on the ten-year rule and a whole-of-life stretch is compressed into ten years (`irc-401-a-9-E-ii-eligible-designated-beneficiary`). The heir-tax metric uses user-entered marginal-rate assumptions, not an heir's actual return, estate/inheritance tax, trust, basis, or distribution schedule. | “Show the 10-year inherited-account horizon and an assumption-driven after-tax estate estimate.” Do not claim compliant annual inherited-IRA RMD amounts or actual heir tax liability. | **Required for annual distribution actions:** *Inherited IRA compliance depth*. |
 | Net unrealized appreciation (NUA) | **Absent.** Equity-comp accounts have aggregate value/basis and vesting support, but no qualified-plan employer-stock NUA election or distribution path. | [Equity-comp schema](../../packages/engine/src/model/plan.ts); absence verified against the [annual ledger](../../packages/engine/src/projection/simulate.ts) and strategy modules | No NUA tests exist. Existing equity-comp and gain tests do not establish NUA. | The engine does not model triggering events, lump-sum distribution qualification, in-kind employer-stock transfer, ordinary tax on plan basis, later LTCG on NUA, account ownership, or election tradeoffs. | **No NUA claim, opportunity, comparison, or action in cockpit v1.** | NUA is excluded from cockpit v1 unless a separately sourced and tested NUA model ships. |
diff --git a/DOCS/features/taxes.md b/DOCS/features/taxes.md
index 611c4fb..a103a3b 100644
--- a/DOCS/features/taxes.md
+++ b/DOCS/features/taxes.md
@@ -91,7 +91,9 @@ State brackets are a separate question and are still held nominal (see `params/s
 - **ACA planning-year premium tax credit** (pre-65): the exact ledger reconciles current-year healthcare
   cash need, withdrawals, tax, and ACA household MAGI before reporting a result
   ([aca.ts](../../packages/engine/src/tax/aca.ts)). ACA MAGI is final federal AGI plus nontaxable Social
-  Security, characterized tax-exempt interest, explicit foreign-exclusion addbacks, and required-filer
+  Security, characterized tax-exempt interest (which can now be account-generated, with an attested ACA
+  contract amount governing its year via max(attested, generated), and the plan-derived/contradicted
+  support codes marking provenance), explicit foreign-exclusion addbacks, and required-filer
   dependent MAGI. Addbacks affect ACA MAGI without becoming ordinary taxable income; the foreign-exclusion
   amount also participates in Social Security provisional income under §86. Signed federal AGI is preserved
   through ACA component assembly and only the final household total is floored at zero.
@@ -328,6 +330,11 @@ Taxable brokerage accounts can model annual tax drag before withdrawals. Each ta
 - `interestYieldPct`: taxable interest generated from the start-of-year balance.
 - `dividendYieldPct`: dividends generated from the start-of-year balance.
 - `qualifiedRatio`: share of dividends taxed federally at qualified-dividend / long-term capital-gain rates.
+- `taxExemptInterestYieldPct`: municipal-bond (and similar) interest generated as a percent of start-of-year
+  balance. Excluded from ordinary income and AGI; included in Social Security provisional income, ACA household
+  MAGI, and IRMAA MAGI history per the domain rules section 20 matrix. Follows `reinvestDividends` and the
+  total-return yield subtraction like the taxable yields. Never derived from the allocation blend, and a bond
+  sleeve's yield belongs in exactly one of the two interest fields (`interestYieldPct` or this field).
 - `reinvestDividends`: whether generated yield is reinvested into the account or paid into annual cash flow.
 
 Interest and non-qualified dividends are ordinary income. Qualified dividends are **not** treated as realized
diff --git a/packages/engine/src/scenarios/comparison.test.ts b/packages/engine/src/scenarios/comparison.test.ts
index e3e6371..fc84219 100644
--- a/packages/engine/src/scenarios/comparison.test.ts
+++ b/packages/engine/src/scenarios/comparison.test.ts
@@ -3,16 +3,21 @@ import { describe, expect, it } from 'vitest'
 import { asAccountId, asActionId, asAllocationId, asPersonId } from '../actions/identity.js'
 import { asPositiveUsdCents } from '../actions/money.js'
 import { createFlatTaxCalculator } from '../projection/flatTax.js'
+import { createFederalTaxCalculator } from '../tax/federalTax.js'
 import {
   cashAccount,
   recurringOrdinaryIncome,
   setAcaYearContract,
   singlePersonPlan,
+  socialSecurityIncome,
+  taxableAccount,
   validatePlan,
 } from '../testing/planFixtures.js'
 import { compareScenarioPlans, compareScenarioSpendingCapacityResults } from './comparison.js'
 import { scenarioPlanSnapshotHash } from './patch.js'
 
+const federalTax = createFederalTaxCalculator()
+
 const noTax = createFlatTaxCalculator(0)
 const stochastic = {
   model: { type: 'lognormal' as const, inflationMeanPct: 0 },
@@ -401,4 +406,224 @@ describe('compareScenarioPlans', () => {
       }),
     ).toThrow('pathCount')
   })
+
+  it('reconciles lifetime tax-exempt interest to annual ledger rows and raises tax through the §86 cascade', () => {
+    const baseline = singlePersonPlan({
+      dob: '1964-06-15',
+      planningAge: 70,
+      retirementAge: null,
+    })
+    baseline.accounts = [cashAccount('cash', 400_000), taxableAccount('brokerage', 100_000, 100_000)]
+    baseline.incomes = [
+      {
+        type: 'oneTime',
+        id: 'ordinary',
+        label: 'Ordinary',
+        year: 2026,
+        amount: 12_900,
+        taxTreatment: 'ordinary',
+      },
+      socialSecurityIncome('ss', 2_000, 62),
+    ]
+    const proposal = structuredClone(baseline)
+    const brokerage = proposal.accounts.find((a) => a.id === 'brokerage')
+    if (!brokerage || brokerage.type !== 'taxable') throw new Error('expected brokerage')
+    brokerage.taxExemptInterestYieldPct = 13
+    brokerage.reinvestDividends = false
+
+    const result = compareScenarioPlans(validatePlan(baseline), validatePlan(proposal), {
+      startYear: 2026,
+      taxCalculatorForPlan: () => federalTax,
+    })
+
+    const annualSum = result.annual.reduce(
+      (total, row) => total + (row.values.taxExemptInterest.proposal ?? 0),
+      0,
+    )
+    expect(result.income.taxExemptInterest.baseline).toBe(0)
+    expect(result.income.taxExemptInterest.proposal).toBeCloseTo(annualSum, 6)
+    expect(result.income.taxExemptInterest.delta).toBeCloseTo(annualSum, 6)
+    expect(result.income.taxExemptInterest.delta).toBeGreaterThan(0)
+    // Proposal pays more ordinary tax via higher taxable Social Security; the
+    // exempt coupon itself never enters AGI as ordinary income.
+    expect(result.headline.lifetimeTax.delta).toBeGreaterThan(0)
+  })
+
+  it('places §86 phase-in below / within / above saturation for single-year tax deltas', () => {
+    // Three regimes for single-filer §86 (tier50 = 25k, tier85 = 34k):
+    // 1. Below both thresholds → zero taxable-SS delta from the exempt coupon.
+    // 2. Inside the phase-in band → positive tax delta from higher taxable SS.
+    // 3. Above 85% saturation → zero marginal tax delta from more exempt interest.
+    const compareRegime = (ordinary: number, yieldPct: number) => {
+      const baseline = singlePersonPlan({
+        dob: '1964-06-15',
+        planningAge: 62,
+        retirementAge: null,
+      })
+      baseline.accounts = [
+        cashAccount('cash', 400_000),
+        taxableAccount('brokerage', 100_000, 100_000),
+      ]
+      baseline.incomes = [
+        {
+          type: 'oneTime',
+          id: 'ordinary',
+          label: 'Ordinary',
+          year: 2026,
+          amount: ordinary,
+          taxTreatment: 'ordinary',
+        },
+        socialSecurityIncome('ss', 2_000, 62),
+      ]
+      const proposal = structuredClone(baseline)
+      const brokerage = proposal.accounts.find((a) => a.id === 'brokerage')
+      if (!brokerage || brokerage.type !== 'taxable') throw new Error('expected brokerage')
+      brokerage.taxExemptInterestYieldPct = yieldPct
+      brokerage.reinvestDividends = false
+      return compareScenarioPlans(validatePlan(baseline), validatePlan(proposal), {
+        startYear: 2026,
+        taxCalculatorForPlan: () => federalTax,
+      })
+    }
+
+    // Below: provisional without exempt ≈ 0 ordinary + 0.5×24k SS = 12k < 25k;
+    // +2k exempt still < 25k → no taxable SS on either side.
+    const below = compareRegime(0, 2)
+    expect(below.income.taxExemptInterest.delta).toBeCloseTo(2_000, 6)
+    expect(below.headline.lifetimeTax.delta).toBe(0)
+
+    // Inside: ordinary 12.9k + half SS 12k = 24.9k without exempt (at/under
+    // tier50); +13k exempt enters the 50%/85% band and raises taxable SS.
+    const inside = compareRegime(12_900, 13)
+    expect(inside.income.taxExemptInterest.delta).toBeCloseTo(13_000, 6)
+    expect(inside.headline.lifetimeTax.delta).toBeGreaterThan(0)
+
+    // Saturated: ordinary 50k + half SS already past the 85% cap; +5k exempt
+    // cannot raise taxable SS further, so the tax delta stays 0.
+    const above = compareRegime(50_000, 5)
+    expect(above.income.taxExemptInterest.delta).toBeCloseTo(5_000, 6)
+    expect(above.headline.lifetimeTax.delta).toBe(0)
+  })
+
+  it('lowers modeled ACA PTC pre-65 and moves IRMAA two years later from lookback MAGI', () => {
+    // Pre-65 ACA: higher characterized MAGI from account-generated exempt yield
+    // reduces modeled allowable PTC; delta reconciles to the year row.
+    const acaBaseline = singlePersonPlan({
+      dob: '1964-01-01',
+      planningAge: 62,
+      retirementAge: null,
+    })
+    acaBaseline.accounts = [taxableAccount('brokerage', 100_000, 100_000)]
+    acaBaseline.incomes = [
+      {
+        type: 'oneTime',
+        id: 'ordinary',
+        label: 'Ordinary',
+        year: 2026,
+        amount: 40_000,
+        taxTreatment: 'ordinary',
+      },
+    ]
+    setAcaYearContract(acaBaseline, {
+      year: 2026,
+      monthlyEnrollment: 1_000,
+      monthlySlcsp: 1_000,
+    })
+    const acaProposal = structuredClone(acaBaseline)
+    const acaBrokerage = acaProposal.accounts.find((a) => a.id === 'brokerage')
+    if (!acaBrokerage || acaBrokerage.type !== 'taxable') throw new Error('expected brokerage')
+    acaBrokerage.taxExemptInterestYieldPct = 20
+    acaBrokerage.reinvestDividends = false
+
+    const acaResult = compareScenarioPlans(validatePlan(acaBaseline), validatePlan(acaProposal), {
+      startYear: 2026,
+      taxCalculatorForPlan: () => federalTax,
+    })
+    const acaYear = acaResult.annual[0]!.values
+    expect(acaResult.income.taxExemptInterest.delta).toBeCloseTo(20_000, 6)
+    expect(acaResult.aca.modeledAllowablePtc.delta).toBe(
+      acaYear.acaModeledAllowablePtc.delta,
+    )
+    expect(acaResult.aca.modeledAllowablePtc.delta).toBeLessThan(0)
+
+    // 65+ IRMAA: exempt yield in the lookback year (2026) moves the premium in
+    // 2028; surcharge delta reconciles to the 2028 annual row.
+    const irmaaBaseline = singlePersonPlan({
+      dob: '1960-06-15',
+      planningAge: 68,
+      retirementAge: null,
+    })
+    irmaaBaseline.assumptions.recentAnnualMagi = 0
+    irmaaBaseline.accounts = [
+      cashAccount('cash', 500_000),
+      taxableAccount('brokerage', 100_000, 100_000),
+    ]
+    irmaaBaseline.incomes = [
+      {
+        type: 'oneTime',
+        id: 'ordinary',
+        label: 'Ordinary',
+        year: 2026,
+        amount: 105_000,
+        taxTreatment: 'ordinary',
+      },
+    ]
+    const irmaaProposal = structuredClone(irmaaBaseline)
+    const irmaaBrokerage = irmaaProposal.accounts.find((a) => a.id === 'brokerage')
+    if (!irmaaBrokerage || irmaaBrokerage.type !== 'taxable') throw new Error('expected brokerage')
+    irmaaBrokerage.taxExemptInterestYieldPct = 5
+    irmaaBrokerage.reinvestDividends = false
+
+    const irmaaResult = compareScenarioPlans(
+      validatePlan(irmaaBaseline),
+      validatePlan(irmaaProposal),
+      {
+        startYear: 2026,
+        taxCalculatorForPlan: () => federalTax,
+      },
+    )
+    const lookbackYear = irmaaResult.annual.find((row) => row.year === 2026)!
+    const premiumYear = irmaaResult.annual.find((row) => row.year === 2028)!
+    expect(lookbackYear.values.taxExemptInterest.delta).toBeCloseTo(5_000, 6)
+    expect(lookbackYear.values.magi.delta).toBeCloseTo(5_000, 6)
+    expect(premiumYear.values.irmaaSurcharge.delta).toBeGreaterThan(0)
+    expect(irmaaResult.irmaa.surcharge.delta).toBe(
+      irmaaResult.annual.reduce((total, row) => total + (row.values.irmaaSurcharge.delta ?? 0), 0),
+    )
+    expect(irmaaResult.irmaa.surcharge.delta).toBe(premiumYear.values.irmaaSurcharge.delta)
+  })
+
+  it('keeps tax delta exactly 0 when exempt yield cannot reach SS, ACA, or NIIT', () => {
+    const baseline = singlePersonPlan({
+      dob: '1966-01-01',
+      planningAge: 60,
+      retirementAge: null,
+    })
+    baseline.accounts = [taxableAccount('brokerage', 100_000, 100_000)]
+    baseline.incomes = [
+      {
+        type: 'oneTime',
+        id: 'ordinary',
+        label: 'Ordinary',
+        year: 2026,
+        amount: 30_000,
+        taxTreatment: 'ordinary',
+      },
+    ]
+    // No Social Security, ACA off, MAGI well under NIIT thresholds.
+    const proposal = structuredClone(baseline)
+    const brokerage = proposal.accounts.find((a) => a.id === 'brokerage')
+    if (!brokerage || brokerage.type !== 'taxable') throw new Error('expected brokerage')
+    brokerage.taxExemptInterestYieldPct = 50
+    brokerage.reinvestDividends = false
+
+    const result = compareScenarioPlans(validatePlan(baseline), validatePlan(proposal), {
+      startYear: 2026,
+      taxCalculatorForPlan: () => federalTax,
+    })
+
+    expect(result.income.taxExemptInterest.delta).toBeCloseTo(50_000, 6)
+    expect(result.headline.lifetimeTax.delta).toBe(0)
+    expect(result.annual[0]!.values.tax.delta).toBe(0)
+  })
 })
diff --git a/packages/engine/src/scenarios/comparison.ts b/packages/engine/src/scenarios/comparison.ts
index 13967ae..a18d8fa 100644
--- a/packages/engine/src/scenarios/comparison.ts
+++ b/packages/engine/src/scenarios/comparison.ts
@@ -114,6 +114,7 @@ export interface ScenarioIncomeComparison {
   ordinaryDividends: ScalarComparison
   qualifiedDividends: ScalarComparison
   taxableYield: ScalarComparison
+  taxExemptInterest: ScalarComparison
   total: ScalarComparison
 }
 
@@ -205,6 +206,7 @@ export interface AnnualComparisonValues {
   acaGrossEnrollmentPremium: number
   acaModeledAllowablePtc: number
   acaEconomicNetPremium: number
+  taxExemptInterest: number
 }
 
 export type AnnualComparisonValue = {
@@ -240,6 +242,7 @@ const ANNUAL_VALUE_KEYS: Array<keyof AnnualComparisonValues> = [
   'acaGrossEnrollmentPremium',
   'acaModeledAllowablePtc',
   'acaEconomicNetPremium',
+  'taxExemptInterest',
 ]
 
 export interface ScenarioRiskComparison {
@@ -354,6 +357,7 @@ function aggregateIncome(years: YearResult[]) {
     ordinaryDividends: sum(years, (y) => y.incomes.ordinaryDividends),
     qualifiedDividends: sum(years, (y) => y.incomes.qualifiedDividends),
     taxableYield: sum(years, (y) => y.incomes.taxableYield),
+    taxExemptInterest: sum(years, (y) => y.incomes.taxExemptInterest),
     total: sum(years, (y) => y.incomes.total),
   }
 }
@@ -416,6 +420,7 @@ function annualValues(year: YearResult): AnnualComparisonValues {
     acaGrossEnrollmentPremium: year.aca?.grossEnrollmentPremium ?? 0,
     acaModeledAllowablePtc: year.aca?.modeledAllowablePtc ?? 0,
     acaEconomicNetPremium: year.aca?.economicNetPremium ?? 0,
+    taxExemptInterest: year.incomes.taxExemptInterest,
   }
 }
 
diff --git a/packages/planner-ui/src/planner/ResultsPage.tsx b/packages/planner-ui/src/planner/ResultsPage.tsx
index 90df087..b73309a 100644
--- a/packages/planner-ui/src/planner/ResultsPage.tsx
+++ b/packages/planner-ui/src/planner/ResultsPage.tsx
@@ -76,6 +76,7 @@ const INCOME_SOURCES = [
   { key: 'recurring', label: 'Other recurring', color: 'var(--chart-5)' },
   { key: 'oneTime', label: 'One-time', color: 'var(--chart-6)' },
   { key: 'taxableYield', label: 'Brokerage yield', color: 'var(--chart-7)' },
+  { key: 'taxExemptInterest', label: 'Tax-exempt interest', color: 'var(--chart-3)' },
 ] as const
 
 /** Spending categories, stacked bottom-to-top; sums to expenses + tax + penalties. */
@@ -269,6 +270,7 @@ export function ResultsPage() {
         recurring: adj(y.year, y.incomes.recurring),
         oneTime: adj(y.year, y.incomes.oneTime),
         taxableYield: adj(y.year, y.incomes.taxableYield),
+        taxExemptInterest: adj(y.year, y.incomes.taxExemptInterest),
       })),
     [view, adj],
   )
@@ -291,7 +293,7 @@ export function ResultsPage() {
 
   const handleCsv = () => {
     const cols = [
-      'year', 'filingStatus', 'wages', 'socialSecurity', 'pension', 'annuity', 'tipsLadder', 'recurring', 'oneTimeIncome', 'taxableInterest', 'ordinaryDividends', 'qualifiedDividends', 'taxableYield', 'totalIncome',
+      'year', 'filingStatus', 'wages', 'socialSecurity', 'pension', 'annuity', 'tipsLadder', 'recurring', 'oneTimeIncome', 'taxableInterest', 'Tax-exempt interest', 'ordinaryDividends', 'qualifiedDividends', 'taxableYield', 'totalIncome',
       'baseSpending', 'goals', 'debtService', 'propertyCosts', 'healthcare', 'insurancePremiums', 'careCost', 'ltcBenefit', 'requiredSpending', 'targetSpending', 'idealSpending', 'excessSpending', 'intendedSpending', 'totalExpenses', 'contributions', 'employerMatch', 'rmd', 'qcd',
       'rothConversion', 'tax', 'amt', 'penalties', 'magi', 'withdrawals', 'realizedGains', 'lossCarryforwardUsed', 'lossCarryforwardRemaining', 'shortfall', 'investable',
       'requiredShortfall', 'targetShortfall', 'idealShortfall', 'excessShortfall', 'guardrailAction', 'guardrailFactor', 'flexibleGoalsFunded', 'flexibleGoalsPartiallyFunded', 'flexibleGoalsDeferred', 'flexibleGoalsSkipped', 'flexibleGoalFundedAmount', 'flexibleGoalUnfundedAmount', 'insuranceCashValue', 'ladderValue', 'deathBenefit', 'netWorth',
@@ -301,7 +303,7 @@ export function ResultsPage() {
       lines.push(
         [
           y.year, y.filingStatus, y.incomes.wages, y.incomes.socialSecurity, y.incomes.pension, y.incomes.annuity, y.incomes.tipsLadder, y.incomes.recurring,
-          y.incomes.oneTime, y.incomes.taxableInterest, y.incomes.ordinaryDividends, y.incomes.qualifiedDividends, y.incomes.taxableYield, y.incomes.total, y.expenses.baseSpending, y.expenses.oneTimeGoals, y.expenses.debtService,
+          y.incomes.oneTime, y.incomes.taxableInterest, y.incomes.taxExemptInterest, y.incomes.ordinaryDividends, y.incomes.qualifiedDividends, y.incomes.taxableYield, y.incomes.total, y.expenses.baseSpending, y.expenses.oneTimeGoals, y.expenses.debtService,
           y.expenses.propertyCosts, y.expenses.healthcare, y.expenses.insurancePremiums, y.expenses.careCost, y.expenses.ltcBenefit, y.expenses.requiredSpending, y.expenses.targetSpending, y.expenses.idealSpending, y.expenses.excessSpending, y.expenses.intendedSpending, y.expenses.total, y.contributions, y.employerMatch, y.rmd, y.qcd, y.rothConversion, y.tax, y.amt, y.penalties,
           y.magi, y.withdrawals.total, y.realizedGains, y.capitalLossUsedAgainstGains + y.capitalLossUsedAgainstOrdinary, y.capitalLossCarryforwardRemaining, y.shortfall, y.investableTotal,
           y.requiredShortfall, y.targetShortfall, y.idealShortfall, y.excessShortfall, y.guardrailAction, y.expenses.guardrailFactor.toFixed(2), y.flexibleGoals.funded, y.flexibleGoals.partiallyFunded, y.flexibleGoals.deferred, y.flexibleGoals.skipped, y.flexibleGoals.fundedAmount, y.flexibleGoals.unfundedAmount, y.insuranceCashValue, y.ladderValue, y.deathBenefit, y.netWorth,
diff --git a/packages/planner-ui/src/planner/ScenariosPage.tsx b/packages/planner-ui/src/planner/ScenariosPage.tsx
index 6ae0bbf..676ac84 100644
--- a/packages/planner-ui/src/planner/ScenariosPage.tsx
+++ b/packages/planner-ui/src/planner/ScenariosPage.tsx
@@ -685,6 +685,7 @@ function ScenarioDetail({
           { label: 'Recurring income', metric: comparison.income.recurring, format: 'money' },
           { label: 'One-time income', metric: comparison.income.oneTime, format: 'money' },
           { label: 'Taxable yield', metric: comparison.income.taxableYield, format: 'money' },
+          { label: 'Tax-exempt interest', metric: comparison.income.taxExemptInterest, format: 'money' },
           { label: 'Total gross income', metric: comparison.income.total, format: 'money' },
         ]}
       />
diff --git a/packages/planner-ui/src/planner/sections/AccountFields.tsx b/packages/planner-ui/src/planner/sections/AccountFields.tsx
index ae9cda3..6e84dda 100644
--- a/packages/planner-ui/src/planner/sections/AccountFields.tsx
+++ b/packages/planner-ui/src/planner/sections/AccountFields.tsx
@@ -131,6 +131,12 @@ export function AccountFields({ account, index }: { account: Account; index: num
             value={(account.qualifiedRatio ?? 0.85) * 100}
             onCommit={(v) => set('qualifiedRatio', Math.min(1, Math.max(0, (v ?? 85) / 100)))}
           />
+          <PercentField
+            label="Tax-exempt interest yield %"
+            help="Annual interest from municipal bonds held in this account, as a percent of its start-of-year balance. Enter a bond's yield here or in Interest yield, not in both fields. This income never joins ordinary taxable income, but it does raise taxable Social Security, ACA household MAGI, and the income Medicare reads for IRMAA two years later. Two limits to know: the model treats none of it as private-activity-bond interest for AMT, and it does not add any of it to state taxable income even though some states tax municipal bonds from other states."
+            value={account.taxExemptInterestYieldPct ?? 0}
+            onCommit={(v) => set('taxExemptInterestYieldPct', v ?? 0)}
+          />
           <CheckboxField
             label="Reinvest yield"
             help="When checked, interest and dividends stay in the brokerage account and add to basis. When unchecked, they flow into annual cash surplus."
@@ -165,6 +171,12 @@ export function AccountFields({ account, index }: { account: Account; index: num
             allowNull
             onCommit={(v) => set('qualifiedRatio', v === null || v === undefined ? undefined : Math.min(1, Math.max(0, v / 100)))}
           />
+          <PercentField
+            label="Tax-exempt interest yield %"
+            help="Annual interest from municipal bonds held in this account, as a percent of its start-of-year balance. Enter a bond's yield here or in Interest yield, not in both fields. This income never joins ordinary taxable income, but it does raise taxable Social Security, ACA household MAGI, and the income Medicare reads for IRMAA two years later. Two limits to know: the model treats none of it as private-activity-bond interest for AMT, and it does not add any of it to state taxable income even though some states tax municipal bonds from other states."
+            value={account.taxExemptInterestYieldPct ?? 0}
+            onCommit={(v) => set('taxExemptInterestYieldPct', v ?? 0)}
+          />
           <CheckboxField
             label="Reinvest yield"
             help="When checked, interest and dividends stay in the brokerage account and add to basis. When unchecked, they flow into annual cash surplus."
