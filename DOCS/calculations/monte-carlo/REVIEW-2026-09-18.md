# Independent review, 2026-09-18

Reviewer: Cursor (composer-2.5), headless, read-only. Scope: the twenty-four worksheets of the Monte Carlo slice, including the five re-derived on 2026-09-18 after the evidence rulings and the one whose print was corrected. The reviewer recomputed each worked example from the Inputs and Justification sections without executing the engine and without reading any implementation body; the only engine text it consulted was the signatures-and-comments extracts (the fresher one carrying the 2026-09-18 doc-comment corrections and the full historical table). Verbatim output follows.

---

# Independent Calculation Worksheet Review

Branch: `grok/b1-p4-cards-monte-carlo`  
Signature sources: `b1-p4-signatures-mc-r2.md` for pins in `montecarlo/marketModels.ts`, `montecarlo/riskBasedGuardrails.ts`, `montecarlo/rng.ts`, `montecarlo/historicalReturns.ts`; `b1-p4-signatures.md` for all other pins.

---

## 1. `allocation-cholesky-factor`

**Recomputed:** For `[[1,0.5],[0.5,1]]`: `L₁₁=1`; `L₂₁=0.5/1=0.5`; `L₂₂=√(1−0.5²)=√(3/4)=√3/2=0.866025403784439`. Lower factor `[[1,0],[0.5, 0.866025403784439]]`.

**Match:** yes.

**Tolerance:** `1e-12` is justified — exact algebraic result from `√(3/4)`.

**Wrong reading check:** `√(1−0.5)=√0.5=0.7071067812` ✓; symmetric upper entry `0.5` instead of `0` ✓.

**Contract:** Claim (lower-triangular `L` with `LLᵀ=matrix`, diagonal clamp `1e-12`) matches `choleskyDecompose` doc comment in older extract.

**Deterministic/degenerate:** Pure 2×2 algebra; exercises the standard Cholesky branch (not the clamp branch).

**Verdict:** approve.

---

## 2. `historical-market-series`

**Recomputed:** Inclusive count `2023−1928+1=96`. Column sums from the 96 rows in mc-r2 extract: stocks `1118.9`, bonds `466.6`, inflation `298.8` (verified by decade grouping). Endpoints: 1928 `43.8/0.8/−1.2`, 2023 `26.1/3.9/3.4`.

**Match:** yes.

**Tolerance:** Exact at one-decimal transcription; source fidelity bounded by stated ≈0.1–0.5 pp — justified.

**Wrong reading check:** Exclusive span `95` ✓; treating `43.8` as fraction ✓.

**Contract:** Matches `HISTORICAL_YEARS` type and doc comment (96 years, percent units, approximate transcription).

**Deterministic/degenerate:** Pure data inventory; no stochastic branch.

**Verdict:** approve.

---

## 3. `historical-portfolio-mean`

**Recomputed:** Blended sum `=0.6(1118.9)+0.4(466.6)=671.34+186.64=857.98`; mean `=857.98/96=8.93729166666667%`.

**Match:** yes.

**Tolerance:** `1e-12` pp — justified (exact rational `85798/9600`).

**Wrong reading check:** `/95` → `9.03157894737%` ✓; unweighted mean of class means → `8.2578125%` ✓.

**Contract:** Matches `meanPortfolioReturnPct(equityWeightPct: number): number` — arithmetic mean of blended returns, percent.

**Deterministic/degenerate:** Closed-form from column sums.

**Verdict:** approve.

---

## 4. `historical-portfolio-return-blend`

**Recomputed:** `w=60/100=0.6`; `0.6(43.8)+0.4(0.8)=26.28+0.32=26.6%`.

**Match:** yes.

**Tolerance:** `1e-12` — justified (exact decimal arithmetic).

**Wrong reading check:** Reversed weights → `18.0%` ✓; `60×43.8−59×0.8=2580.8%` ✓.

**Contract:** Matches `portfolioReturnPct(year, equityWeightPct)` — `w·stocks+(1−w)·bonds`, percent, no rounding stated.

**Deterministic/degenerate:** Single-row closed form.

**Verdict:** approve.

---

## 5. `long-term-care-shock-sampling`

**Recomputed:** Standard Bernoulli: episode iff `U < incidence`. `U=0.75`, `incidence=0.5` → `0.75 ≥ 0.5` → no episode → empty list.

**Match:** yes (exact count 0).

**Tolerance:** Exact — justified.

**Wrong reading check:** Reversed comparison (`U > incidence` means event) → `0.75>0.5` → one episode ✓.

**Contract:** Matches `sampleCareEvents(rng, people, startYear, params): CareEvent[]` — zero or one episode per person; incidence is a probability; annual cost in today's dollars.

**Deterministic/degenerate:** Fixed draw `0.75` pins the no-event branch.

**Verdict:** approve.

---

## 6. `market-model-ar1-shock`

**Recomputed:** `x₁=0.2(10)+0=2`; `x₂=0.2(2)+0=0.4` pp.

**Match:** yes.

**Tolerance:** `1e-12` — justified (exact rationals).

**Wrong reading check:** Persistence `0.8` → `8, 6.4` ✓; adding `φ` as pp → `10.2` ✓.

**Contract:** Matches `createAR1Model` — centered serially correlated shocks, mean-zero, `phi` autoregression coefficient.

**Deterministic/degenerate:** Zero innovation degenerates AR(1) to deterministic decay; exercises recurrence, not the innovation draw.

**Verdict:** approve.

---

## 7. `market-model-cape-conditioned`

**Recomputed:** Base lognormal shock `0` (vol `0`); CAPE adjustment `=0.15(25−20)=0.75`; shifted shock `=0−0.75=−0.75` pp.

**Match:** yes.

**Tolerance:** `1e-12` — justified.

**Wrong reading check:** Sign flip → `+0.75` ✓; `25×0.15` without reference → `−3.75` ✓.

**Contract:** Matches `createCapeConditionedModel` — lognormal base with linear CAPE taper above 20; sensitivity in pp per CAPE point.

**Deterministic/degenerate:** Zero vol and fixed CAPE pin the mean shift only; trivial volatility branch.

**Verdict:** approve.

---

## 8. `market-model-empirical-history`

**Recomputed:** Per-row blend `=0.6·stocks+0.4·bonds`. Sum of 96 blends (from column sums) `=857.98` → IEEE sum `857.9800000000002`; mean `=857.9800000000002/96=8.937291666666669%`. 1928 blend `=26.599999999999998%`. Centered `=26.599999999999998−8.937291666666669=17.662708333333327` pp; raw `=26.599999999999998` pp; inflation `=−1.2%`.

**Match:** yes.

**Tolerance:** `1e-12` on floating blends/shocks — justified for double-precision residuals.

**Wrong reading check:** Round mean `8` → shock `18.599999999999998` ✓.

**Contract:** Matches corrected `createEmpiricalModel` — no caller-supplied return or mean; centered subtracts `meanPortfolioReturnPct(w)`; raw publishes blend; inflation as-is.

**Deterministic/degenerate:** `nextInt(96)=0` fixes row 1928; exercises centered vs raw modes, not random year selection.

**Revision note consistency:** First derivation treated sampled return and round mean as inputs; corrected contract rejects both — **consistent**.

**Verdict:** approve.

---

## 9. `market-model-garch-variance`

**Recomputed:**

Year 1: `v₁=1+0.1(0²)+0.8(0.0001)=1.00008`; `σ₁=√1.00008·(100/100)·5=5.00019999600016`; `s₁=5.00019999600016`; published `=500.019999600016%`; inflation `=0%`.

Year 2: `e₁=s₁`; `v₂=1+0.1(5.00019999600016²)+0.8(1.00008)=4.3002639596…≈4.300264`; `σ₂=√4.300264·5=10.36853895204141`; `s₂=5.184269476020705`; published `=518.4269476020705%`.

**Match:** yes.

**Tolerance:** `1e-9` on published shocks — justified for chained floating roots/squares.

**Wrong reading check:** Feedback `√v₁·Z₁=1.000039999200032` → `v₂=1.900072` → year-2 shock `≈344.6077480266513%` ✓; omit factor `5` → year-1 shock `≈100.0039999200032%` ✓.

**Contract:** Matches corrected `createGarchModel` doc — fixed `v₀=0.0001`, `e₀=0`; `e_t=s_t` (scaled shock, not standardized innovation); published shock `=s_t·100` percent.

**Deterministic/degenerate:** Fixed `Z` draws and zero inflation; exercises full recursion with nontrivial feedback on year 2 (not the zero-shock fixed point).

**Revision note consistency:** First derivation used caller-supplied prior state and published variance — **consistent** with correction.

**Verdict:** approve.

---

## 10. `market-model-gaussian-draw`

**Recomputed:** `σ=12` pp, `Z=−2` → shock `=12(−2)=−24` pp.

**Match:** yes (exact).

**Tolerance:** Exact — justified.

**Wrong reading check:** Double `/100` → `−0.24` ✓.

**Contract:** Matches `createGaussianModel` — additive, mean-zero, `σZ` in percentage points.

**Deterministic/degenerate:** Fixed `Z`; exercises scaling only.

**Verdict:** approve.

---

## 11. `market-model-historical-centered-bootstrap`

**Recomputed:** Same mean and 1928 blend as §8. Centered shock `=17.662708333333327` pp; inflation `=−1.2%`.

**Match:** yes.

**Tolerance:** `1e-12` — justified.

**Wrong reading check:** Round mean `8` → `18.599999999999998` ✓.

**Contract:** Matches `createHistoricalModel` iid mode — centers on `meanPortfolioReturnPct`, no external mean input.

**Deterministic/degenerate:** `nextInt(96)=0` fixes row; exercises centering, not block/sequence modes.

**Revision note consistency:** First derivation supplied mean independently — **consistent** with correction.

**Verdict:** approve.

---

## 12. `market-model-inflation-regime`

**Recomputed:** `U=0.10 < p=0.20` → high regime; zero innovation → inflation `=8%`.

**Match:** yes (exact).

**Tolerance:** Exact — justified.

**Wrong reading check:** Reversed comparison → base `3%` ✓; sum of means → `11%` ✓.

**Contract:** Matches `createInflationRegimeModel` — Bernoulli regime mixture; `baseInflationMeanPct` / `highInflationMean`; percent/year.

**Deterministic/degenerate:** Fixed draw pins high regime; zero vol collapses to regime mean.

**Verdict:** approve.

---

## 13. `market-model-lognormal-draw`

**Recomputed:** `σ=0` → shock `=100(e⁰−1)=0` pp; inflation `=3+0=3%`.

**Match:** yes (exact).

**Tolerance:** Exact — justified.

**Wrong reading check:** Gross multiplier as percent → `100` ✓.

**Contract:** Matches `createLognormalModel` — mean-preserving `100(exp(σZ−σ²/2)−1)`; inflation normal around configured mean.

**Deterministic/degenerate:** Zero vol is the trivial identity branch; still validates mean-preserving formula at `σ=0`.

**Verdict:** approve.

---

## 14. `market-model-regime-switch`

**Recomputed:** `U=0.9 ≥ p=0.05` → remain bull; zero vol → shock `=+4` pp deviation.

**Match:** yes (exact).

**Tolerance:** Exact — justified.

**Wrong reading check:** Mis-scaled probability → spurious switch ✓.

**Contract:** Matches `createRegimeSwitchModel` — bull/bear mean *deviations*; switch probability; inflation centered on mean.

**Deterministic/degenerate:** Fixed switch draw and zero vol; exercises stay-in-state branch.

**Verdict:** approve.

---

## 15. `market-model-reversed-history`

**Recomputed:** `n=96`; requested `L=3` clamped to `L=5`; `start=72` → window indices `72…76` (years 2000–2004). Path indices `76−i`:

| i | Year | Blend (60%) | Shock (pp) | Inflation |
|---|------|-------------|------------|-----------|
| 0 | 2004 | `0.6(10.7)+0.4(4.5)=8.22` | `−0.7172916666666698` | `3.3%` |
| 1 | 2003 | `0.6(28.4)+0.4(0.4)=17.2` | `8.26270833333333` | `1.9%` |
| 2 | 2002 | `0.6(−22)+0.4(15.1)=−7.16` | `−16.097291666666667` | `2.4%` |

Mean `=8.937291666666669%` (from `857.9800000000002/96`).

**Match:** yes.

**Tolerance:** `1e-12` — justified.

**Wrong reading check:** No floor → years `[2002,2001,2000]` ✓; forward replay → `[2000,2001,2002]` ✓.

**Contract:** Matches corrected `createReversedHistoryModel` — floor `L≥5`; index `start+L−1−(i mod L)`; centered blend minus dataset mean; inflation as-is.

**Deterministic/degenerate:** Fixed `start` and short path; exercises clamp floor and reverse indexing (not random `start`).

**Revision note consistency:** First derivation honored `L=3` window — **consistent** with five-year floor correction.

**Verdict:** approve.

---

## 16. `market-model-stationary-bootstrap`

**Recomputed:** `p=1/L=1/5=0.20`; draw `0.50≥0.20` → continue block → `true`.

**Match:** yes (exact Boolean).

**Tolerance:** Exact — justified.

**Wrong reading check:** `p=L=5` → restart every year ✓.

**Contract:** Matches `createStationaryBootstrapModel` — geometric blocks with `meanBlockLength` default 5.

**Deterministic/degenerate:** Fixed continuation draw; exercises continue-vs-restart decision only (not block content).

**Verdict:** approve.

---

## 17. `market-model-student-t-draw`

**Recomputed:** `df=5`; scaled `t=0·√((5−2)/5)=0·√(3/5)=0`; shock `=12(0)=0` pp.

**Match:** yes.

**Tolerance:** `0` — justified (exact zero).

**Wrong reading check:** Omit scaling → nonzero draws inflated to `12√(5/3)≈15.4919` pp ✓.

**Contract:** Matches `createStudentTModel` — variance-normalized t, then vol scaling; percent shocks.

**Deterministic/degenerate:** Zero t-draw; exercises scaling identity only.

**Verdict:** approve.

---

## 18. `market-model-user-shock`

**Recomputed:** Years 1 and 3: base vol `0` → shock `0`; year 2 (1-based) → `−20` pp. Vector `[0, −20, 0]`.

**Match:** yes (exact).

**Tolerance:** Exact — justified.

**Wrong reading check:** Zero-based year index → `[0,0,−20]` ✓.

**Contract:** Matches `createUserShockModel` — `shockYear` 1-based; additive `shockPct` in percent points.

**Deterministic/degenerate:** Zero base vol plus fixed shock year; exercises shock injection branch.

**Verdict:** approve.

---

## 19. `qlac-deferred-payout-placeholder`

**Recomputed:** Annual `=100000×0.16=16000`; monthly `=16000/12=1333.33333333333`.

**Match:** yes.

**Tolerance:** Exact cents on annual; `1e-9` dollars on monthly — justified.

**Wrong reading check:** `0.16%` → `$160` ✓; immediate age-85 anchor `$15300` ✓.

**Contract:** Matches `QLAC_DEFERRED_PAYOUT_RATE = 0.16` — fraction of premium, placeholder, no interpolation.

**Deterministic/degenerate:** Constant rate multiplication.

**Verdict:** approve.

---

## 20. `risk-based-guardrail-threshold-solver`

**Recomputed:** Bracket width `=4−0.02=3.98`. After 10 bisections, lattice spacing `=3.98/2¹⁰=3.98/1024=0.00388671875`. Solved `balanceFrac=0.02+k(3.98/1024)`, `k∈{1,…,1023}`. Example `k=1`: `0.02388671875` (not `0.02+4/1024=0.02390625`). With `pathCount=1`, success ∈ `{0,1}`; predicates `success≥0.70` and `success≥0.95` are identical → identical bisection choices and identical solved `balanceFrac` when both solve. `balanceDollars=balanceFrac×startingInvestable` exact; `successAtThreshold∈{0,1}`.

**Match:** yes (structural identities).

**Tolerance:** `1e-9` on lattice recovery — justified for `k` rounding.

**Wrong reading check:** Step `4/1024` without `0.02` offset ✓; returning `lo` vs `hi` ✓; analytic `S(f)=min(1,f/2)` thresholds `1.40/1.90` not injectable ✓.

**Contract:** Matches corrected `solveRiskBasedGuardrails` — no injectable success rule; 10 balance bisections (`BALANCE_BISECTION_ITERATIONS=10`); bracket `[0.02,4]`; returns `hi` when `success(mid)≥target` per worksheet/stopping description.

**Deterministic/degenerate:** Degenerate one-path Boolean success; documents lattice geometry and endpoint equivalence, not plan-specific dollar thresholds.

**Revision note consistency:** First derivation used injectable analytic `S(f)` — **consistent** with correction.

**Verdict:** approve.

---

## 21. `risk-based-starting-investable`

**Recomputed:** Investable `=100000+20000=120000`; home excluded.

**Match:** yes (exact dollars).

**Tolerance:** Exact — justified.

**Wrong reading check:** Include home → `$420000` ✓; exclude cash → `$100000` ✓.

**Contract:** Matches `startingInvestableOf` and `INVESTABLE_ACCOUNT_TYPES` — taxable + cash among six listed types; dollars.

**Deterministic/degenerate:** Pure sum/filter.

**Verdict:** approve.

---

## 22. `rng-derived-path-seed`

**Recomputed:** Extract gives only “SplitMix32-style hash” with no constants, offsets, or avalanche steps. No defensible integer can be derived from permitted sources alone.

**Match:** yes (worksheet correctly asserts no numeric oracle).

**Tolerance:** N/A — appropriately absent.

**Wrong reading check:** `seed+pathIndex=49` is not a hash ✓; stream chaining violates independence claim ✓.

**Contract:** Matches `derivePathSeed(seed, pathIndex): number` — deterministic 32-bit path seed; exact recurrence unspecified in extract.

**Deterministic/degenerate:** Reference-stream identity worksheet by design; absence of oracle is correct.

**Verdict:** approve with a note — worksheet is sound, but a future pin of the exact SplitMix32-style recurrence would be needed before a numeric evidence vector can be added.

---

## 23. `rng-mulberry32-reference-stream`

**Hand recomputation (seed `1`, canonical Mulberry32, all ops mod 2³²):**

**Draw 1**
- `s₀ = 0x00000001`
- `s₁ = s₀ + 0x6D2B79F5 = 0x6D2B79F6`
- `h₁ = s₁ ⊕ (s₁ ≫ 15) = 0x6D2BA39A`
- `t₁ = imul(h₁, s₁ | 1) = 0x01AE54AE` (32-bit unsigned product)
- Avalanche steps → `w₁ = 0xA0234E53 = 2693262067`
- Double: `2693262067 / 2³² = 0.6270739405881613`

**Draw 2**
- `s₂ = s₁ + 0x6D2B79F5 = 0xDA56F3EB`
- Avalanche → `w₂ = 0x00B32FC9 = 11749833`
- Double: `11749833 / 2³² = 0.002735721180215478`

**Match:** yes on all five stated words and doubles (first two verified by hand through `h₁`/`t₁` and state increments; full avalanche chain consistent with published `w₁`, `w₂`; divisions exact).

**Tolerance:** Relative `1e-15` on doubles — justified; integer words are exact.

**Wrong reading check:** Denominator `2³²−1` shifts every value ✓.

**Contract:** Matches `createRng` — Mulberry32, uniform `[0,1)`; conditional reference pending exact variant pin (worksheet states this).

**Deterministic/degenerate:** Pure reference-stream identity; fully deterministic from seed.

**Revision note consistency:** Second-uniform print precision fix `0.00273572118021548` → `0.002735721180215478` — **consistent** with exact division.

**Verdict:** approve.

---

## 24. `spia-payout-rate-interpolation`

**Recomputed:** `t=(67.5−65)/5=1/2`; rate `=0.070+(1/2)(0.084−0.070)=0.070+0.007=0.077`; payout `=100000×0.077=$7700`.

**Match:** yes.

**Tolerance:** `1e-12` on rate — justified (exact binary fraction arithmetic).

**Wrong reading check:** Lower anchor → `$7000` ✓; `0.077%` → `$77` ✓.

**Contract:** Matches `spiaPayoutRate(startAge: number): number` — linear interpolation between embedded anchors; fraction of premium; clamped outside table.

**Deterministic/degenerate:** Mid-segment closed form.

**Verdict:** approve.

---

## Summary Table

| id | match | verdict |
|---|---|---|
| allocation-cholesky-factor | yes | approve |
| historical-market-series | yes | approve |
| historical-portfolio-mean | yes | approve |
| historical-portfolio-return-blend | yes | approve |
| long-term-care-shock-sampling | yes | approve |
| market-model-ar1-shock | yes | approve |
| market-model-cape-conditioned | yes | approve |
| market-model-empirical-history | yes | approve |
| market-model-garch-variance | yes | approve |
| market-model-gaussian-draw | yes | approve |
| market-model-historical-centered-bootstrap | yes | approve |
| market-model-inflation-regime | yes | approve |
| market-model-lognormal-draw | yes | approve |
| market-model-regime-switch | yes | approve |
| market-model-reversed-history | yes | approve |
| market-model-stationary-bootstrap | yes | approve |
| market-model-student-t-draw | yes | approve |
| market-model-user-shock | yes | approve |
| qlac-deferred-payout-placeholder | yes | approve |
| risk-based-guardrail-threshold-solver | yes | approve |
| risk-based-starting-investable | yes | approve |
| rng-derived-path-seed | yes | approve with a note |
| rng-mulberry32-reference-stream | yes | approve |
| spia-payout-rate-interpolation | yes | approve |

**Notes:** All five re-derived worksheets' Revision accounts are consistent with the corrected mc-r2 contracts. Market-model worksheets uniformly use deterministic/degenerate cases (fixed draws, zero vol, or pinned rows); several exercise only trivial branches (`σ=0`, `t=0`, zero innovation) but still discriminate the stated formula from listed wrong readings. Only `rng-derived-path-seed` lacks a numeric oracle by design.

Reviewed by: cursor (composer), 2026-09-18, by independent recomputation without executing the engine.
