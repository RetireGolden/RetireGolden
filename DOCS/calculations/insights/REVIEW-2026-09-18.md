# Independent review, 2026-09-18

Reviewer: Cursor (composer-2.5), headless, read-only.

Orchestrator notes, 2026-09-18, added after the pull-request review of #720 read this file:

- The reviewer's section 1 prints the payout arithmetic as "$200,000 × (66/100) = $200,000 × 0.066 = $13,200". The fraction is mistyped (66/100 is 0.66); the product and the result are right (6.6% of $200,000 is $13,200, and the worksheet's $1,100/month follows). The reviewer's text is kept verbatim; this note is the correction.
- The "signature checks" the reviewer cites (b1-p4-signatures-r2.md, b1-p4-signatures-headroom.md) are orchestrator artifacts: parser-built extracts of the engine at the stated commit with every function body elided, produced by the plan's build-signatures-extract.mjs and kept with the plan package in RetireGolden-Docs (calculations/bidirectional-validation-plan-2026-09-13), not in this repository. Anyone can rebuild them from the commit named in the extract header.
- Four of the twelve worksheets were revised after this review on the #720 findings (asset-location on screen()'s preferred-candidate rule, SS-bridge-gap on the household gate, guardrails on the OR/strict screen, state-relocation's Family section); their re-review is recorded in an addendum below when it lands. Scope: the twelve worksheets of slice six (nine insight detectors in this directory, ss-bridge-sizing, scenario-irmaa-surcharge-tier-years, projection-summary-lifetime-roth-conversions), including insight-spending-headroom-rough-annual as re-derived on 2026-09-18. The reviewer recomputed each worked example from the Inputs and Justification sections without executing the engine and without reading any implementation body; the only engine text it consulted was the signatures-and-comments extracts. Its one note (state the start and end years as inputs) was applied to the worksheet before this commit. Verbatim output follows.

---

# Independent Worksheet Review Report

Reviewed 12 calculation worksheets by independent recomputation from Inputs and Justification only (Arithmetic sections not trusted). Signature checks use `b1-p4-signatures-r2.md` and `b1-p4-signatures-headroom.md` as directed.

---

## 1. `insight-annuitization-headroom-illustrative-spia`

**Recomputed values**
- Quarter share: `$800,000 × ¼ = $200,000`
- Premium: `min($200,000, $250,000) = $200,000`
- Annual payout: `$200,000 × (66/100) = $200,000 × 0.066 = $13,200`
- Monthly payout: `$13,200 ÷ 12 = $1,100`

**Match:** yes

**Tolerance:** Exact-cent tolerance is justified; all operations terminate in integer cents.

**Wrong-reading spot check**
- “Always using the cap”: premium `$250,000`, monthly `$250,000 × 0.066 ÷ 12 = $1,375.00` ✓

**Signature consistency:** Claim matches `annuitizationHeadroom.screen`, constants `ILLUSTRATIVE_PREMIUM_FRACTION_OF_LIQUID = 0.25`, `ILLUSTRATIVE_PREMIUM_CAP_DOLLARS = 250_000`, and upstream `spiaPayoutRate(startAge)` (stated as 6.6%/year). Planning age 97 satisfies the 95+ screen. Units: nominal dollars; payout annualized then ÷12.

**Inputs completeness:** Qualifying balance, floor-coverage flag, and resolved payout rate are all stated as inputs.

**Verdict:** approve

---

## 2. `insight-asset-location-swappable-exposure`

**Recomputed values**
- Beneficial candidates (delta > 0): A ($4,000), B ($3,000); C excluded (−$1,000)
- `max($4,000, $3,000) = $4,000` → candidate A
- Published swappable exposure: `$80,000`

**Match:** yes

**Tolerance:** Exact-cent tolerance justified; value is copied metadata.

**Wrong-reading spot check**
- Largest exposure regardless of benefit → C → `$150,000` ✓

**Signature consistency:** Claim matches `assetLocation.screen` / `pickBestBeneficialCandidate` (positive ending-after-tax-estate delta; published field is candidate metadata, not delta). Domain: nominal dollars.

**Inputs completeness:** Candidate table supplies exposure and delta for each row.

**Verdict:** approve

---

## 3. `insight-hecm-buffer-illustrative-credit-line`

**Recomputed values**
- Investable: `$20,000 + $90,000 + $10,000 + $180,000 + $80,000 + $20,000 = $400,000`
- Home-to-investable ratio: `$400,000 ÷ $400,000 = 1` (≥ 0.75)
- Credit line: `$400,000 × (45/100) = $180,000`

**Match:** yes

**Tolerance:** Exact-cent tolerance justified.

**Wrong-reading spot check**
- Multiply by `45` instead of `0.45`: `$400,000 × 45 = $18,000,000` ✓

**Signature consistency:** Claim matches `hecmBufferCandidate.screen`, `MIN_HOME_TO_INVESTABLE_RATIO = 0.75`, upstream `hecmPrincipalLimitFactorPct` (stated as 45%). Investable sum spans cash/taxable/equity comp/traditional/Roth/HSA per detector scope.

**Inputs completeness:** Residence value, factor, all balance buckets, and HECM flag stated.

**Verdict:** approve

---

## 4. `insight-irmaa-tier-edge-premium-cliff`

**Recomputed values**
- Per-person cliff: `$4,200 − $2,400 = $1,800/year`
- Household cliff: `2 × $1,800 = $3,600/year`

**Match:** yes

**Tolerance:** Exact-cent tolerance justified.

**Wrong-reading spot check**
- One enrollee: `$1,800.00` ✓

**Signature consistency:** Claim matches `irmaaTierEdge.screen` cliff convention (above-minus-below, scaled by enrollee count, upstream `medicareAnnualPremiumPerPerson`). MAGI lag (2 years) is domain context for charge-year lookup, not part of this difference arithmetic. Units: nominal dollars/person/year, household-annual total.

**Inputs completeness:** Enrollee count and both resolved premium inputs stated. Lag is stated as pass-through context.

**Verdict:** approve

---

## 5. `insight-spending-guardrails-illustrative-floor`

**Recomputed values**
- Fallback: `0.80 × $60,000 = $48,000/year`
- Explicit: `$42,000/year` (no 80% applied)

**Match:** yes

**Tolerance:** Exact-cent tolerance justified.

**Wrong-reading spot check**
- 80% of first-year investable: `0.80 × $150,000 = $120,000` ✓

**Signature consistency:** Claim matches `spendingGuardrails.screen` (explicit `requiredAnnual` when present; else 80% of base spending from generator patch). Units: nominal dollars/year.

**Inputs completeness:** Both cases’ depletion flag, investable, base spending, and explicit floor presence/value stated. The 80% convention is in the Claim (generator convention), not a separate numeric input—acceptable for a detector worksheet.

**Verdict:** approve

---

## 6. `insight-spending-headroom-rough-annual`

**Recomputed values**
- Ending estate today: `$1,200,000 × (3/4) = $900,000`
- Excess: `$900,000 − $500,000 = $400,000` (≥ $250,000 screen)
- `N = max(1, 2035 − 2026) = max(1, 9) = 9` year boundaries
- Rough headroom: `$400,000 ÷ 9 = 44,444.444444444445` dollars/year (≥ $2,000 screen)
- Whole-dollar format: `$44,444`

**Match:** yes

**Tolerance:** `1e-9` on the quotient is defensible; `400,000/9` is an exact rational with a non-terminating decimal expansion. Whole-dollar display is separate from the numeric quotient.

**Wrong-reading spot check**
- Divide by inclusive row count 10: `$400,000 ÷ 10 = $40,000/year` ✓
- Skip deflation: `($1,200,000 − $500,000) ÷ 9 = 77,777.77777777778/year` ✓

**Revision note:** Yes — Provenance states: *“Replaced the first derivation's guessed inclusive-row-count convention with the specified year-boundary convention `N = max(1, endYear - startYear)`.”*

**Signature consistency:** Claim matches corrected `spendingHeadroom.screen` comment in `b1-p4-signatures-headroom.md`: `E = deflate(endYear, endingAfterTaxEstate)`, `N = max(1, endYear − startYear)`, thresholds `MIN_EXCESS_ESTATE_TODAY_DOLLARS = 250_000`, `MIN_ROUGH_HEADROOM_PER_YEAR = 2_000`, whole-dollar formatting. Units: today’s (deflated) dollars/year.

**Inputs completeness:** Deflation factor, estate, bequest target, and row-count context stated. `startYear`/`endYear` are implied (2026/2035) via the arithmetic and the “10 inclusive rows” note rather than named explicitly in the Inputs table—a minor documentation gap, not an arithmetic error.

**Verdict:** approve with a note (add explicit `startYear`/`endYear` to Inputs for self-containment)

---

## 7. `insight-ss-bridge-gap-total`

**Recomputed values**
- Eligible: A (uncovered, funding 0.75 ≥ 0.5), C (uncovered, funding 0.50 ≥ 0.5)
- Excluded: B (gap already covered)
- `totalCost = $120,000 + $80,000 = $200,000`
- `annualTotal = $18,000 + $12,000 = $30,000/year`

**Match:** yes

**Tolerance:** Exact-cent tolerance justified.

**Wrong-reading spot check**
- Include B: cost `$120,000 + $90,000 + $80,000 = $290,000`; annual `$18,000 + $14,000 + $12,000 = $44,000` ✓

**Signature consistency:** Claim matches `ssBridgeGap.screen` composition over eligible `sizeBridge` rows, `BRIDGE_FUNDING_MIN_FRACTION = 0.5`, exclusion when gap covered. Units: nominal dollars and dollars/year.

**Inputs completeness:** Per-claimant ladder cost, annual bridge, funding ratio, and coverage flag all stated as upstream `sizeBridge`/screen inputs.

**Verdict:** approve

---

## 8. `insight-widows-penalty-bracket-jump`

**Recomputed values**
- Nominal bracket jump: `$28,000 − $18,000 = $10,000`
- Today’s dollars: `$10,000 × (4/5) = $8,000`

**Match:** yes

**Tolerance:** Exact-cent tolerance justified (exact rational arithmetic).

**Wrong-reading spot check**
- Omit deflation: `$10,000` ✓

**Signature consistency:** Claim matches `widowsPenalty.screen`: `J_today = (Tax_single(M) − Tax_joint(M)) × deflate(...)`. Survivor year, MAGI, and both tax results stated as upstream inputs. Units: deflated to today’s dollars.

**Inputs completeness:** Year, MAGI, both tax computations, and deflation factor stated.

**Verdict:** approve

---

## 9. `insight-state-relocation-lifetime-state-tax-savings`

**Recomputed values** (deflate: `amount ÷ 1.03^(year−2026)`)

| Year | Nominal (baseline − FL) | Deflated |
|------|-------------------------|----------|
| 2026 | `$6,000 − $3,000 = $3,000` | `$3,000 ÷ 1.03^0 = $3,000` |
| 2027 | `$6,180 − $0 = $6,180` | `$6,180 ÷ 1.03 = $6,000` (since `$6,000 × 1.03 = $6,180`) |
| 2028 | `$0 − $3,182.70 = −$3,182.70` | `−$3,182.70 ÷ 1.03^2 = −$3,000` (since `$3,000 × 1.03^2 = $3,182.70`) |

- Sum: `$3,000 + $6,000 − $3,000 = $6,000`
- `max(0, $6,000) = $6,000`; whole-dollar display `$6,000`
- Candidate: FL (ties TX at $120,000 lifetime taxes+penalties; strict `<` keeps earlier shortlist row FL)

**Match:** yes

**Tolerance:** `$0.000001` is conservative; with the chosen inputs the deflated terms are algebraically exact integers, so exact-cent would also hold. The stated bound is still defensible for binary-float representation.

**Wrong-reading spot check**
- Per-year floor (drop 2028’s −$3,000): `$3,000 + $6,000 = $9,000` ✓

**Signature consistency:** Claim matches `stateRelocation.evaluate` / `compareRelocationCandidates`: selection on `lifetimeTaxesAndPenalties` with strict `<`; savings identity over union of annual state+local tax series with zero-fill; final `max(0, …)` only. Units: start-year (today) dollars for published savings.

**Inputs completeness:** Selection rows, annual tax series, deflation formula, move-year/month defaults, and baseline residency context all stated as inputs/pass-throughs.

**Verdict:** approve

---

## 10. `ss-bridge-sizing` (`social-security-bridge-sizing`)

**Recomputed values**
- Age 62 year: DOB 1965-01-01 → 62 on 2027-01-01
- `startYear = max(2027, 2026, 2027) = 2027`
- January claim at 65 → claim year 2030; `endYear = 2029` (year before January claim)
- Payment years: 2027, 2028, 2029 → `N = 3`
- `M = $2,000 × (7/10) = $1,400/month`
- `A = 12 × $1,400 = $16,800/year`
- Zero real yield: `ladderCost = 3 × $16,800 = $50,400`

**Match:** yes

**Tolerance:** Exact integers for years; exact-cent for dollar fields.

**Wrong-reading spot check**
- Start in current year 2026 → four payments (2026–2029): `4 × $16,800 = $67,200` ✓

**Signature consistency:** Claim matches `sizeBridge` / `BridgeSizing` fields and comments (start never before age-62 year or next calendar year; January vs mid-year `endYear` rule; ladder cost on supplied curve). Units: today’s dollars; calendar years for window.

**Inputs completeness:** PIA, claim factor, DOB, claim age, current/retirement years, and curve all stated.

**Verdict:** approve

---

## 11. `scenario-irmaa-surcharge-tier-years`

**Recomputed values**
- Baseline tiers `0,1,3,0,5` → indicators `0,1,1,0,1` → count **3**
- Proposal tiers `0,0,2,0,4` → indicators `0,0,1,0,1` → count **2**
- Delta (proposal − baseline): `2 − 3 = −1`

**Match:** yes

**Tolerance:** Exact integer counts; no floating tolerance needed.

**Wrong-reading spot check**
- Sum tier numbers: baseline `0+1+3+0+5 = 9` ✓

**Signature consistency:** Claim matches `compareScenarioPlans` → `ScenarioIrmaaComparison.surchargeTierYears` and `YearResult.irmaaTier` (0 = standard; count where `irmaaTier > 0`). Delta convention `proposal − baseline` per `ComparisonMoneyBasis`.

**Inputs completeness:** Tier sequences for both sides stated.

**Verdict:** approve

---

## 12. `projection-summary-lifetime-roth-conversions`

**Recomputed values**
- `$40,000.00 + $0.00 + $55,500.25 = $95,500.25`

**Match:** yes

**Tolerance:** Exact-cent tolerance justified.

**Wrong-reading spot check**
- Truncate horizon at 2027: `$40,000.00` ✓

**Signature consistency:** Claim matches `summarizeProjection` → `ProjectionSummary.lifetimeRothConversions` as sum of per-year `YearResult.rothConversion` (nominal dollars, all projection rows).

**Inputs completeness:** Annual conversion amounts for each projection year stated.

**Verdict:** approve

---

## Summary Table

| id | match | verdict |
|---|---|---|
| `insight-annuitization-headroom-illustrative-spia` | yes | approve |
| `insight-asset-location-swappable-exposure` | yes | approve |
| `insight-hecm-buffer-illustrative-credit-line` | yes | approve |
| `insight-irmaa-tier-edge-premium-cliff` | yes | approve |
| `insight-spending-guardrails-illustrative-floor` | yes | approve |
| `insight-spending-headroom-rough-annual` | yes | approve with a note |
| `insight-ss-bridge-gap-total` | yes | approve |
| `insight-widows-penalty-bracket-jump` | yes | approve |
| `insight-state-relocation-lifetime-state-tax-savings` | yes | approve |
| `ss-bridge-sizing` | yes | approve |
| `scenario-irmaa-surcharge-tier-years` | yes | approve |
| `projection-summary-lifetime-roth-conversions` | yes | approve |

**Reviewed by:** cursor (composer), 2026-09-18, by independent recomputation without executing the engine.

---

# Addendum, 2026-09-18

After the bot review of PR #720, four worksheets were revised on corrected contracts (the asset-location screen selects the bonds-to-traditional candidate when present, otherwise the first; the SS-bridge gap applies one household gate; the guardrails screen is OR/strict; the state-relocation Family names no feeds). The same reviewer recomputed every case of the four. It approved three and rejected insight-ss-bridge-gap-total for an inverted gate: the orchestrator had given the deriver the suppression comparison (liquid below half the eligible cost, which returns null) as the firing condition, and the reviewer read the worksheet against the funding-fraction comment and caught it. Verbatim output follows; the correction and its re-check are recorded after it.

---

# Independent worksheet review (2026-09-18 addendum)

Recomputed from **Inputs** and **Justification** only; **Arithmetic** treated as untrusted. Signatures cross-check limited to the extract and permitted doc comments/constants.

---

## 1. `insight-asset-location-swappable-exposure`

**Rule (from Claim / corrected header):** publish `metadata.swappedDollars` for id `asset-location-bonds-to-traditional` when present; otherwise the **first** generator candidate. Deltas ignored.

### Case A — preferred id present

| Step | Result |
|------|--------|
| Scan list in order | stocks-to-roth 80k; **bonds-to-traditional 120k**; stocks-to-traditional 150k |
| Preferred id found | `asset-location-bonds-to-traditional` |
| Published exposure | **$120,000** |

**Match:** yes  
**Tolerance:** exact cent — justified (metadata copy).

### Case B — preferred id absent

| Step | Result |
|------|--------|
| List in order | bonds-to-roth **70k** (first); stocks-to-traditional 200k; stocks-to-roth 100k |
| No preferred id | take first → bonds-to-roth |
| Published exposure | **$70,000** |

**Match:** yes  
**Tolerance:** exact cent — justified.

### Wrong readings

| Misread | Case A | Case B | Discriminates? |
|--------|--------|--------|----------------|
| Largest `swappedDollars` | $150,000 | $200,000 | yes — both cases differ from correct |
| Largest-positive-delta (`evaluate()` rule) | $150,000 (delta +20k) | $200,000 (delta +30k) | yes |
| Last candidate | $150,000 | $100,000 | yes — especially case B (70k vs 100k) |

**Claim vs corrected doc comment:** consistent with the 2026-09-18 `assetLocation` header (preferred id, else first; `evaluate()` uses delta).

**Revision note:** correctly names the prior error (using evaluated-benefit / largest-delta selection for `screen()`).

**Verdict:** **approve**

---

## 2. `insight-ss-bridge-gap-total`

**Rule (from Claim / Justification):** sum eligible claimants only; household gate `liquidBalance < 0.5 × totalCost`; no per-claimant funding ratios.

### Single household case

| Step | Result |
|------|--------|
| Eligible | A (uncovered), C (uncovered); **B excluded** (covered) |
| `totalCost` | $120,000 + $80,000 = **$200,000** |
| `annualTotal` | $18,000 + $12,000 = **$30,000/year** |
| Threshold | 0.50 × $200,000 = **$100,000** |
| Gate | $50,000 < $100,000 → **gap reported** (per worksheet) |

**Match (sums and exclusion logic):** yes  
**Match (gate direction vs module comments):** no — see below  
**Tolerance:** exact cent for summed dollars — justified.

### Wrong readings

| Misread | Cost | Annual | Discriminates? |
|---------|------|--------|----------------|
| Include covered B | $290,000 | $44,000/year | yes |
| Average two eligible | $100,000 | $15,000/year | yes |
| Per-claimant 50% gate | A: $50k < $60k ✓; C: $50k ≮ $40k ✗ → **$120,000** / **$18,000** | yes — targets revised household-vs-claimant error |

### Claim vs doc comments

**Inconsistent.** The worksheet Claim and Justification say the card reports when `liquidBalance < 0.5 × totalCost`. Permitted comments say the opposite funding story:

- `ssBridgeGap` header: fires when “liquid savings **could fund** one.”
- `BRIDGE_FUNDING_MIN_FRACTION` in `bridge.ts`: “Minimum share of the quoted ladder cost the funding account **must hold before a bridge is proposed**.”

Those describe a **sufficiency** gate (`liquid ≥ 50%`), not insufficiency. With $50,000 liquid and $200,000 eligible cost, the worksheet says the card fires; the comments imply it should **not** (50k < 100k minimum).

Aggregation math ($200k / $30k) and exclusion of B are correct; the **gate direction** in Claim/Justification conflicts with the detector/bridge comments.

**Revision note:** correctly names claimant-level → household aggregation, but does **not** flag that the inequality direction may still be inverted relative to `bridge.ts`.

**Verdict:** **reject** — inverted liquidity gate vs `ssBridgeGap` header and `BRIDGE_FUNDING_MIN_FRACTION` comment; sums are fine but the firing condition in Claim/Justification is wrong.

---

## 3. `insight-spending-guardrails-illustrative-floor`

**Rule:** screen when **depletion OR** `firstYearInvestable > $100,000` (strict); floor = explicit `requiredAnnual` if present, else `0.80 × baseAnnualSpending`. Constant: `MIN_INVESTABLE_FOR_GUARDRAILS_DOLLARS = 100_000`.

### Case — Fallback

| Step | Result |
|------|--------|
| Screen | depletes = yes → fires |
| Floor | 0.80 × $60,000 = **$48,000/year** |

**Match:** yes

### Case — Explicit

| Step | Result |
|------|--------|
| Screen | depletes = yes → fires |
| Floor | explicit **$42,000/year** (no second 80%) |

**Match:** yes

### Case — Non-depleting above threshold (new)

| Step | Result |
|------|--------|
| Screen | depletes = no; $150,000 **>** $100,000 → fires |
| Floor | fallback 0.80 × $60,000 = **$48,000/year** |

**Match:** yes — discriminates OR-trigger and strict `>` vs `≥`.

### Boundary — exactly $100,000 (Expected note)

| Step | Result |
|------|--------|
| Screen | depletes = no; $100,000 **>** $100,000 is false → **no card** |

**Match:** yes — note is correct and discriminates `>` vs `≥`.

**Tolerance:** exact cent — justified.

### Wrong readings

| Misread | Value | Discriminates? |
|---------|-------|----------------|
| 80% of first-year investable | 0.80 × $150,000 = **$120,000** | yes |
| 80% of explicit floor | 0.80 × $42,000 = **$33,600** | yes |

**Claim vs doc comment:** consistent with `MIN_INVESTABLE_FOR_GUARDRAILS_DOLLARS` comment (“below this… card only fires on a plan that already depletes”) plus strict `>` in Claim.

**Revision note:** correctly names prior trigger error (depletion-only → depletion OR investable strictly > $100k).

**Verdict:** **approve**

---

## 4. `insight-state-relocation-lifetime-state-tax-savings`

**Rule:** select lowest `lifetimeTaxesAndPenalties` among non-baseline, `error=null` rows; strict `<` → FL over TX at 120k tie.  
Savings = `max(0, −Σ deflate(year, candidateTax − baselineTax))` over **union** of years; missing = 0.  
Worksheet uses equivalent baseline−candidate form: `deflate(year, amount) = amount / 1.03^(year−2026)`.

### Selection

FL and TX tied at 120,000; FL order 1 < TX order 2; strict `<` keeps FL. **Selected: FL** ✓

### Year-by-year (baseline − candidate, then negate for savings identity)

| Year | Nominal Δ (B−FL) | Deflator | Contribution |
|------|------------------|----------|--------------|
| 2026 | $6,000 − $3,000 = $3,000 | /1.03⁰ = 1 | **+$3,000** |
| 2027 | $6,180 − $0 = $6,180 | /1.03¹ | $6,180/1.03 = **+$6,000** |
| 2028 | $0 − $3,182.70 = −$3,182.70 | /1.03² | $3,182.70/1.0609 = −$3,000 → **−$3,000** |

| Step | Result |
|------|--------|
| Sum | $3,000 + $6,000 − $3,000 = **$6,000** |
| Floor | max(0, $6,000) = **$6,000** |
| Display | whole-dollar **$6,000** |

**Match:** yes  
**Tolerance:** ±$0.000001 — justified (binary float on 1.03 powers; inputs cancel algebraically to integers).

### Wrong readings (stated values)

| Misread | Stated effect | Discriminates? |
|---------|---------------|----------------|
| Candidate − baseline, then floor | sign reversal | qualitative |
| Per-year floor | drops 2028 −$3k → **$9,000** | yes — $9,000 vs $6,000 |
| TX over FL on tie | wrong candidate | qualitative |
| Intersection vs union | drops orphan years | qualitative |
| Negative / post-failure publish | violates floor | qualitative |

**Claim vs doc comment:** consistent with `stateRelocation` evaluate behavior described in Claim (sweep, strict `<`, union, final floor only). Family **`feeds: none`** matches revision (“relocation series is an input”).

**Revision note:** correctly names Family-direction error (feeds removed).

**Verdict:** **approve**

---

## Summary

| id | Match | Verdict |
|----|-------|---------|
| `insight-asset-location-swappable-exposure` | yes (both cases) | approve |
| `insight-ss-bridge-gap-total` | partial — sums yes; gate direction no vs module comments | **reject** (inverted liquidity inequality) |
| `insight-spending-guardrails-illustrative-floor` | yes (all three cases + $100k boundary) | approve |
| `insight-state-relocation-lifetime-state-tax-savings` | yes | approve |

**Reviewed by:** cursor (composer), 2026-09-18 (addendum), by independent recomputation without executing the engine.

---

# Orchestrator note, 2026-09-18

The SS-bridge-gap rejection was correct and the error was the orchestrator's contract statement, not the deriver's arithmetic: `ssBridgeGap.screen` returns null when `liquid.balance < totalCost * BRIDGE_FUNDING_MIN_FRACTION` (0.5), so the card fires when liquid savings are at least half the eligible cost. Codex Sol corrected the worksheet (the example balance is now $120,000 against a $100,000 gate; the insufficiency gate is the wrong reading). The fixture and receipt follow the corrected worksheet; its approving recomputation is recorded in the next addendum when it lands, and until then its record says reviewedBy unreviewed.

---

# Addendum 2, 2026-09-18

The corrected insight-ss-bridge-gap-total worksheet (the card fires when liquid savings are at least half the eligible bridge cost; example balance $120,000 against a $100,000 gate) was recomputed by the same reviewer and approved with a note: the Revision line names the corrected gate direction but not the inverted inequality itself. For the record, the inverted statement was "the card reports when liquidBalance < 0.5 x totalCost"; the contract is "the card is suppressed when liquidBalance < 0.5 x totalCost". Verbatim output follows.

---

# Independent recomputation review: `insight-ss-bridge-gap-total.md`

Sources: worksheet (corrected 2026-09-18), `BRIDGE_FUNDING_MIN_FRACTION` doc comment via `bridge.ts` signatures/comments (slice6-r2 imports the constant but does not reproduce `bridge.ts`; the funding-fraction text is: *“Minimum share of the quoted ladder cost the funding account must hold before a bridge is proposed”* at 0.5).

---

## 1. Eligible set

**Rule (Justification + Inputs):** include claimants who delay past retirement and whose gap is **not** already covered.

| Claimant | Delays | Covered | Eligible? |
|----------|--------|---------|-----------|
| A | yes | no | **yes** |
| B | yes | yes | **no** (covered) |
| C | yes | no | **yes** |

**Recomputed:** eligible = {A, C}.  
**Worksheet (Arithmetic):** “A and C are eligible; B is excluded.”  
**Match:** yes

---

## 2. Total cost (`totalCost`)

**Step:** sum eligible ladder costs  
= $120,000 (A) + $80,000 (C)  
= **$200,000**

**Worksheet:** $200,000 (Inputs, Arithmetic, Expected).  
**Match:** yes

---

## 3. Annual total (`annualTotal`)

**Step:** sum eligible annual bridge amounts  
= $18,000 (A) + $12,000 (C)  
= **$30,000/year**

**Worksheet:** $30,000/year (Arithmetic, Expected).  
**Match:** yes

---

## 4. Gate arithmetic and direction

**Contract (Justification + Claim):** single household test after aggregation:  
`liquidBalance >= 0.5 × totalCost` → gap reported (card fires).

**Steps:**
1. Threshold = 0.5 × $200,000 = **$100,000**
2. Household liquid balance = **$120,000**
3. $120,000 ≥ $100,000 → **true** → gap reported

**Worksheet:** same threshold, comparison, and conclusion.  
**Match:** yes

**Direction vs funding-fraction comment:**  
Comment requires the funding account to hold **at least half** the quoted cost **before** a bridge is proposed. That implies suppression when balance **&lt;** half and reporting when balance **≥** half. Worksheet uses `>= 0.5 * totalCost`.  
**Gate direction agrees with comments:** yes

---

## 5. Wrong readings — recomputed values

| Wrong reading | Recomputed | Worksheet stated | Match |
|---------------|------------|------------------|-------|
| Include covered claimant B in sums | cost = $120k + $90k + $80k = **$290,000**; annual = $18k + $14k + $12k = **$44,000/year** | $290,000.00; $44,000.00/year | yes |
| Average two eligible claimants (not sum) | cost = ($120k + $80k) / 2 = **$100,000**; annual = ($18k + $12k) / 2 = **$15,000/year** | $100,000.00; $15,000.00/year | yes |
| Insufficiency gate (`liquid < 0.5 × totalCost` fires) at $50k balance | threshold still $100k; $50,000 &lt; $100,000 → correct contract **suppresses**; inverted gate would **fire** | $50,000 balance; suppressed because $50,000 &lt; $100,000 | yes |
| Per-claimant 50% gates vs one post-aggregation gate | qualitative (no numeric target) | stated qualitatively | yes (no numeric mismatch) |

---

## 6. Revision note (Provenance)

**Text:** *“The gate direction was corrected after the independent reviewer read it against the funding-fraction comment.”*

**Earlier error (from review context):** gate was stated as firing on insufficiency (`liquid < 0.5 × totalCost`) instead of sufficiency (`liquid >= 0.5 × totalCost`).

**Assessment:** The note correctly identifies that **gate direction** was wrong and ties the fix to the funding-fraction comment. It does **not** spell out the inverted inequality or the “insufficiency gate” label explicitly. That is accurate but minimal.

**Names the earlier error correctly:** partially — direction error yes; specific backward inequality not named.

---

## 7. Internal consistency

- Inputs table total eligible cost ($200,000) matches summed eligible costs.
- Example balance $120,000 is above the $100,000 gate, so the card fires — consistent with corrected direction.
- Claim, Justification, Arithmetic, Expected, and insufficiency wrong reading are mutually consistent.

No quantity mismatch found.

---

## Verdict: **approve with a note**

All recomputed quantities match. Gate direction now aligns with the funding-fraction comment and the `ssBridgeGap` “liquid savings could fund one” screening intent. Minor note only: Provenance could name the prior error explicitly (e.g. “was stated as `liquid < 0.5 × totalCost` fires”) rather than only “gate direction was corrected”; not a calculation defect.

Reviewed by: cursor (composer), 2026-09-18 (addendum 2), by independent recomputation without executing the engine.
