# Independent review, 2026-09-18

Reviewer: Cursor (composer-2.5), headless, read-only. Scope: the fourteen worksheets of the accounts-and-growth and spending slice (nine in this directory, five in spending-and-withdrawals). The reviewer recomputed each worked example from the Inputs and Justification sections without executing the engine and without reading any implementation body; the only engine text it consulted was the signatures-and-comments extract. Verbatim output follows.

---

# Independent Worksheet Review Report

Branch: `claude/b1-p4-cards-accounts-spending`  
Signatures extract: `b1-p4-signatures.md` (doc comments and signatures only; no engine bodies read)

---

## 1. `allocation-account-expected-return`

**Recomputed:** Allocation present → blend only.  
`0.6 × 7 + 0.4 × 4 = 4.2 + 1.6 = 5.8` (%/yr nominal). Account scalar 9% and plan default 5% are not used.

**Match:** yes (`5.8%`)

**Tolerance (`1e-12` pp):** justified — exact rational arithmetic on terminating decimals.

**Wrong reading checked:** Account scalar override → `9%` ✓. Unweighted class average → `(7+4)/2 = 5.5%` ✓.

**Signature consistency:** `expectedAccountReturnPct` doc: allocation blend when opted in supersedes account `annualReturnPct`, else account return, else plan default; returns nominal %/yr. Units, precedence, and timing (“this year”) align with the worksheet.

**Verdict:** approve

---

## 2. `allocation-blended-expected-return`

**Recomputed:**  
`0.6(7) + 0.4(4) = 4.2 + 1.6 = 5.8` percentage points.

**Match:** yes (`5.8%`)

**Tolerance (`1e-12` pp):** justified — exact finite decimal product.

**Wrong reading checked:** Weights divided by 100 again → `0.006(7)+0.004(4)=0.058%` ✓. Weighted geometric one-period return: `exp(0.6·ln(1.07)+0.4·ln(1.04))−1 ≈ 5.79%` ✓ (not `5.8%`).

**Signature consistency:** `blendedReturnPct(weights, params)` → blended expected **nominal** return in **percent**; weight-fraction arithmetic mean matches claim.

**Verdict:** approve

---

## 3. `allocation-blended-taxable-yield`

**Recomputed:**  
- Interest: `0.6(0)+0.4(4)=1.6%`  
- Dividends: `0.6(1.5)+0.4(0)=0.9%`  
- Qualified dividend yield: `0.6×1.5×0.95=0.855%`  
- Qualified ratio: `0.855/0.9 = 855/900 = 19/20 = 0.95`

**Match:** yes (interest `1.6%`, dividends `0.9%`, ratio `0.95`)

**Tolerance (`1e-12`):** justified — all values are exact terminating decimals/rationals.

**Wrong reading checked:** Weight qualified ratios by all assets → `0.6(0.95)+0.4(0)=0.57` ✓.

**Signature consistency:** `blendedTaxableYield` returns `BlendedTaxableYield` with `interestYieldPct`, `dividendYieldPct`, `qualifiedRatio` (0–1, `DEFAULT_QUALIFIED_DIVIDEND_RATIO = 0.85` fallback when no dividends). Worksheet’s dividend-dollar-weighted ratio and percent yields align; fallback not exercised in this example.

**Verdict:** approve

---

## 4. `allocation-glidepath-interpolation`

**Recomputed (2025 probe):**  
`t = (2025−2020)/10 = 5/10 = 1/2`  
US: `0.8 + (1/2)(−0.2) = 0.8 − 0.1 = 0.7`  
Bonds: `0.2 + (1/2)(0.2) = 0.3` → `[0.7, 0.3]`

**2015 (clamp to first endpoint):** `[0.8, 0.2]`  
**2035 (clamp to last endpoint):** `[0.6, 0.4]`

**Match:** yes

**Tolerance (`1e-12` per component):** justified — exact binary fractions.

**Wrong reading checked:** Linear extrapolation at 2015 (no clamp): `t=(2015−2020)/10=−1/2` → US `0.8+(−1/2)(−0.2)=0.9`, bonds `0.1` → `[0.9,0.1]` ✓ (worksheet’s stated wrong value).

**Signature consistency:** `targetWeightsAt(policy, year)` — linear/custom interpolate and clamp outside first/last; staged is step-from-`fromYear`. Calendar-year probe and normalized fraction outputs match.

**Verdict:** approve

---

## 5. `allocation-non-cash-share`

**Recomputed:**  
Sum non-cash: `0.5+0.1+0.3=0.9`  
Cross-check: `1−0.1=0.9`

**Match:** yes (`0.9`)

**Tolerance (`1e-12`):** justified — exact sum.

**Wrong reading checked:** Cash share returned → `0.1` ✓.

**Signature consistency:** `nonCashWeight(weights)` — non-cash share of weight vector (single-factor shock portion). Fixed class order implied by vector position matches worksheet layout.

**Verdict:** approve

---

## 6. `allocation-rebalance-turnover`

**Recomputed:**  
`max(0, 0.7−0.6)=0.1`; `max(0, 0.3−0.4)=0`  
Turnover sold `= 0.1`

**Match:** yes (`0.1`)

**Tolerance (`1e-12`):** justified — exact.

**Wrong reading checked:** Sum of absolute differences → `|0.1|+|−0.1|=0.2` ✓.

**Signature consistency:** `rebalanceTurnoverFraction(current, target)` — Σ positive overweights; fractions summing to 1. Matches.

**Verdict:** approve

---

## 7. `allocation-total-return-drift`

**Recomputed:**  
Ending wealth: `0.6×1.10=0.66`; `0.4×0.95=0.38`; total `=1.04`  
Renormalized weights:  
`0.66/1.04 = 66/104 = 33/52 ≈ 0.6346153846153846…`  
`0.38/1.04 = 38/104 = 19/52 ≈ 0.3653846153846154…`

**Match:** yes (`[33/52, 19/52]`)

**Tolerance (`1e-12` per weight):** justified — exact rational weights.

**Wrong reading checked:** Add returns to weights → `[0.7, 0.35]`, sum `1.05` ✓. Skip renormalize → `[0.66, 0.38]`, sum `1.04` ✓.

**Signature consistency:** `driftWeights(weights, ratesPct)` — multiply by `1+r_i/100`, renormalize; distributions reinvested. One-year drift, fraction weights, percent returns — consistent.

**Verdict:** approve

---

## 8. `allocation-weight-normalization`

**Recomputed:**  
`W = 60+20+20+0 = 100`  
Vector `= [60/100, 20/100, 20/100, 0/100] = [0.6, 0.2, 0.2, 0]`; sum `=1`.

**Match:** yes

**Tolerance (`1e-12` per component):** justified — exact rationals.

**Wrong reading checked:** Raw percentages `[60,20,20,0]`, sum `100` ✓.

**Signature consistency:** `weightsToVector(weights)` → normalized fractions in `ASSET_CLASS_IDS` order (US, international, bonds, cash). Pin name matches worksheet id; no rounding stated in signature.

**Verdict:** approve

---

## 9. `asset-class-parameter-overrides`

**Recomputed:**  
Bonds: override return `5.0` present → resolved return `5.0`; volatility absent in override → keep default `7.7`.  
US stocks: no override → return stays `7.0`.

**Match:** yes (bonds `5.0/7.7`, US stocks `7.0`)

**Tolerance (“exact at supplied one-decimal precision”):** justified — field-level merge, no arithmetic beyond selection.

**Wrong reading checked:** Whole-record replacement drops inherited volatility (would not remain `7.7`) ✓.

**Signature consistency:** `resolveAssetClassParams(overrides)` — “Assumption overrides merged over the sourced defaults (absent = default).” Partial overlay semantics match; rates remain annual percent values per module context.

**Verdict:** approve

---

## 10. `abw-expected-real-return`

**Recomputed (exact fractions):**  
`w = 60/100 = 3/5`  
Equity yield `= 100/25 = 4` (%/yr real)  
Blend `= (3/5)(4) + (2/5)(2) = 12/5 + 4/5 = 16/5 = 3.2` (%/yr real)

**Note:** Review brief asked for an annuity-factor recomputation; this worksheet has no rate/horizon annuity inputs — only the CAPE blend above applies.

**Match:** yes (`3.2` %/yr real)

**Tolerance (`1e-12`):** justified for these exactly representable inputs; short closed-form expression.

**Wrong reading checked:** CAPE yield alone (100% equity) → `4.0%` ✓. Treating `60` as fraction `w=60` → `60(4%)+(1−60)(2%)=122%` ✓.

**Signature consistency:** `abwExpectedRealReturnPct` doc: CAPE branch `100/CAPE` blended with bond real yield at equity share; returns expected real %/yr. Worksheet’s `w(100/CAPE)+(1−w)b` with `w` as fraction converted from percent matches.

**Verdict:** approve with a note (annuity-factor instruction N/A to this worksheet)

---

## 11. `balance-risk-guardrail-step`

**Recomputed:**  
Lower trigger `= 100 × (80/100) = 80` real dollars  
`70 < 80` → cut one step: `0.8 − 10/100 = 0.7`  
→ `{multiplier: 0.7, action: 'cut'}`

**Thresholds vs extract (`spending/guardrails.ts`):**  
- `adjustmentPct` default `DEFAULT_ADJUSTMENT_PCT = 10` — worksheet uses `10` ✓  
- Balance mode uses `lowerBalanceThresholdPct` (solver-sourced; no numeric default in extract); worksheet supplies `80` explicitly ✓  
- `nextBalanceGuardrailMultiplier` compares **real** current balance to **percent of starting portfolio** ✓

**Match:** yes

**Tolerance (`1e-12` on multiplier):** justified — exact decimal subtraction.

**Wrong reading checked:** Treat `80` as multiplier not percent → threshold `100×80=8000`; `70<8000` still cuts (spurious “always cut”) ✓.

**Signature consistency:** Real-dollar signal, one `adjustmentPct` step, clamp `[0, maxMultiplier]`, `'hold'` when bound blocks move — all align with doc comment.

**Verdict:** approve

---

## 12. `spending-shape-annual-delta-phases`

**Recomputed:** `delta = −2%` → factor `(1−0.02)=0.98`; retirement `65`; step ages `65+5=70`, `65+10=75` (`STEP_YEARS=5` in extract).

- Age 70: `0.98^5 = 0.9039207968` → round 2 dp → `0.90`  
- Age 75: `0.98^10 = (0.98^5)^2 = 0.817072806887547` → round 2 dp → `0.82`

**Match:** yes (ages `70,75`; multipliers `0.90,0.82`)

**Tolerance (“exact to declared two-decimal rounding”):** justified — discrete rounding rule, not a continuous solver.

**Wrong reading checked:** Linear `1−0.02×10=0.80` at 75 vs compound `0.82` ✓.

**Signature consistency:** `annualDeltaPhases(deltaPct, retirementAge)` — `(1+delta/100)^(yearsSinceRetirement)` at 5-year steps to age 100, round 2 dp, clamp `[0,3]`; zero delta → no phases. Matches.

**Verdict:** approve

---

## 13. `spending-shape-preset-compilation`

**Recomputed (smirk):** Extract: `SMIRK_ANNUAL_REAL_DELTA_PCT = -1`; smirk delegates to `annualDeltaPhases(-1, 65)`.

- Age 70: `0.99^5 = 0.9509900499` → `0.95`  
- Age 75: `0.99^10 = 0.904382075008805` → `0.90`  
Flat → empty phase list (constant-real emits no phases per module doc).

**Match:** yes (`(70,0.95)`, `(75,0.90)`; flat `[]`)

**Tolerance (“exact after two-decimal rounding”):** justified.

**Wrong reading checked:** First step at retirement `65` would give multiplier `0.99^0=1.00` (off-by-one) — consistent with `STEP_YEARS=5` starting at `70` ✓.

**Signature consistency:** `spendingShapePhases(shape, retirementAge)`; `SpendingShapeId` includes `'smirk'`; smirk constant `−1` %/yr real confirmed in extract. Compilation-to-visible phases, no live coupling — matches claim.

**Verdict:** approve

---

## 14. `withdrawal-rate-guardrail-step`

**Recomputed:**  
Upper trigger `= 1.20 × 4% = 4.8%`  
`6% > 4.8%` → cut: `0.8 − 10/100 = 0.7` (within `[0,1]`)  
→ `{multiplier: 0.7, action: 'cut'}`

**Thresholds vs extract (`spending/guardrails.ts`):**  
- `DEFAULT_UPPER_GUARDRAIL_PCT = 120` — worksheet `120` ✓  
- `DEFAULT_ADJUSTMENT_PCT = 10` — worksheet `10` ✓  
- `nextGuardrailMultiplier` uses `targetSpending/startOfYearPortfolio` rates vs starting-year rate ✓

**Match:** yes

**Tolerance (`1e-12` on multiplier):** justified.

**Wrong reading checked:** Cut by 10% **of** multiplier → `0.8−0.08=0.72` ✓. Compare `6` directly to `120` → hold (wrong) ✓.

**Signature consistency:** Withdrawal-rate mode, upper/lower bands as % of starting rate, `adjustmentPct` as percentage points of full discretionary layer, clamp `[0, maxMultiplier]` — consistent.

**Verdict:** approve

---

## Summary Table

| id | match | verdict |
|---|---|---|
| `allocation-account-expected-return` | yes | approve |
| `allocation-blended-expected-return` | yes | approve |
| `allocation-blended-taxable-yield` | yes | approve |
| `allocation-glidepath-interpolation` | yes | approve |
| `allocation-non-cash-share` | yes | approve |
| `allocation-rebalance-turnover` | yes | approve |
| `allocation-total-return-drift` | yes | approve |
| `allocation-weight-normalization` | yes | approve |
| `asset-class-parameter-overrides` | yes | approve |
| `abw-expected-real-return` | yes | approve with a note |
| `balance-risk-guardrail-step` | yes | approve |
| `spending-shape-annual-delta-phases` | yes | approve |
| `spending-shape-preset-compilation` | yes | approve |
| `withdrawal-rate-guardrail-step` | yes | approve |

**Overall:** 14/14 arithmetic matches; 0 rejects; 1 approve-with-note (`abw-expected-real-return`: no annuity factor in worksheet inputs).

Reviewed by: cursor (composer), 2026-09-18, by independent recomputation without executing the engine.
