# Independent review, 2026-09-18

Reviewer: Cursor (composer-2.5), headless, read-only. Scope: the fourteen worksheets of the cash-flow, optimizer and taxes slice (ten in this directory, two in optimizer-and-comparisons, two in taxes). The reviewer recomputed each worked example from the Inputs and Justification sections without executing the engine and without reading any implementation body; the only engine text it consulted was the signatures-and-comments extract. Verbatim output follows.

---

# Independent Calculation Worksheet Review

Branch: `codex/b1-p4-cards-cashflow-optimizer-taxes`  
Authority: worksheet Inputs/Justification + pinned signatures in `b1-p4-signatures.md` only. No engine execution.

---

## 1. `civil-date-parse-format-and-month-shift`

**Recomputed**
- Parse `2024-01-31` → `{year: 2024, month: 1, day: 31}`.
- 2024 is leap (÷4, century exception does not apply) → February length = 29.
- Jan 31 + 1 month → day = min(31, 29) = 29 → `2024-02-29`.

**Match:** yes (exact).

**Tolerance:** exact strings/integers — justified.

**Wrong-reading check:** JS rollover to `2024-03-02` is the standard Date pitfall; nonleap February gives `2024-02-28` — both plausible wrong answers.

**Signature consistency:** `parseCivilIsoDate`, `formatCivilDate`, `addCalendarMonths` — proleptic Gregorian, clamp-to-month-end, no rollover. Units/timing consistent.

**Verdict:** approve

---

## 2. `exact-cent-largest-remainder-slices`

**Recomputed**
- Total W = 3; exact shares = 10/3 = 3 + ⅓ each.
- Floor: [3, 3, 3], sum = 9, residual = +1.
- Remainders tied at ⅓; position 0 wins → **[4, 3, 3]**, sum = 10.

**Match:** yes (exact cents).

**Tolerance:** exact integers — justified.

**Wrong-reading check:** independent round → [3, 3, 3], sum 9 (off by 1); last-position tie-break → [3, 3, 4] — both verified.

**Signature consistency:** `exactCentLargestRemainderSlices(amountMinorUnits, weightMinorUnits[])` — nonnegative bigint minor units; ties by position. Consistent.

**Verdict:** approve

---

## 3. `exact-cent-pro-rata-half-up`

**Recomputed**
- N = 101 × 1 = 101; D = 2.
- 101 = 50×2 + 1 → q = 50, r = 1.
- 2r = 2 ≥ D → round up → **51** cents.

**Match:** yes (exact).

**Tolerance:** exact integer — justified.

**Wrong-reading check:** tie-to-even or floor both yield 50 — verified.

**Signature consistency:** `exactCentProRataNearestHalfUp(amount, ratioNum, ratioDen)` — half-up on exact rational. Consistent.

**Verdict:** approve

---

## 4. `exact-cent-rational-half-up`

**Recomputed**
- N = 5, D = 2 → 5 = 2×2 + 1, r = 1.
- 2r = D → exact half → round up → **3** cents.

**Match:** yes (exact).

**Tolerance:** exact integer — justified.

**Wrong-reading check:** banker's → 2; treating denominator as monetary weight → 5 — verified.

**Signature consistency:** `exactCentNearestHalfUp(numeratorMinorUnits, denominatorMinorUnits)` — degenerate pro-rata, half-up. Consistent.

**Verdict:** approve

---

## 5. `flexible-goal-scheduling`

**Recomputed**
- Nominal cost = 1,000 × 1.10 = **1,100**.
- Minimum = 1,100 × 0.50 = **550**.
- Budget 700: 550 ≤ 700 < 1,100 → partial fund **700**; unfunded = 1,100 − 700 = **400**; remaining = 0.

**Match:** yes (exact dollars).

**Tolerance:** exact for integer inputs — justified.

**Wrong-reading check:** 50% of today's $1,000 → threshold $500; at budget $700 both wrong ($500) and correct ($550) thresholds allow partial funding, so this input does **not** discriminate that wrong reading (would matter e.g. at budget $525).

**Signature consistency:** `createGoalScheduler` — `minFundingPct`, `allowPartialFunding`, classification/priority/order ordering. Nominal inflation before minimum test is consistent with `amountTodayDollars` + inflation in model context.

**Verdict:** approve with a note — first wrong reading is valid conceptually but not discriminating at the stated $700 budget.

---

## 6. `lifestyle-required-discretionary-split`

**Recomputed**
- R = min(max(70,000, 0), max(60,000, 0)) = min(70,000, 60,000) = **60,000**.
- D = max(60,000, 0) − 60,000 = **0**.
- R + D = 60,000 ✓

**Match:** yes (exact).

**Tolerance:** exact dollars — justified (no rounding stated).

**Wrong-reading check:** no clamp → required 70,000, discretionary −10,000; swap roles → required 0, discretionary 60,000 — verified.

**Signature consistency:** `splitLifestyle(baseAnnualNominal, requiredAnnualNominal)` — clamp floor to target, layers sum to base. Consistent.

**Verdict:** approve

---

## 7. `projection-money-tolerance-thresholds`

**Recomputed**
- 0.0049 ≤ 0.005 → annual acceptance **true**.
- 0.0075 < 0.01 → conversion materiality **false**.
- Ratio 0.01 / 0.005 = **2**.

**Match:** yes (exact Booleans).

**Tolerance:** exact for stated decimals — justified.

**Wrong-reading check:** if funding tolerance were 0.01, 0.0075 would pass (incorrect); reading 0.005 as $0.05 loosens tenfold — verified.

**Signature consistency:** `ANNUAL_FUNDING_TOLERANCE_PLAN_DOLLARS = 0.005`, `AGGREGATE_ROTH_CONVERSION_EPSILON_PLAN_DOLLARS = 0.01` — distinct constants, unrounded plan dollars. Consistent.

**Verdict:** approve

---

## 8. `spending-layer-shortfall-attribution`

**Recomputed** (A = max(0, F − max(0, W)))

**Case (a):** F=85, W=5 → A=80  
- Rmiss = max(0, 60−80) = **0**  
- Tmiss = max(0, 100−80) = **20**  
- idealFunded = max(0, min(20, 80−100)) = 0 → Imiss = **20**  
- excessFunded = max(0, min(10, 80−120)) = 0 → Xmiss = **10**

**Case (b):** F=62, W=5 → A=57  
- Rmiss = max(0, 60−57) = **3**  
- Tmiss = max(0, 100−57) = **43**  
- Imiss = **20**, Xmiss = **10**

**Match:** yes — 0/20/20/10 and 3/43/20/10.

**Tolerance:** 1e-12 — justified (only min/max/subtraction on small integers).

**Wrong-reading check:** (a) F as delivered → Tmiss = 100−85 = **15** ✓; charge full W to floor → Rmiss = **5** ✓; (b) max(0, W−(T−R)) = max(0, 5−40) = **0** ✓.

**Signature consistency:** `attributeShortfall` — `fundedSpending` is attempted (`expenses.total`); `withdrawalShortfall` separate; target miss ⊇ required miss. Worksheet's A = F−W matches doc-comment contract. Layer inputs map to `requiredSpending`, `targetSpending`, `idealSpending`, `excessSpending`.

**Verdict:** approve

---

## 9. `sustainable-spending-bisection`

**Recomputed** (integer midpoints, resolution $1,000)

| Step | Bracket | Midpoint | Result |
|------|---------|----------|--------|
| 0 | [60,000, 70,000] | 65,000 | infeasible → [60,000, 65,000] |
| 1 | [60,000, 65,000] | 62,500 | feasible → [62,500, 65,000] |
| 2 | [62,500, 65,000] | 63,750 | infeasible → [62,500, 63,750] |
| 3 | [62,500, 63,750] | 63,125 | infeasible → [62,500, 63,125] |

Width = 63,125 − 62,500 = **625 ≤ 1,000** → converged; feasible lower bound = **$62,500**.

**Match:** yes.

**Tolerance:** exact integer dollars — justified; matches `resolutionDollars` stopping rule and feasible lower-bound return in doc comment.

**Wrong-reading check:** upper bound 63,125; average 62,812.50 (never probed feasible) — verified.

**Signature consistency:** `solveMaxSustainableSpending` — bisection on `expenses.baseAnnual` (today's $), integer midpoints, stop when bracket ≤ `resolutionDollars`, return feasible lower bound. Degenerate oracle inputs are explicitly stated.

**Verdict:** approve

---

## 10. `usd-cent-arithmetic`

**Recomputed**
- 125 + 25 = **150**
- 125 − 25 = **100**
- 125 + 25 + 50 = **200**

**Match:** yes (exact cents).

**Tolerance:** exact integers — justified.

**Wrong-reading check:** dollars confusion → $150 vs $1.50; reversed subtraction → −100 outside `UsdCents` — verified.

**Signature consistency:** `addUsdCents`, `subtractUsdCents`, `sumUsdCents` on validated nonnegative integer cents. Consistent.

**Verdict:** approve

---

## 11. `conversion-coordinate-descent-search`

**Recomputed** (oracle score table)
- $0 → $10k: 100→130, Δ=30 > $1 min → keep $10k.
- $0 → $20k: 125 < 130 → reject.
- $10k → $12.5k (fine +$2,500): 130→135, Δ=5 > $1 → keep **$12,500**.

**Match:** yes — best $12,500, improved true.

**Tolerance:** exact given stated oracle — justified.

**Wrong-reading check:** accept all moves → $20k at 125; require Δ ≥ coarse step → stay at $0 — verified.

**Signature consistency:** `refineConversionSchedule` — coordinate descent, `DEFAULT_MINIMUM_IMPROVEMENT = 1`, coarse/fine steps $10k/$2.5k. Simplified single-year oracle is a stated degenerate case.

**Verdict:** approve

---

## 12. `swr-rule-rate-and-initial-spend`

**Recomputed** (starting investable $1,000,000, CAPE 25)
- Bengen: 4.7% → 1,000,000 × 0.047 = **$47,000**
- Morningstar: 3.9% → **$39,000**
- ERN: 1.75 + 0.5×(100/25) = 1.75 + 2 = **3.75%** → 1,000,000 × 0.0375 = **$37,500**

**Match:** yes.

**Tolerance:** 1e-12 pp / 1e-8 dollars — conservative; products are exact here.

**Wrong-reading check:** omit 0.5 weight → 5.75%, $57,500; multiply by 3.75 not 0.0375 → $3,750,000 — verified.

**Signature consistency:** `compareSwrRules` / `SWR_RULES` — Bengen 4.7%, Morningstar 3.9%, ERN `1.75 + 0.5*(100/CAPE)`; `initialAnnualSpend = rate × startingInvestable` in today's dollars. Consistent.

**Verdict:** approve

---

## 13. `conversion-tax-funding-gcd-reduction`

**Recomputed** (Euclid on 150, 100)
- 150 = 1×100 + 50  
- 100 = 2×50 + 0 → gcd = **50**  
- Reduced: 150/50 = **3**, 100/50 = **2** → **3/2** (1.5 cents)

**Match:** yes (numeratorMinorUnits=3, denominator=2).

**Tolerance:** exact integer equality — justified.

**Wrong-reading check:** divide numerator only → 3/100; stop at factor 10 → 15/10 — verified.

**Signature consistency:** `reducedConversionTaxFundingExactCentAmount(numeratorMinorUnits, denominator)` — lowest-terms coprime pair, null on invalid domain. Consistent.

**Verdict:** approve

---

## 14. `parameter-provenance-catalog`

**Recomputed**
- IDs in justification: 15 listed.
- Extract `PARAMETER_PROVENANCE`: 15 entries, IDs in order  
  `federal-brackets`, `senior-deduction`, `capital-gains-niit`, `section-121-exclusion`, `ss-benefit-taxation`, `contribution-limits`, `rmd-qcd`, `annuity-purchase`, `hecm-plf`, `medicare-irmaa`, `social-security`, `federal-poverty-line`, `aca-ptc`, `real-yield-curve`, `state-income-tax`.
- Distinct count = 15; duplicates = 0.

**Match:** yes (structural count only, as Expected states).

**Tolerance:** exact integers — justified for counting.

**Wrong-reading check:** omit `real-yield-curve` → 14; count states inside `state-income-tax` summary → >15 — verified.

**Signature consistency:** `PARAMETER_PROVENANCE: ParameterSource[]` — ordered catalog, no calculation. Listed IDs match extract verbatim.

**Verdict:** approve

---

## Summary Table

| id | match | verdict |
|---|---|---|
| civil-date-parse-format-and-month-shift | yes | approve |
| exact-cent-largest-remainder-slices | yes | approve |
| exact-cent-pro-rata-half-up | yes | approve |
| exact-cent-rational-half-up | yes | approve |
| flexible-goal-scheduling | yes | approve with a note |
| lifestyle-required-discretionary-split | yes | approve |
| projection-money-tolerance-thresholds | yes | approve |
| spending-layer-shortfall-attribution | yes | approve |
| sustainable-spending-bisection | yes | approve |
| usd-cent-arithmetic | yes | approve |
| conversion-coordinate-descent-search | yes | approve |
| swr-rule-rate-and-initial-spend | yes | approve |
| conversion-tax-funding-gcd-reduction | yes | approve |
| parameter-provenance-catalog | yes | approve |

**14/14 arithmetic matches.** One note: `flexible-goal-scheduling` wrong-reading #1 does not change the outcome at the stated $700 budget (would need a budget between $500 and $550 to discriminate).

Reviewed by: cursor (composer), 2026-09-18, by independent recomputation without executing the engine.
